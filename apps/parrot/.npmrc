# Parrot (Workspace Worker) has peer-dep ranges that npm's strict resolver
# rejects; installs here use legacy peer resolution. Pinning it here makes
# `npm ci` / `npm install` deterministic locally and in CI (no per-command flag).
legacy-peer-deps=true
