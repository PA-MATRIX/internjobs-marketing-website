{
  "name": "@internjobs/ai-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that proxies Workers AI (embeddings + chat completions) to the Fly Node app. Uses native Worker AI bindings so the Node side never needs a Cloudflare API token. Optionally routes through an internjobs-ai AI Gateway when that gateway exists in the CF account.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail internjobs-ai-proxy",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
