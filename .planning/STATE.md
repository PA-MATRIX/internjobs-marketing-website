---
schema_version: 2
milestone: "v1.2"
phase: 13
phase_name: "Cross-pane Actions + Launch Polish"
phase_total: 13
plan: 3
plan_total: 3
status: "phase_13_complete"
progress: 100
last_activity: "2026-05-19"
session_last: "2026-05-19"
# 2026-05-19: PHASE-13 WAVE 3 COMPLETE — 13-03 (Onboarding Wizard + Pilot Readiness) shipped via /rrr:execute-plan in 7m 1s. 2 atomic commits on main (e97bffe feat + e0b3e81 feat). Wave 3 deliverables: (1) Migration `5_onboarding_flags` on EmployeeMailboxDO — two NULL-safe ALTER TABLE profile ADD COLUMN statements (`onboarded_at TEXT`, `feature_flags TEXT`). NULL-safe (no DEFAULT, no UPDATE pass) means existing rows interpret as "not onboarded yet", mapping cleanly to "show the wizard next login." (2) Three new DO methods: `setOnboardedAt()` (UPDATE profile … now() — only path to flip the gate); `getFeatureFlags()` (merges KV `global_defaults` overlaid with per-employee JSON overrides from `profile.feature_flags`, employee wins; degrades to default-all-on `{cross_pane:true,push:true,onboarding_wizard:true}` when KV unbound OR on ANY error — over-grant > lock-out posture); `isFeatureEnabled(flag)` (missing key === enabled, default-on with explicit-false override). (3) `EmployeeProfile.onboardedAt: string | null` added; `getProfile()` SELECTs the new column. `upsertProfile()` deliberately UNTOUCHED so onboarded_at is only writable via the explicit setOnboardedAt() gate (prevents accidental wipes). (4) Env extensions in `types.ts`: `PARROT_FEATURE_FLAGS?: KVNamespace` + `SENTRY_DSN?: string` (both added to CfEnvBase Omit tuple). Both OPTIONAL so the Worker boots without either. (5) Inline `reportToSentry(env, err, ctx)` helper in `workers/index.ts` — parses DSN URL, posts JSON event to `/api/{projectId}/store/` with `X-Sentry-Auth: Sentry sentry_key=…,sentry_version=7`, fire-and-forget. NO `@sentry/cloudflare` dependency (same posture as inline VAPID signer in Wave 1: when one short fetch can do the job, don't pull in a transitive-dep-heavy SDK). Wired into `app.onError` Hono global handler at END of route chain. SENTRY_DSN absent → falls back to `console.error` only. (6) `GET /healthz` public no-auth endpoint — returns `{ mattermost_reachable: bool (2s timeout on /api/v4/system/ping), ai_gateway_reachable: bool (5s timeout on 1-token llama-3.1-8b completion via gateway.ai.cloudflare.com — the sanctioned LLM URL pattern), mailbox_count: -1 (placeholder until v1.3 WorkspaceDO.countEmployees() RPC) }`. Used by pre-launch curls + uptime monitors. (7) Three new Hono routes — `POST /api/onboarding/complete` (optionally upsertProfile with new display_name from step 1, then setOnboardedAt — owns the wizard's completion gate); `GET /api/feature-flags` (returns merged flag map); `POST /api/dev/smoke/onboarding` (PARROT_DEV_MODE-gated — deterministic assertions on onboarded_at flip + flag map + isFeatureEnabled defaults). (8) `/api/me` extended to return `onboarded_at` field (canonical signal the wizard reads to decide whether to render). (9) `wrangler.jsonc` — `kv_namespaces` block declaring `PARROT_FEATURE_FLAGS` (empty `id` field; user fills after `wrangler kv namespace create PARROT_FEATURE_FLAGS --name internjobs-parrot`). `SENTRY_DSN` secret comment block added next to existing PUSH_VAPID_PRIVATE_KEY block. (10) `app/lib/api.ts` updates: `MeResponse.onboarded_at: string | null`; new `api.getFeatureFlags()` and `api.completeOnboarding({display_name?, push_enabled?})` helpers. (11) NEW `apps/parrot/app/components/OnboardingWizard.tsx` (293 lines) — 3-step modal: Step 1 confirm display name (pre-filled, editable, Next disabled when empty after trim); Step 2 browser push opt-in toggle (default OFF; on enable → `Notification.requestPermission()` → `navigator.serviceWorker.ready` → `pushManager.subscribe({applicationServerKey: urlBase64ToUint8Array(vapidPublicKey)})` → `api.subscribePush(subscription)`; full status state machine idle/requesting/granted/denied/unavailable with copy for each; toggle disabled gracefully when VAPID key empty or Notification API absent); Step 3 Mattermost ping placeholder + Finish (POST /api/onboarding/complete → invalidate ['parrot','me'] query → setOpen(false)). Skippable via × button (per-visit dismiss only) — server flag is canonical so wizard re-renders next page load until onboarded_at flips. Inline `urlBase64ToUint8Array` helper for VAPID key conversion. (12) `app/root.tsx` — loader extended to return `vapidPublicKey: env.PUSH_VAPID_PUBLIC_KEY` (safe-to-ship — public half of keypair). New `AppShell({vapidPublicKey})` component lives INSIDE QueryClientProvider tree (required for useCurrentEmployee → React Query), renders <Outlet /> + conditional <OnboardingWizard initialDisplayName={me.display_name} vapidPublicKey={vapidPublicKey}> when `me?.onboarded_at === null`. AppShell wraps both ClerkProvider branch AND the no-Clerk fallback branch. (13) NEW `.planning/milestones/v1.2-two-sided-agent-mvp/phase-13-cross-pane-launch-polish/PILOT-RUNBOOK.md` (~400 lines, 7 sections + bonus) — Section 1: Pre-flight checklist (secrets / vars / KV / SW / /healthz / Infisical), Section 2: Feature flag KV setup (global defaults + per-employee overrides via DO column), Section 3: Smoke test sequence (push + crosspane + onboarding + ranking + seed-email + healthz, all PARROT_DEV_MODE-gated), Section 4: Day-1 rollout protocol (raj → ridhi → external, per-user onboarding sequence, telemetry watchpoints), Section 5: Rollback procedure (KV flag flip ≤60s preferred, Worker rollback last resort with DO storage forward-only caveats, emergency DNS bypass, per-user surgery limits), Section 6: On-call escalation (primary contact + service ownership table + severity ladder + comms protocol), Section 7: Out of scope for v1.2 pilot (Daily.co Phase 11 deferred, RFC 8291 encryption v1.3, Telnyx v1.3, admin console UI v1.3). Audit: TypeScript clean (`cd apps/parrot && npx tsc --noEmit` zero errors after each task). Zero new npm packages (no `@sentry/cloudflare`, no `@daily-co/*`, no `@cloudflare/voice`, no `@telnyx`, no `agents`, no `web-push`). LLM-URL audit `grep -rn "api\.cloudflare\.com.*ai/run\|workers-ai" apps/parrot/workers/` returns only the existing 3 lines in `workers/lib/ai.ts` + the new /healthz line (both use `gateway.ai.cloudflare.com` — the sanctioned pattern). Deviations: 1 auto-fixed — Rule-3 Blocking: `apps/parrot/app/lib/api.ts` updated even though omitted from frontmatter `files_modified`; the plan's Task 1 `<action>` body explicitly enumerated the api.ts additions (`getFeatureFlags` + `completeOnboarding` helpers + `MeResponse.onboarded_at` field) — frontmatter/body inconsistency, not scope creep. Wave 3 user-action gate: (a) `wrangler kv namespace create PARROT_FEATURE_FLAGS --name internjobs-parrot` + paste id into wrangler.jsonc → kv_namespaces[0].id (Worker degrades cleanly when binding unbound); (b) Sentry project + `wrangler secret put SENTRY_DSN` + Infisical persist (optional — falls back to console.error); (c) carryover VAPID provisioning from Wave 1 (still optional — wizard step 2 disables gracefully if PUSH_VAPID_PUBLIC_KEY is empty). Phase 13 is code-complete. v1.2 is code-complete — Phase 11 (Daily.co) deferred per user direction 2026-05-19, all other v1.2 phases shipped to production code. Remaining work is user-actions only (DNS / Clerk dashboard / CF Email Routing / KV / VAPID / Sentry / INTEG-01 smoke test in prod). First pilot rollout target per PILOT-RUNBOOK §4.1: raj (self) → ridhi (CEO) → external pilots. v1.3 follow-ups flagged: WorkspaceDO.countEmployees() RPC to flip /healthz mailbox_count from -1 to real count; RFC 8291 aes128gcm push body encryption; admin console UI for flag overrides + per-employee DO surgery; POST /api/admin/employees/:id/flags endpoint to write per-employee profile.feature_flags overrides.
# 2026-05-19: PHASE-13 WAVE 2 COMPLETE — 13-02 (Cross-pane Actions) shipped via /rrr:execute-plan in 4m 35s. 2 atomic commits on main (efb2d38 feat + ec61857 feat). Wave 2 deliverables: (1) Two new EmployeeMailboxDO methods — `emailToChat(emailId)`: SELECTs email row, slugifies subject (lowercase, non-alnum→'-', truncate 60, fallback `email-{id-prefix}`), resolves bot's first team via `GET /api/v4/teams`, POSTs `POST /api/v4/channels` (type='P' private), POSTs `POST /api/v4/posts` to seed channel with `**Email from {sender}**\n\n{body[:2000]}`. Wrapped in try/catch + early-returns on missing env so every failure becomes `{ok:false, error:string}` instead of a thrown exception. Returns `{ok:true, channel_id, channel_url}` shaped to `{MATTERMOST_URL}/{team_name}/channels/{channel_name}`. `chatToEmail(postId, postBody)`: deterministic — no LLM, no external HTTP. Subject = `'From chat: ' + body[:60].replace(/\n/g,' ') + (truncated?'…':'')`. Body = every line markdown-quoted (`> `) + blank line for reply. Returns `{ok:true, draft:{to:'', subject, body}}`. (2) Three live `/api/crosspane/*` Hono routes replace the 501 stubs — `email-to-chat` validates `email_id`, delegates to DO, maps DO ok:false to HTTP 502 with error string in `reason`; `chat-to-email` same shape with `post_body` required; `start-meeting` UI seam — writes notifications row (`event_type='urgent_todo'`, title='Meeting requested (Phase 11 pending)', body='Employee clicked Start Meeting — Daily.co integration deferred.', url='/meetings') so pilot demand is measurable. Returns `{ok:true, reason:'meetings_coming_soon', message}`. NO @daily-co/* dep. (3) `POST /api/dev/smoke/crosspane` (PARROT_DEV_MODE-gated) — exercises chatToEmail draft assembly (asserts non-empty subject + body) + emailToChat graceful-failure (passes nonexistent email_id, asserts ok:false with non-empty error string, asserts no thrown exception). Returns `{pass: bool, chat_to_email_draft_assembled, email_to_chat_graceful_failure}`. (4) Three crosspane components fully rewritten — `EmailToChat({emailId: string})` POSTs the endpoint, stashes channel URL in `sessionStorage['parrot_crosspane_channel_url']`, navigates to `/chat` on success; renders inline red-text error label when ok:false. `ChatToEmail({postId?, postBody?})` POSTs, opens a fixed-position compose modal (To/Subject/Body fields editable) pre-filled from server draft; "Open in Inbox" button stashes edited draft at `sessionStorage['parrot_compose_draft']` for v1.3 composer + navigates to `/inbox?compose=1`. `StartMeeting()` POSTs, shows fixed-bottom toast "Meetings coming soon — Daily.co integration is on the roadmap." for 3.5s. (5) `InboxPane.tsx` passes `selectedId ?? ""` to `<EmailToChat />` (was no-arg call before — TypeScript flagged it cleanly via `npx tsc --build`). (6) `app/lib/api.ts` updated: `crosspaneEmailToChat(emailId)`, `crosspaneChatToEmail(postId, postBody)`; `crosspaneStartMeeting` signature unchanged (UI seam). Audit: TypeScript clean (`cd apps/parrot && npx tsc --build` exits 0). `grep "not_implemented_wave_4" apps/parrot/workers/ apps/parrot/app/` → 0 hits (all stubs replaced). `grep "Wave 4" apps/parrot/app/components/crosspane/*.tsx` → 0 hits (no placeholder comments remain). `grep "@daily-co\|web-push" apps/parrot/app/components/crosspane/StartMeeting.tsx` → 1 hit which is the comment explicitly stating no such package is installed. `grep "daily-co\|@daily" apps/parrot/package.json` → 0 hits. LLM-URL audit clean (`grep -rn "api\.cloudflare\.com.*ai/run\|workers-ai" apps/parrot/workers/ | grep -v lib/ai.ts` → 0 hits). Skills-referenced header `cloudflare/skills: agents-sdk` lands in 5 places this wave (3 components + 2 worker route/DO-method blocks). Deviations: NONE — plan executed exactly as written; `files_modified` frontmatter matched git diff one-for-one. Wave 2 user-action gate: MATTERMOST_BOT_TOKEN + MATTERMOST_URL already provisioned in Phase 12; no new secrets required. Phase 13 readiness: Wave 3 (13-03 Onboarding Wizard) can now reference three real working crosspane components in its product tour — no more "Wave 4 coming soon" placeholder copy. Phase 11 (Daily.co) has a clean swap-in point: count `notifications WHERE event_type='urgent_todo' AND title LIKE 'Meeting requested%'` to size pilot demand before paying integration cost; when Phase 11 lands, the start-meeting handler gains a real `fetch(env.DAILY_API + '/rooms', ...)` call and the audit-write stays. v1.3 follow-ups: (a) full composer reads from `sessionStorage['parrot_compose_draft']`; (b) LLM subject summarization via `callAiGateway()` from Phase 12 replaces deterministic heuristic in `chatToEmail()`; (c) dedicated `start_meeting_requested` event_type added when Phase 11 expands the notifications CHECK constraint.
# 2026-05-19: PHASE-13 WAVE 1 COMPLETE — 13-01 (Cross-pane Notifications + Push) shipped via /rrr:execute-plan in 7m 31s. 2 atomic commits on main (41818a2 feat + e369ddd feat). Wave 1 deliverables: (1) Migration `4_notifications_push` on EmployeeMailboxDO adds two per-employee tables — `notifications` (id, employee_id, event_type∈{urgent_todo,starred_email,chat_mention}, title, body, url, read, created_at) with `idx_notifications_employee_read` on (employee_id, read, created_at DESC), and `push_subscriptions` (endpoint PK, employee_id, p256dh, auth, created_at) with `idx_push_subs_employee` on employee_id. (2) 7 new DO methods: `addPushSubscription/removePushSubscription/getPushSubscriptions` (push CRUD), `addNotification/getNotifications/markNotificationsRead` (notification CRUD), and `sendPushToSubscriptions(payload)` (fan-out — writes a notification row, then iterates subscriptions with `await Promise.all(...)` issuing VAPID-signed POSTs; 410/404 responses trigger `removePushSubscription` to prune dead endpoints; tries continue on individual failures). (3) Three push triggers wired inline: `insertTodos()` peeks the unique-index BEFORE inserting and queues a push only on a TRULY-new urgent (`urgency_score >= 70`) row — dedupes against `INSERT OR IGNORE` re-inserts so alarm re-polls don't re-push the same urgent todo (Rule 1 bug fix); `createEmail()` checks `folderId === Folders.INBOX && data.starred === true` and pushes `Starred email from {sender}` → /inbox; `pollMattermostNewPosts()` iterates each batch's posts for `@{profile.displayName}` token and pushes `Mention in Chat` → /chat. (4) NEW `apps/parrot/workers/lib/vapid.ts` — builds the RFC 8292 Authorization header `vapid t=<jwt>,k=<base64url-public-key>` via `crypto.subtle.importKey('pkcs8', …, ECDSA P-256)` + `crypto.subtle.sign(ECDSA SHA-256)`. NO `web-push` npm dep (forbidden by plan; Node-only anyway). Handles PEM with BEGIN/END framing or bare base64; throws a clear error if a raw 32-byte scalar is supplied (asks user to re-export as PKCS#8). (5) NEW `apps/parrot/public/sw.js` — VANILLA JavaScript service worker with ONLY `push` + `notificationclick` handlers (intentionally NO `fetch` listener so Vite/React Router asset caching is untouched). `push` parses `event.data.json()`, falls back to safe defaults on malformed, calls `self.registration.showNotification`. `notificationclick` focuses existing client and `client.navigate(url)` if matching origin, else `clients.openWindow(url)`. (6) Four Hono routes: POST/DELETE `/api/push/subscribe` (subscription CRUD), GET `/api/notifications?limit=N` (drawer query — returns `{notifications, unread}`), POST `/api/notifications/mark-read` (mark subset by id OR all unread if no body). All require `requireEmployeeMailbox`. (7) Dev-only `POST /api/dev/smoke/push` PARROT_DEV_MODE-gated — inserts sentinel push subscription, adds notification, asserts read-back, marks-all-read, asserts all rows read=1, cleans up. Returns `{notification_stored, mark_read_works, vapid_configured, pass}` — `pass: true` independent of VAPID key presence (store/read/mark-read is the only deterministic surface). (8) WorkspaceShell topbar gets a Bell button (lucide) with red-dot badge (no count digit — Slack/Linear style) when unread > 0, opens a slide-in NotificationDrawer aside that polls `/api/notifications` every 30s via React Query, shows event-typed icons (Bell rose for urgent_todo / Mail violet for starred_email / MessageSquare sky for chat_mention), unread bg+dot, relative-time labels (Xs/Xm/Xh/Xd ago — built-in arithmetic, no date-fns), row click marks single notification read + navigates + closes drawer; on drawer-open auto-marks-all-read after 600ms delay so user sees the unread highlight first. SW registration runs once in `useEffect` on mount — push opt-in itself lives in onboarding wizard (Plan 13-03). (9) `app/lib/api.ts` gains `NotificationItem` + `NotificationsResponse` types + `getNotifications/markNotificationsRead/subscribePush/unsubscribePush` helpers (subscribePush uses `subscription.toJSON()` to extract endpoint+keys). (10) Env types: `PUSH_VAPID_PRIVATE_KEY?: string` + `PUSH_VAPID_PUBLIC_KEY?: string` — BOTH OPTIONAL so Worker boots without them; sendPushToSubscriptions records the notification row + logs `console.warn('VAPID keys not configured — skipping push fan-out (notification row saved)')` and returns early. wrangler.jsonc gets `vars.PUSH_VAPID_PUBLIC_KEY = ""` placeholder + secret-contract comment block for `PUSH_VAPID_PRIVATE_KEY`. (11) Skills-referenced comment block landed in 2 places: `durableObject/index.ts` top of file (`cloudflare/skills: agents-sdk, durable-objects — per-employee DO, alarm-driven Mattermost poll, Web Push VAPID signing via crypto.subtle`) + `sw.js` header (`cloudflare/skills: cloudflare`). Audit: TypeScript clean (`cd apps/parrot && npx tsc --noEmit` zero errors after each task). Zero new npm packages (no `web-push`, no `@daily-co`, no `@cloudflare/voice`, no `agents`, no `@telnyx` — VAPID via Workers crypto built-in). `grep "addEventListener.*fetch" sw.js` → 0 (correct — no asset caching). `grep "api\.cloudflare\.com.*ai/run\|workers-ai" workers/` returns only the existing 3 lines in `workers/lib/ai.ts` (gateway.ai.cloudflare.com, NOT direct REST). Deviations: 3 auto-fixed — (a) Rule-3 Blocking: added `apps/parrot/workers/lib/vapid.ts` (not in plan's `files_modified`, extracted from inline DO method for clarity); (b) Rule-3 Blocking: added `apps/parrot/app/lib/api.ts` modifications (drawer needs typed helpers, not in plan's `files_modified`); (c) Rule-1 Bug: `insertTodos` dedupe via pre-insert SELECT 1 EXISTS check so `INSERT OR IGNORE` re-inserts don't re-push urgent todos on every alarm cycle. All three preserve plan intent, zero scope creep. Phase 13 user-action gate: VAPID keypair generation + `wrangler secret put PUSH_VAPID_PRIVATE_KEY` + `vars.PUSH_VAPID_PUBLIC_KEY` paste + Infisical `/internjobs-ai/PUSH_VAPID_PRIVATE_KEY` persist. Code degrades gracefully without these (drawer stays usable; only live Notification.showNotification fan-out is suppressed). v1.3 follow-up: RFC 8291 aes128gcm body encryption — current implementation sends raw JSON payloads (title/body/url have no PII-sensitivity in v1.2 scope). Plan 13-02 (Cross-pane Actions) can now record cross-pane confirmation notifications via `addNotification`. Plan 13-03 (Onboarding Wizard) push-opt-in toggle calls `navigator.serviceWorker.ready` → `pushManager.subscribe({applicationServerKey: PUSH_VAPID_PUBLIC_KEY})` → `api.subscribePush()`; wizard will also need a new `GET /api/push/vapid-public-key` endpoint to fetch the key client-side (not built here — wizard-coupled, flagged for 13-03).
# 2026-05-19: PHASE-12 COMPLETE — 12-03 (Dashboard Mothership Agent UI + Ranking, Wave 3) shipped via /rrr:execute-plan in 2m 32s. 2 atomic commits on main (8e71b18 feat + 5596b9b feat). Wave 3 deliverables: (1) NEW `apps/parrot/app/components/TodoCard.tsx` — exports `TodoCard` (default + named) + `TodoItem` interface mirror of EmployeeMailboxDO.getTodos() rows; anatomy = 36×36 colored source-icon badge (Mail violet / MessageSquare sky / MessageCircle slate / Phone slate / Video amber) + 2-line clamp title + 8px urgency dot (red ≥70 / amber ≥40 / slate else) + 1-line preview + age badge (`Xm/Xh/Xd ago`) + deadline chip (Intl.DateTimeFormat with overdue=red / <24h=amber / else=slate tone bands) + @mention chip; presentation-only with onSelect callback hoist so the parent owns routing. (2) Rebuilt `apps/parrot/app/routes/dashboard.tsx` — replaced Wave 1 placeholder card with four-state UI: LoadingSkeleton (3 pulsing bars) → ErrorCard ("agent may still be warming up") → EmptyState (view-aware copy: all/mentions/today/week each get their own first-line) → `<ul>` of TodoCard rows ordered by DO-side hybrid rank. useEffect fetch with `cancelled` flag cleanup (defends against React 18 strict-mode double-fire). View filter links carry `?view=mentions|today|week` query params; active highlight via useSearchParams. (3) handleSelect() routes email-source clicks to `/inbox?message=${encodeURIComponent(source_id)}` via useNavigate; chat-source clicks to `/chat` root (Mattermost iframe deep-link `?post=` is a v1.3 enhancement — inline comment documents the SSO-bridge follow-up). (4) NEW Hono route `POST /api/dev/smoke/ranking` on `apps/parrot/workers/index.ts` — PARROT_DEV_MODE-gated, inserts two todos via debugInsertTodo RPC with explicit urgency=80 (is_mention=false → rank=160) + urgency=20 (is_mention=true → rank=70), re-fetches via getTodos("all"), asserts hi.rank > lo.rank, returns `{ hi_todo, lo_todo, hi_inserted, lo_inserted, hi_ranks_first, pass, note }`. Test is 100% deterministic — runs without PARROT_AI_GATEWAY_ID / CLOUDFLARE_AI_API_TOKEN. Without PARROT_DEV_MODE returns 403. (5) Skills-referenced "Workers AI via AI Gateway" comment block landed in 3 places this wave: TodoCard.tsx top of file, dashboard.tsx top of file, ranking endpoint header in workers/index.ts. Audit: TypeScript clean (`npx tsc --noEmit` zero errors after each task commit). Zero new npm packages (no package.json edits). LLM-URL audit `grep -rn "gateway.ai.cloudflare.com\|/workers-ai/"` returns only workers/lib/ai.ts (3 lines) — no direct Workers AI REST anywhere. files_modified frontmatter matched git diff exactly: TodoCard.tsx + dashboard.tsx + workers/index.ts. Deviations: none. Wave 3 user-setup: none new; carryover production gates from Wave 1+2 (CF AI Gateway provisioning + 3 secrets + Mattermost bot token) unchanged. Phase 12 is complete — Phase 13 (Cross-pane Actions + Launch Polish) is unblocked. Decisions locked: (a) chat-source click-through goes to /chat root not deep-link (SSO bridge doesn't accept ?post= yet); (b) ranking regression uses debugInsertTodo NOT live LLM (so the regression gate runs without secrets, isolates the SQL formula as the only moving part under test); (c) TodoCard is `<button type="button">` not `<a>` (reusable from search/settings/digest without route binding); (d) date formatting via built-in Intl.DateTimeFormat (no date-fns/dayjs); (e) TodoItem interface lives in TodoCard.tsx as single source of truth; (f) empty-state copy is view-aware (mentions/today/week each get distinct first-line). Phase 13 follow-ups noted in SUMMARY: SSO-bridge `?post=` param for chat deep-link, audit_events migration, optional migration of dashboard fetch from useEffect to React Query when resolve/snooze actions need cross-pane invalidation.
# 2026-05-19: PHASE-12 WAVE 2 COMPLETE — 12-02 (Dashboard Mothership Agent ingest + extraction) shipped via /rrr:execute-plan. 2 atomic commits (611ba42 feat + b502609 feat). Wave 2 deliverables: (1) `apps/parrot/workers/lib/mattermost.ts` new file with `resolveMmUserId` / `getMmChannelsForUser` / `getMmPostsSince` + `MM_USER_ID_NONE` sentinel; `since=ms-5000` overlap window guards against mattermost#13846 race. (2) `EmployeeMailboxDO` gains 9 methods: `insertTodos` (INSERT OR IGNORE), `getTodos` (real hybrid-rank SQL — replaces Wave 1 stub), `debugInsertTodo` (`PARROT_DEV_MODE` gate for Wave 3 deterministic tests), `cleanupTodosForEmail`, `extractTodosFromEmail` (fire-and-forget caller, cacheTtl=3600), `initAlarm`, `alarm` (self-reschedule in `finally` block — at-least-once survives Mattermost 5xx/network blips), `pollMattermostNewPosts`, `extractTodosFromChat` (cacheTtl=1800). (3) `createEmail()` calls `void this.extractTodosFromEmail(email, profile.employeeId)` when `folderId === Folders.INBOX` — email storage NEVER blocked. (4) `upsertProfile()` calls `void this.initAlarm()` to register the 2-min polling cycle on first employee login. (5) `deleteEmail()` calls `cleanupTodosForEmail(id)` to mark todos resolved before source goes away. (6) 429 from AI Gateway logs `audit_events.event_type='ai_gateway_quota_exceeded'` (best-effort, table is future migration) — both extractor sites. (7) `POST /api/dev/smoke/seed-email` Hono endpoint — `PARROT_DEV_MODE`-gated, seeds urgent contract email, waits 500ms, asserts ≥1 email-sourced todo via `getTodos('all')`, returns `{ pass, todos_extracted, todos }`. (8) `types.ts` untouched — Wave 1 already declared `PARROT_AI_GATEWAY_ID: string` + `PARROT_DEV_MODE?: string` (task action explicitly said "do not duplicate"). Audit: TypeScript clean (`npx tsc --noEmit` zero errors). Zero new npm packages. Only LLM URL is `gateway.ai.cloudflare.com` (no direct Workers AI REST introduced). Deviations: none. SEC-ROTATE-CF-AI-PARROT-01 still pending (rotate `CLOUDFLARE_AI_API_TOKEN` after Wave 2 production deploy). Wave-2 user-action gate persists: user must create CF AI Gateway (`internjobs-parrot`, 200 req/day/employee) + `wrangler secret put` PARROT_AI_GATEWAY_ID/CLOUDFLARE_AI_API_TOKEN/CLOUDFLARE_ACCOUNT_ID on the Worker before production LLM calls work; for Mattermost ingest also need bot account + `MATTERMOST_BOT_TOKEN` secret. Wave-3 (12-03) can proceed immediately on top of these RPCs (`debugInsertTodo` is the deterministic test entrypoint).
# 2026-05-19: PHASE-12 WAVE 1 COMPLETE — 12-01 (Dashboard Mothership Agent foundation) shipped via /rrr:execute-plan. 3 atomic commits (5fe02a9 + 7ef33cc + e4e2ed4). Wave 1 deliverables: (1) Migration `3_todos_table` on EmployeeMailboxDO with all columns + indexes; (2) Drizzle `todos` table mirror in workers/db/schema.ts; (3) workers/lib/ai.ts rewritten from Phase-10 stub to real `callAiGateway()` + `extractTodosFromText()` + `TODO_EXTRACTION_SCHEMA` routing through Cloudflare AI Gateway (NOT direct Workers AI REST) with `cf-aig-metadata.user_id` for per-employee quota + `cf-aig-cache-ttl` for prompt cache; fail-soft `[]` on missing config / 429 / non-2xx; Skills referenced comment block names `cloudflare/skills: agents-sdk, durable-objects, cloudflare`; (4) GET /api/dashboard/todos Hono route behind requireEmployeeMailbox, accepts ?view=all|mentions|today|week, returns `{todos:[]}` via DO stub; (5) Phone (lucide Phone) + SMS (lucide MessageCircle) NAV entries in WorkspaceShell.tsx between Meetings and the operator-only admin section; (6) /phone + /sms placeholder routes with future @cloudflare/voice + withVoice(Agent) + @cloudflare/voice-twilio architecture inline comments (no telephony packages installed); (7) routes.ts registration for /phone + /sms; (8) Env extensions: CLOUDFLARE_AI_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, PARROT_AI_GATEWAY_ID, KIMI_MODEL, MATTERMOST_BOT_TOKEN?, PARROT_DEV_MODE? (+ matching CfEnvBase Omit entries); (9) wrangler.jsonc KIMI_MODEL=@cf/moonshotai/kimi-k2.6 var + secret declarations as inline comments. TypeScript compiles clean. Zero new npm packages. Deviations: 2 Rule-3 path corrections (routes config is `apps/parrot/app/routes.ts` NOT `apps/parrot/app/routes/routes.ts`; getTodos stub on EmployeeMailboxDO not declared in plan frontmatter but explicitly requested in Task 2 body) — both anticipated by plan task text, no scope creep. Pre-existing `npm run build` / `wrangler deploy --dry-run` failures verified pre-existing (vite missing from workspace root, virtual:react-router/server-build resolution issue) — NOT a regression from this plan. Wave-2 user-action gate: user must create CF AI Gateway (`internjobs-parrot` slug, 200 req/day/employee) + run `wrangler secret put PARROT_AI_GATEWAY_ID` + `CLOUDFLARE_AI_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` (0fffd3dc637bdb26d4963df445a69fd3) on the internjobs-parrot Worker before Wave 2 production deploy can make live LLM calls. Wave-2 dev work can proceed with envs unset (callAiGateway fails soft to `[]`).
# 2026-05-19: PHASE-12 planning kickoff — /rrr:plan-phase 12 invoked. Pre-flight cleanup: phase-10 branch (19 commits, Parrot Worker + UI + Mattermost) merged into main (merge commit d5fecf9 on 2026-05-19); ROADMAP.md now lists Phases 07/07b/08/09/10 shipped + 11/12/13 pending. Uncommitted Vanta clouds + embedded Clerk waitlist UI committed first (commit 0d27b78). Phase 12 scope confirmed: per-employee LLM agent monitoring Email/Chat/Meetings/Phone+SMS channels, ranking cross-channel todos onto Parrot's Dashboard pane. Phone + SMS placeholder nav icons land inside this phase (Telnyx integration deferred to v1.3 per feedback-seam-not-integration pattern). Research mandatory (risky domain: LLM + auth + cross-channel ingest).
# 2026-05-19: PHASE-10 Wave 2b CLOSE — Parrot workspace usable end-to-end. Architecture finalized after detours: TWO separate production Clerk apps (student at clerk.app.internjobs.ai, LinkedIn-only; employee at clerk.workspace.internjobs.ai, phone-OTP only). Student app domain RENAMED from clerk.internjobs.ai → clerk.app.internjobs.ai to free the apex for the new employee app. Organizations were tried + rolled back (would have let students bypass LinkedIn). Operator gate: publicMetadata.role ∈ {operator, admin, ceo} OR PARROT_OPERATOR_EMAILS allowlist. Workspace UX: Slack-style dual-rail shell (icon rail + per-pane secondary nav), Vanta Birds bg on /sign-in, embedded Clerk SignIn (phone-OTP), UserMenu dropdown (Profile / Settings / Sign out), lucide-react icons. Student waitlist redesigned: Standout-style 3-tier card with InternJobs ∞ brand, LinkedIn-blue "Continue with LinkedIn" CTA, Vanta Clouds bg, "Made with ♥ from Texas 🦄" footer. CF Email Routing rule for ridhi@internjobs.ai → rentalaraj@gmail.com (Ridhi = CEO, user_3DvOELvczcR9tk0rAC5b4FjgUbQ). LinkedIn OAuth callback updated to clerk.app.internjobs.ai/v1/oauth_callback. CF rate limit on workspace.internjobs.ai/sign-in* at 50 req/10s/IP. Wave 2b commits on phase-10 branch: b10897b (Clerk migration), b16579c (embedded SignIn), 4d3aef4 (accept claims-without-identifier — fixed the redirect loop that came from Clerk's default JWT having only `sub`). Memory: project-auth-architecture.md + feedback-clerk-jwt-claims.md + user-interview-prep.md. PLAN.md updated with the Wave 2b auth-pivot retrospective.
# 2026-05-19: PHASE-10 SCOPE-SPLIT — original Wave 3 (Daily.co), Wave 4 (Dashboard mothership agent + cross-pane actions), Wave 5 (polish + launch) are SPLIT OUT into separate phases for cleaner RRR tracking. New phases: Phase 11 = Daily.co integration, Phase 12 = Dashboard mothership agent (cross-channel todo surfacing), Phase 13 = Cross-pane actions + launch polish. See ROADMAP.md updates this commit.
# 2026-05-18: PHASE-09 ship — LinkedIn enrichment + QR onboarding (Phase 09) completed and live. Standout-style flow: LinkedIn OAuth → Proxycurl/Cognee profile enrichment → QR-code phone pairing into iMessage. Commits a00deb9 + fd004a7. Maya voice tightened post-ship in commit f5279a6 (warm openers, depth-matching, long-form exemplar).
# 2026-05-18: PHASE-07b ship — BlueBubbles + Private API helper on HostMyApple Mac mini (SIP-disabled). Native iMessage UX live: tapback ack + read receipt + held typing bubble across LLM round-trip. Full round-trip 4.1s. Commits captured under phase-07b branch.
# 2026-05-17: PHASE-08 ship — Cloudflare agentic-inbox Worker (`internjobs-agentic-inbox`) live at agent.internjobs.ai. Mailbox DO + Email MCP + send_email + R2 + AI bindings. CF Email Routing rule for maya@agent.internjobs.ai → Worker live (zone-scoped CF token added).
# 2026-05-17: SCOPE-EXPAND — added Phase 07 (self-hosted iMessage bridge, code-complete) and Phase 08 (Cloudflare agentic-inbox + MCP, plan written, awaiting CF Access user action). Plan docs at .planning/milestones/v1.2-two-sided-agent-mvp/phase-07-self-hosted-imessage-bridge/PLAN.md + phase-08-agentic-inbox-mcp/PLAN.md. Phase 07 commits: 61e9707 + d89fe4a + d5f3eb1. Phase 08 sunsets the home-rolled /webhooks/agent-mail + agent_emails table (0006 migration) that shipped earlier this turn (c715aa9) before the agentic-inbox direction was set. SCOPE drift acknowledged: today's work was executed ad-hoc before plans were captured; this STATE.md commit closes that gap retroactively for Phase 07 and prospectively for Phase 08. SEC-ROTATE backlog grows: HOSTMYAPPLE_ROOT_PASSWORD (pasted in chat), BRIDGE_HMAC_SECRET (deployed to Fly only; not yet in Infisical because the active Infisical session is scoped to a non-InternJobs workspace).
# 2026-05-17: PHASE-08 progress — Cloudflare agentic-inbox Worker (`internjobs-agentic-inbox`) deployed with all bindings (MailboxDO + EmailAgent + EmailMCP + send_email + R2 `internjobs-agentic-inbox` + AI). EMAIL_ADDRESSES=["maya@agent.internjobs.ai"]. CF Access App created via API (id=ceda0ea4-a7bb-455a-96db-0bd2253a789d, aud=37f2e5a4...) with email-allow policy on rentalaraj@gmail.com. POLICY_AUD + TEAM_DOMAIN=gpprotect.cloudflareaccess.com set as Worker secrets. Verified: 302 to Access SSO instead of 500. Email Routing rule for maya@agent.internjobs.ai → Worker pending (current CF API token needs Zone:Email Routing:Edit scope). SEC-ROTATE adds: HOSTMYAPPLE_ROOT_PASSWORD + BRIDGE_HMAC_SECRET + CF_ACCESS_TOKEN cfut_pp... + Access App AUD — all currently in /tmp/*.txt chmod 600, NOT in Infisical because the active CLI session is scoped to Projecta Labs org while the InternJobs project lives in the older Projecta org.# 2026-05-17: MEMORY-01 (Phase B) — self-hosted graph memory shipped. New Fly app `internjobs-graph` in `internjobs-sios-org` (FalkorDB Redis-protocol graph DB + Cypher, falkordb/falkordb:latest base + custom Dockerfile/entrypoint.sh wrapper, 10GB Fly volume `falkor_data` at /data with AOF persistence, region ord, shared-cpu-1x 1GB RAM, single machine, no public IP). Internal-only at `internjobs-graph.internal:6379` on the org's private network. Password auth via Fly secret `REDIS_PASSWORD` (openssl rand -hex 32, saved to Infisical /internjobs-ai as `FALKORDB_PASSWORD` + `FALKORDB_URL=redis://default:<pw>@internjobs-graph.internal:6379`; latter staged on the student app via `flyctl secrets import --stage`). The custom Dockerfile + entrypoint.sh wrap the upstream image because the upstream `run.sh`'s `${REDIS_ARGS}` expansion path doesn't survive Fly's secret-injection prefix layer (two failure modes documented in the Dockerfile comments — both observed before the fix). Native Node implementation of Graphiti temporal-fact pattern at `apps/app/src/memory/graph.mjs`: every fact is a `:Fact` node attached to `:Student | :Role | :Startup` via `-[:HAS_FACT]->`, with predicate/object_value/confidence/source_message_id/valid_from/valid_to. Single-valued predicates (STUDIES_AT, STATUS, PREFERS) close conflicting active facts atomically (valid_to = now()); multi-valued (INTERESTED_IN, SKILLS_IN, MENTIONED, OTHER) accumulate. Deterministic fact id (sha256 prefix) makes recordFact idempotent. NO Python sidecar. New npm dep `falkordb@6.6.2` (official Node client, Redis wire protocol + Cypher). Workflow integration in `apps/app/src/workflows/student-inbound.mjs`: pre-LLM `getStudentSummary(studentId)` injection as the FIRST block of the per-turn user prompt (cross-day recall — "hey raj. been a minute - how'd the valon thing land?"); post-reply fire-and-forget `recordTurnFacts` extraction via Workers AI 70B (temperature 0.1, JSON-only, up to 5 facts/turn). Fail-soft end-to-end: app boots without FALKORDB_URL, workflow completes turn without graph. `apps/app/src/server.mjs` runs `ensureGraphSchema()` on startup and adds `graphReady` to /healthz (cached 30s — pingGraph round-trips a `RETURN 1`). New script `npm run smoke:graph --workspace=apps/app` validates 6 invariants (ping, schema, 5 seed facts, conflict close-out (Graphiti invariant), summary string non-empty, recallHistory keyword scoring); skipped (exit 0) when FALKORDB_URL is unset. Sample summary from smoke run: `studies at: Columbia. interested in: typescript, backend python. prefers: small startup, remote or NYC. mentioned: Acme Backend Python Intern role - intro pending operator. last active: 2026-05-17 16:01.` build:app + smoke:ops + smoke:graph all green. Deployed live to `internjobs-ai-student-app`; production /healthz returns `graphReady: true`. Commits: `629bcc1` (FALKORDB-DEPLOY fly.toml) + `41584c4` (fix custom Dockerfile/entrypoint) + `cd4af93` (MEMORY-01 graph.mjs + workflow hooks + server bootstrap + smoke-graph).
# 2026-05-17: AGENT-VOICE — chat model upgraded `@cf/meta/llama-3.1-8b-instruct` → `@cf/meta/llama-3.3-70b-instruct-fp8-fast` for conversational quality. System prompt rewritten with explicit voice rules (lowercase except proper nouns, hyphen-break " - " clauses, no emojis, first-person, push back when candidate is wrong, name actual roles/companies/schools) + 5 few-shot exemplars from a competitor's recruiting-agent SMS thread. New exports in `apps/app/src/workflows/student-inbound.mjs`: `AGENT_VOICE`, `AGENT_VOICE_EXEMPLARS`, `AGENT_SYSTEM_PROMPT` (composed: persona + voice + exemplars + safety). `loadStudentProfile` now also pulls `students.name` + latest `profile_snapshots.display_name` and derives a `firstName` for in-prompt addressing. `composeProfileBlob` emits `first_name:` + `full_name:` keys. `composePrompt` slimmed — persona/voice/safety moved to the system message; per-turn dynamic context stays in the user message. max_tokens 512 → 800, temperature 0.7 → 0.5. Stub-mode fallback preserved. Embedding model UNTOUCHED (`@cf/baai/bge-base-en-v1.5`, 768-dim, pgvector tables locked). No new npm deps, no new env vars. Live-probed reachable on account `0fffd3dc637bdb26d4963df445a69fd3`. fp8-fast preferred; documented fallback to `@cf/meta/llama-3.1-70b-instruct` if catalog drops (non-fast `llama-3.3-70b-instruct` returned "No route" — NOT a fallback). build:app + smoke:ops green. Deployed live to `internjobs-ai-student-app`; /healthz green-check.
# 2026-05-17: AUTONOMY PIVOT (AGENT-AUTO + OPS-AUDIT) — removed the v1.2 operator approval gate. Mastra workflow now drafts AND autonomously sends agent responses on student SMS. /ops/drafts becomes a read-only audit log; /ops/feedback lists flagged messages (was: rejected/edited). Drafts go directly to status='sent' (or 'failed') after the autonomous send, with a transient 'sending' state for in-flight rows. Workflow wraps send in try/catch; failures land in audit_events event_type='auto_send_failed' and never crash the workflow. Safety posture: system-prompt-level guardrails (AGENT_SAFETY_GUARDRAILS in apps/app/src/workflows/student-inbound.mjs) — no legal/financial promises, no PII about other parties, polite refusal of illegal asks. Lakera Guard pre-LLM screening planned for v1.3 (SAFETY-01). Deprecated POST /ops/drafts/:id/{approve,edit,reject} routes return 410 Gone with reason='approval_gate_removed_2026_05_17'. Rationale: turn-by-turn approval latency made conversational UX impossibly slow. Risk acknowledged (agent can say bad things) and mitigated by (1) prompt guardrails, (2) operator flag-for-review post-hoc via POST /ops/drafts/:id/flag, (3) v1.3 Lakera Guard. Commits: d8a0bb3 (AGENT-AUTO: workflow auto-send + system-prompt safety guardrails + outbound.mjs header rewrite + server.mjs workflow-call passes smsProvider+config) + 57e3320 (OPS-AUDIT: server.mjs route changes + views.mjs renderMessageLog/renderDraftDetail rewrite + store.mjs listAllDrafts + smoke-ops.mjs 30+ new assertions). build:app + smoke:ops + test:auth all green. PROJECT.md + REQUIREMENTS.md + ROADMAP.md + USER-ACTIONS.md (Section E) all updated.
# 2026-05-16: AUTH-PROD — switched apps/app/src/auth.mjs::getAuth() to @clerk/backend@3.4.9 authenticateRequest(). Home-rolled JWKS-only verifier couldn't handle Clerk's `__clerk_handshake` cross-subdomain URL param flow (clerk.internjobs.ai + accounts.internjobs.ai → app.internjobs.ai on first hop after LinkedIn sign-in). Symptom in prod was an auth loop: /auth/callback → 302 to sign-in indefinitely. New code returns a handshake sentinel when SDK status==='handshake'; applyHandshakeOrContinue(res, auth) helper forwards Clerk's Set-Cookie + Location headers verbatim with 307. All 5 getAuth() callsites + the requireStartupAuth + requireOperatorAuth middlewares updated. Dev paths (signed cookie + x-clerk-user-id header) preserved exactly. Dead code removed: verifyJwtWithJwks + bearerToken. Deployed live to internjobs-ai-student-app; /healthz still all-green (mastraReady + pgvectorReady + workersAiReady + cloudflareEmailReady). test:auth (5 tests) + build:app smoke pass.
# 2026-05-16: EMAIL-03 subdomain isolation — agent aliases moved from apex `internjobs.ai` to dedicated `agent.internjobs.ai` subdomain. Worker bound to *@agent.internjobs.ai only; apex stays for human email (raj@, support@) forwarded to rentalaraj@gmail.com. Hard cut-over (no apex fallback in Worker matcher). OPERATOR_FALLBACK changed from ops@internjobs.ai → rentalaraj@gmail.com. New behavior: non-conv subdomain emails forward to operator fallback + audit-only POST to Fly. Worker + Fly both redeployed; build:app + smoke:ops + reply-to unit tests all green.
# 2026-05-16: Workers AI direct tear-out — apps/ai-worker proxy Worker removed; Fly Node app calls Cloudflare Workers AI REST directly with CLOUDFLARE_AI_API_TOKEN. /healthz: aiProxyReady → workersAiReady.
# 2026-05-16: Workers AI swap (OpenAI → Cloudflare Workers AI via internjobs-ai-proxy). Embedding dim 1536 → 768. Deployed live + smoke-tested in prod.
resume_file: ""  # Phase 13 (last phase of v1.2) is code-complete 2026-05-19 across all three waves. v1.2 milestone is code-complete pending user-actions (KV namespace create, Sentry DSN, VAPID provisioning, Phase 11 Daily.co DEFERRED, INTEG-01 prod smoke). Next planner work picks up at v1.3 — start there with a fresh /rrr:plan-milestone.
blockers:
  - "All 6 v1.2 phases code-complete + post-launch 2026-05-16 Workers AI tear-out deployed live to prod. The OPENAI_API_KEY user-action is DROPPED. The proxy Worker (`apps/ai-worker/`) is also DROPPED — Fly Node app now calls Cloudflare Workers AI REST API directly with `CLOUDFLARE_AI_API_TOKEN` (Workers-AI-scoped). Remaining work is user-only: DNS proxy fix, Clerk key rotation, Clerk strategy enablement, operator publicMetadata, CF Email Routing setup, Cloudflare Email Service onboarding (Account ID + Email-Sending-scoped API token), R2 bucket creation (`internjobs-agent-store`) + R2 API token + four R2_* envs in Infisical, INTEG-01 11-step smoke test in prod. See .planning/milestones/v1.2-two-sided-agent-mvp/USER-ACTIONS.md for the ordered checklist (Section B2 covers R2 setup; Section A5 — OPENAI_API_KEY — and the proxy Worker user-action are now dropped)."
  - "SEC-ROTATE-CF-EMAIL-01: rotate the Cloudflare Email Service API token pasted in chat 2026-05-16. Same posture as SEC-ROTATE-01 (Clerk). Update Infisical `prod`/`/internjobs-ai` → `CLOUDFLARE_EMAIL_API_TOKEN` and re-run flyctl secrets import. Do AFTER Section E smoke-test passes."
  - "SEC-ROTATE-CF-AI-01 (NEW 2026-05-16 tear-out): rotate the Cloudflare Workers AI API token pasted in chat 2026-05-16. Same posture as SEC-ROTATE-CF-EMAIL-01. Update Infisical `prod`/`/internjobs-ai` → `CLOUDFLARE_AI_API_TOKEN` and re-run flyctl secrets import. Do AFTER the next post-launch verification pass."
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-15)
See: .planning/REQUIREMENTS.md (defined 2026-05-16)
See: .planning/ROADMAP.md (created 2026-05-16)
See: .planning/milestones/v1.2-two-sided-agent-mvp/USER-ACTIONS.md (created 2026-05-16)

**Core value:** InternJobs.ai helps students and startups meet through natural messages, not resume piles or application black holes.
**Current focus:** v1.2 — all phase code shipped (plus 2026-05-16 Resend → Cloudflare Email Service swap); user-action checklist (DNS / secrets / Clerk dashboard / CF Email Routing / CF Email Sending onboard / deploy / smoke test) is the remaining gate.

## Current Position

Milestone: v1.2 — Two-Sided Agent MVP — CODE-COMPLETE 2026-05-19. All three Phase 13 waves shipped. Phase 11 (Daily.co) DEFERRED per user direction (Wave 2 start-meeting handler is the UI seam that records pilot demand). Phases 07/07b/08/09/10/12/13 shipped to production code. Phases 01–06 base v1.2 code still gated on USER-ACTIONS Sections A–D before INTEG-01 can run.
Phase: 13 of 13 (Cross-pane Actions + Launch Polish) — COMPLETE.
Plan: 3 of 3 (Wave 3: First-Login Wizard + Pilot Readiness) — shipped 2026-05-19 in 7m 1s.
Status: Phase 13 Wave 3 shipped + v1.2 milestone code-complete. Wave 3 delivers: (a) Migration 5_onboarding_flags adds onboarded_at + feature_flags TEXT columns to profile (NULL-safe ALTERs); (b) setOnboardedAt / getFeatureFlags / isFeatureEnabled DO methods (default-all-on flag posture, KV-unbound graceful fallback); (c) OnboardingWizard 3-step modal (display name / push opt-in / Mattermost ping) rendered from root.tsx AppShell when me.onboarded_at IS NULL; (d) GET /healthz public probe ({mattermost_reachable, ai_gateway_reachable, mailbox_count:-1}); (e) inline reportToSentry helper (no @sentry/cloudflare dep) wired into app.onError; (f) PARROT_FEATURE_FLAGS KV binding declared in wrangler.jsonc (empty id until user runs wrangler kv namespace create); (g) POST /api/onboarding/complete + GET /api/feature-flags + PARROT_DEV_MODE-gated POST /api/dev/smoke/onboarding; (h) PILOT-RUNBOOK.md (~400 lines, pre-flight + KV setup + smoke + rollout + rollback + on-call + out-of-scope). TypeScript clean. Zero new npm packages. No forbidden imports. Deviations: 1 Rule-3 Blocking (api.ts updated despite frontmatter omission — body explicitly enumerated the additions; doc drift, not scope creep). v1.2 remaining work: user-actions only (KV namespace, VAPID secrets, Sentry DSN, CF AI Gateway, MM bot token, INTEG-01 prod smoke).
Last activity: 2026-05-19 — PHASE-13 Wave 3 shipped (13-03-PLAN.md executed via /rrr:execute-plan, 7m 1s, commits e97bffe + e0b3e81). Earlier same day — PHASE-13 Wave 2 shipped (13-02-PLAN.md, 4m 35s, commits efb2d38 + ec61857). Earlier same day — PHASE-13 Wave 1 shipped (13-01-PLAN.md, 7m 31s, commits 41818a2 + e369ddd). Earlier same day — PHASE-12 Wave 3 shipped (12-03-PLAN.md, 2m 32s). Earlier same day — PHASE-12 Wave 2 shipped (12-02-PLAN.md, 3m 5s). Earlier same day — PHASE-12 Wave 1 shipped (12-01-PLAN.md, 6m 23s). Earlier 2026-05-17 — MEMORY-01 (Phase B): self-hosted FalkorDB on Fly + Graphiti-style temporal fact model in Node shipped. Earlier 2026-05-17 — AGENT-VOICE: agent chat model upgraded `llama-3.1-8b-instruct` → `llama-3.3-70b-instruct-fp8-fast` for conversational quality. Earlier 2026-05-17 — autonomy pivot: removed operator approval gate; agent sends autonomously on student SMS; /ops/drafts is read-only audit log + flag-for-review. Commits `d8a0bb3` + `57e3320`. Earlier 2026-05-16 — Phases 01–06 executed end-to-end via parallel planning + sequential execution.

Progress: ██████████ 100% (v1.2 code-complete across all 13 phases; Phase 11 Daily.co DEFERRED — UI seam captures pilot demand; remaining work is user-actions only: DNS / Clerk / CF Email Routing / KV / VAPID / Sentry / INTEG-01 prod smoke test)

## Performance Metrics

**Velocity (cumulative):**

- Total plans completed: 26 (v1.0: 15, v1.1: 1, v1.2: 10 — adds 13-03)
- Total phases completed: 7 + 7 code-complete = 14 (Phase 13 closes v1.2; last 6 awaiting user verification)
- Milestones shipped: 2 (v1.0, v1.1); v1.2 CODE-COMPLETE across all 13 phases, awaiting prod activation

**v1.2 progress:**

| Phase | Plans | Status | Commits |
|-------|-------|--------|---------|
| 01. Pre-flight + SMS Abstraction | 1 | Code-complete (USER ACTION blocks: DNS, Clerk rotate, fly deploy) | f8f01bb, 52cf272 |
| 02. Startup Identity, Consent & Roles | 1 | Code-complete (USER ACTION blocks: Clerk strategy enable, migrate, deploy) | b8aa57b, 90a36c4, 7918567, 182b4f0 |
| 03. Startup Email Channel | 1 | Code-complete (USER ACTION blocks: CF Email Routing, CF Email Sending onboard, secrets, wrangler deploy, fly deploy). 2026-05-16: outbound provider swapped from Resend to Cloudflare Email Service. | 75f809a, e9478b4 |
| 04. Mastra Agent Core | 1 | Code-complete (USER ACTION blocks: OPENAI_API_KEY, migrate, fly deploy, optional load test) | 4b9706b, b793fd3, 0422272 |
| 05. Operator Audit Log (was: Approval Gate) | 1 | Code-complete (USER ACTION blocks: set publicMetadata.userType='operator', fly deploy). 2026-05-17 autonomy pivot rewired this phase from approval-gate to read-only audit log + flag-for-review (commits d8a0bb3 + 57e3320). | e27cc19, 8e19fa9, d8a0bb3, 57e3320 |
| 06. Two-Sided Integration Smoke Test | 1 | Code artifacts shipped (runbook + admin endpoint); the actual 11-step prod smoke test is the user's hands | 9f84368, e1c21e9, 0013630 |
| Scope-add (STORAGE-01) — R2 storage scaffold | — | Code-complete (USER ACTION blocks: B2 bucket + token + envs). Smoke tests pass (16 unit + 4 integration). | 830aa0f |
| Scope-add (EMAIL-03) — per-conversation Reply-To aliases | — | Code-complete. Smoke tests pass (10 unit + 5 integration). Subdomain isolation (2026-05-16 update): aliases moved from apex to `agent.internjobs.ai` subdomain — Worker + Fly both redeployed live; USER-ACTIONS Section B revised to add subdomain Email Routing setup (B4 + B5 + new B5a verified-destination step). | f3b5a8f + EMAIL-03 subdomain commit |
| 13. Cross-pane Actions + Launch Polish | 3 (all shipped) | COMPLETE 2026-05-19. Wave 1 (Notifications + Push): per-employee browser push wired into EmployeeMailboxDO + bell/drawer in WorkspaceShell + VAPID via crypto.subtle. Wave 2 (Cross-pane Actions): three working crosspane components — EmailToChat (Mattermost channel + seed message), ChatToEmail (compose modal with sessionStorage handoff), StartMeeting (Daily.co UI seam + notifications row). Wave 3 (Onboarding Wizard + Pilot Readiness): migration 5_onboarding_flags + setOnboardedAt/getFeatureFlags/isFeatureEnabled DO methods + OnboardingWizard 3-step modal (display name / push opt-in / MM ping) + GET /healthz + inline reportToSentry (no @sentry/cloudflare dep) + PARROT_FEATURE_FLAGS KV binding (degrades graceful when unbound) + POST /api/onboarding/complete + GET /api/feature-flags + POST /api/dev/smoke/onboarding + PILOT-RUNBOOK.md. USER ACTION blocks: VAPID keypair gen + Sentry DSN + `wrangler kv namespace create PARROT_FEATURE_FLAGS` + id paste + Infisical persist. Code degrades gracefully without each. | 41818a2, e369ddd, efb2d38, ec61857, e97bffe, e0b3e81 |

## Accumulated Context

### Decisions logged this session

- 2026-05-19: **PHASE-13 Wave 3 (13-03) — First-Login Wizard + Pilot Readiness shipped.** Two atomic commits on main (e97bffe + e0b3e81) in 7m 1s. Wave 3 closes Phase 13 and code-completes v1.2 (Phase 11 Daily.co DEFERRED per user direction). Key locked decisions:
  - **Wizard re-shows per visit until server flag flips.** The × button dismisses the modal for the CURRENT PAGE-LOAD ONLY. Next page load (or React Query re-fetch of `/api/me`) re-renders the wizard until `profile.onboarded_at` is non-null. Forces real engagement (can't permanently hide it by clicking ×) while staying non-blocking. Server-side `setOnboardedAt()` is the single canonical gate.
  - **Default-all-on feature flags — missing key means enabled, only explicit false disables.** `isFeatureEnabled('foo')` returns `true` UNLESS the flag is `false`. New features ship enabled by default; flags are a surgical KILL SWITCH, not an enablement gate. Friction-free rollout. Greppable: `return flags[flag] !== false` (NOT `=== true`).
  - **`getFeatureFlags()` returns defaults on ANY error.** KV throw, malformed JSON in override column, missing binding — all fall back to `{cross_pane:true, push:true, onboarding_wizard:true}`. Over-grant > lock-out in incident response. Wrapped in outer try/catch so even unexpected throws can't break a pilot user's workspace.
  - **`/healthz` is public — NO Clerk auth.** Pre-flight curls and uptime monitors don't have Clerk session cookies. Endpoint only reveals booleans (no PII, no internal state). Returns `{mattermost_reachable, ai_gateway_reachable, mailbox_count}`. The canonical liveness probe per PILOT-RUNBOOK §1.5.
  - **`mailbox_count: -1` placeholder in `/healthz`.** Counting DO instances across a namespace isn't a Workers primitive. The `WorkspaceDO.countEmployees()` RPC that would make this real is v1.3 work. Surface `-1` so monitors can render "n/a" and the response shape stays stable when the count goes live.
  - **Inline Sentry envelope POST — NOT `@sentry/cloudflare`.** The Sentry Store API accepts a JSON envelope at `/api/{projectId}/store/` with an `X-Sentry-Auth` header — that's all unhandled-error capture needs. Same posture as the inline VAPID signer in Wave 1: when one short fetch can do the job, don't pull in a transitive-dep-heavy SDK. Bundle stays small. `reportToSentry(env, err, ctx)` is fire-and-forget — NEVER throws (a malformed DSN or network blip cannot turn into a secondary 500 inside the global error handler).
  - **VAPID public key threaded via root loader, NOT a dedicated endpoint.** The publishable Clerk key is already passed via `useLoaderData` — adding `vapidPublicKey` alongside is one less round-trip than a `GET /api/push/vapid-public-key` would cost. Both keys are safe-to-ship (Clerk pk_… is designed for client embedding; VAPID public key is literally the public half of a keypair). Anti-pattern avoided: a "fetch the public key on wizard mount" request.
  - **`AppShell` lives INSIDE `QueryClientProvider`.** The wizard's gate uses `useCurrentEmployee()` from `~/lib/auth`, which uses React Query. So the wizard host MUST be inside the provider tree. We added a small inner component rather than restructuring the entire shell — minimum-blast-radius pattern.
  - **`onboarded_at` is set-only via `setOnboardedAt()`.** `upsertProfile()` was deliberately NOT extended to take `onboardedAt` — the only path to setting it is `POST /api/onboarding/complete`, which calls `setOnboardedAt()`. Prevents accidental resets: if a future endpoint upserts the profile with stale data, it CAN'T wipe the onboarding flag. Pattern lock: when a column has lifecycle semantics, give it a dedicated setter, don't fold it into upsert.
  - **Empty KV namespace id committed to wrangler.jsonc.** User runs `wrangler kv namespace create PARROT_FEATURE_FLAGS --name internjobs-parrot` and pastes the id into the JSON. Until then, the binding is "declared but unbound" — `getFeatureFlags()` handles this gracefully. Decoupling means the merge can land BEFORE the user-action completes. KV namespace IDs are NOT secrets (safe to commit).
  - **`profile.feature_flags` is the per-employee override store, NOT KV.** Plan considered keying per-employee KV entries (`employee:{clerk_user_id}:cross_pane_enabled`) but landed on TEXT-as-JSON column on the per-employee DO instead. Reasons: (a) KV writes are eventually consistent across the edge — a flag flip might not stick on the user's next request; (b) the DO is already the per-employee source of truth; (c) cheaper for the common-case read path (no extra round-trip). KV holds global defaults only. Trade-off accepted: per-employee surgery requires either direct DO SQL or a future admin endpoint, not a wrangler kv command.
  - **NULL-safe migration 5 ALTERs — no DEFAULT, no UPDATE pass.** Both new columns (`onboarded_at`, `feature_flags`) are bare `TEXT` with no default. Existing profile rows interpret as "onboarded_at IS NULL" → wizard shows on next login. This is exactly the behavior we want; no separate migration step needed to seed values for the (small set of) already-provisioned employees.
  - **PILOT-RUNBOOK §4.1 sequence is RAJ → RIDHI → external.** Onboard self first (catch the bugs the test suite missed). 24h soak. Then ridhi@internjobs.ai (second human, first real concurrency test of OIDC bridge for Mattermost team-membership). 24h soak. Only after both succeed without operator intervention → external pilots. This is operator discipline, not technical constraint.
  - **Phase 11 (Daily.co) deferral encoded in start-meeting handler — already shipped Wave 2 — measured via `notifications WHERE event_type='urgent_todo' AND title LIKE 'Meeting requested%'`.** Wave 3 documents this in PILOT-RUNBOOK §7 ("Out of scope for v1.2 pilot"). When Phase 11 lands, the handler swaps in a real `/rooms` POST and the audit write stays as-is — backward-compatible Telemetry trail.
  - **Skills referenced comment block** landed in 2 new places this wave: `getFeatureFlags()` JSDoc (`cloudflare/skills: durable-objects, cloudflare — KV read with safe defaults; per-employee override merge`) + `reportToSentry()` JSDoc (`cloudflare/skills: cloudflare — fire-and-forget telemetry post`) + OnboardingWizard header (`cloudflare/skills: durable-objects, cloudflare — Web Push VAPID + per-employee onboarded_at on EmployeeMailboxDO`).

- 2026-05-19: **PHASE-13 Wave 1 (13-01) — Cross-pane Notifications + Push shipped.** Two atomic commits on main (41818a2 + e369ddd) in 7m 31s. Wave 1 ships per-employee browser push notifications wired into the existing Phase 12 ingest pipeline + Phase 10 email pipeline, plus an in-app notification drawer that works regardless of VAPID provisioning. Key locked decisions:
  - **VAPID signing via Workers `crypto.subtle`, NOT npm `web-push`.** Plan explicitly forbids `web-push` (Node-only). `apps/parrot/workers/lib/vapid.ts` implements RFC 8292 Authorization header inline using `crypto.subtle.importKey('pkcs8', …, ECDSA P-256)` + `crypto.subtle.sign(ECDSA SHA-256)`. Zero npm deps. Audit pattern locked: `grep -r "\"web-push\"" apps/parrot/package.json` MUST return empty.
  - **Unencrypted JSON push body for v1.2 — RFC 8291 (aes128gcm) deferred to v1.3.** Title/body/url payloads sent today carry no PII (todo titles, sender names, message snippets up to 100 chars). RFC 8291 is real work and the bodies will eventually need it; flagged in `workers/lib/vapid.ts` header + SUMMARY. SW already parses `event.data.json()` so the future swap is internal.
  - **Service worker is push-only — NO `fetch` listener.** Hijacking fetch would collide with Vite/React Router asset versioning and cause stale-bundle pain. Greppable invariant: `grep "addEventListener.*fetch" apps/parrot/public/sw.js` MUST return empty. Service worker is at `/sw.js` served by Vite from `apps/parrot/public/sw.js`.
  - **PUSH_VAPID_* env both OPTIONAL → Worker boots without keys.** `Env` type declares both as `string | undefined`. `sendPushToSubscriptions` records the notification row first, then checks for keys; without keys, logs `console.warn('VAPID keys not configured — skipping push fan-out (notification row saved)')` and returns. Decouples code merge from the user-action of generating VAPID keys.
  - **Drawer-first, push-second pattern.** Every trigger call site goes `void this.sendPushToSubscriptions(...)`. Inside that method, `addNotification` ALWAYS runs even if push fan-out fails or is skipped. The in-app drawer is the canonical surface; browser push is a delivery enhancement. Disconnects "did we record this event" from "did the browser get a popup."
  - **Trigger-at-source pattern, NOT event bus.** Three push call sites live INLINE next to the source-of-truth mutation: `insertTodos()` (urgent_todo when urgency_score >= 70), `createEmail()` (starred_email when folderId === Folders.INBOX && data.starred === true), `pollMattermostNewPosts()` (chat_mention when message includes @{profile.displayName}). No separate event bus / queue — keeps the data-flow grep-able in one file.
  - **`insertTodos` pre-insert dedup check (Rule 1 bug fix).** The unique index on `(source_channel, source_id)` makes `INSERT OR IGNORE` silently drop re-inserts (e.g., Mattermost alarm re-polls). Without dedup, every poll cycle would re-push the same urgent todo. Fix: before each insert, `SELECT 1 FROM todos WHERE source_channel=? AND source_id=?` — only queue a push when the row was NOT already present. Preserves "push fires once per real urgent todo" semantic.
  - **410/404 push prune pattern.** When `fetch(sub.endpoint, ...)` returns 410 Gone or 404, the subscription is dead and we call `removePushSubscription(sub.endpoint)`. All other non-2xx responses log a warning but keep the subscription (could be transient push-service issue). Each push attempt wrapped in try/catch so one failure doesn't take down the fan-out. Fan-out uses `Promise.all` so slow endpoints don't serialize.
  - **Notifications scoped per-employee on EmployeeMailboxDO — NO new DO class.** Both new tables go on the existing per-employee DO via migration `4_notifications_push`. Per-employee scoping is enforced via WHERE employee_id = (profile.employee_id) on every read path. There is no path through which one employee can read another's notifications over HTTP.
  - **Smoke endpoint is store/read/mark-read only — NOT live push.** `POST /api/dev/smoke/push` exercises `addPushSubscription` + `addNotification` + `getNotifications` + `markNotificationsRead` + `removePushSubscription`, asserts `{pass: true}` based on those five steps. Does NOT call `sendPushToSubscriptions` (which would need VAPID keys for a real test). Live VAPID fan-out is covered by separate integration testing once keys are provisioned. Pattern: dev smoke endpoints must be deterministic without external secrets.
  - **WorkspaceShell registers SW on mount but does NOT auto-prompt for push permission.** Push opt-in is part of the onboarding wizard (Plan 13-03). Wave 1 only ensures the SW is installed so the wizard's `pushManager.subscribe()` call works without an install round-trip. Avoids user-hostile "this site wants to send notifications" popup on first dashboard visit.
  - **Bell badge is a red DOT, not a count digit.** Matches Slack/Linear visual weight; avoids relayout when unread crosses 9→10. Drawer-open auto-marks-all-read after 600ms delay so user sees the unread highlight first.
  - **Skills-referenced comment block** landed in 2 places this wave: `durableObject/index.ts` top of file (`agents-sdk, durable-objects — Web Push VAPID signing via crypto.subtle`) + `public/sw.js` header (`cloudflare`).
  - **Deviations: 3 auto-fixed, zero scope creep.** (a) Rule-3 Blocking — added `apps/parrot/workers/lib/vapid.ts` (not in plan's `files_modified`; extracted from inline DO method for clarity); (b) Rule-3 Blocking — added `apps/parrot/app/lib/api.ts` modifications (drawer needs typed `getNotifications`/`markNotificationsRead`/`subscribePush`/`unsubscribePush` helpers; not in plan's `files_modified` but required for the UI); (c) Rule-1 Bug — `insertTodos` pre-insert dedupe described above. All three documented in 13-01-SUMMARY.md Deviations section.
  - **VAPID-USER-ACTION-01 backlog:** user must generate VAPID keypair (`npx web-push generate-vapid-keys --json` or `openssl ecparam -genkey -name prime256v1 ... | openssl pkcs8 -topk8`), paste publicKey into `wrangler.jsonc vars.PUSH_VAPID_PUBLIC_KEY`, `wrangler secret put PUSH_VAPID_PRIVATE_KEY` (PKCS#8 PEM form), AND persist private key to Infisical `/internjobs-ai/PUSH_VAPID_PRIVATE_KEY` per project secrets-to-Infisical convention. Until then, drawer stays usable; live `Notification.showNotification` fan-out is suppressed.
  - **Phase 13 readiness:** Wave 2 (13-02 Cross-pane Actions) can record cross-pane confirmation notifications via `addNotification`. Wave 3 (13-03 Onboarding Wizard) push opt-in calls `navigator.serviceWorker.ready` → `pushManager.subscribe({applicationServerKey: PUSH_VAPID_PUBLIC_KEY})` → `api.subscribePush()`; wizard needs a new `GET /api/push/vapid-public-key` endpoint to fetch the key client-side (not built in 13-01 — wizard-coupled, flagged for 13-03).

- 2026-05-19: **PHASE-12 Wave 3 (12-03) — Dashboard Mothership Agent UI + Ranking shipped.** Two atomic commits on main (8e71b18 + 5596b9b) in 2m 32s. Wave 3 makes the dashboard pane render real ranked todos and adds a deterministic regression gate for the hybrid-rank SQL. Phase 12 is COMPLETE — Phase 13 (Cross-pane Actions + Launch Polish) is unblocked. Key locked decisions:
  - **TodoCard is presentation-only with onSelect hoist.** The card receives `onSelect: (todo) => void` rather than `href` so the parent decides routing. Reusable from search / settings / digest email previews without pre-binding to a URL. The component is a `<button type="button">` not an `<a>`.
  - **Chat-source click-through goes to /chat ROOT, NOT a deep-link.** Mattermost's SSO bridge (`apps/parrot/workers/routes/oidc.ts`) does not currently accept or forward a `?post={id}` query param to the Mattermost web client. Inline comment in `handleSelect()` documents the v1.3 follow-up. Alternative — manipulating the iframe `src` post-SSO — would have been fragile across the redirect chain.
  - **Ranking regression is DETERMINISTIC via debugInsertTodo, NOT live LLM.** New `POST /api/dev/smoke/ranking` inserts two todos with explicit urgency scores (80 + 20) and asserts hi.rank > lo.rank. Runs without `PARROT_AI_GATEWAY_ID` / `CLOUDFLARE_AI_API_TOKEN` — the only moving part under test is the hybrid-rank SQL in `EmployeeMailboxDO.getTodos()`. Live-LLM coverage stays on Wave 2's `seed-email` endpoint.
  - **Date formatting via built-in Intl.DateTimeFormat.** No date-fns / dayjs / luxon dependency added. Age math is raw `Date.now() - new Date(...).getTime()` arithmetic. Same posture as Phase 10 — Parrot UI deliberately stays on built-ins to keep the bundle small.
  - **TodoItem interface lives in `app/components/TodoCard.tsx` as single source of truth.** Future panes/tests should `import { type TodoItem }` from there rather than redeclare. When a column changes in `workers/durableObject/index.ts`, the type fan-out is one file.
  - **Empty-state copy is view-aware.** Each filter (all / mentions / today / week) gets its own first-line ("Your workspace agent is monitoring your channels.", "No mentions yet.", "No today's todos.", "No this week's todos.") so the user understands WHY there are no results in this filter rather than a generic "no items".
  - **Loading / error / empty / list four-state UI pattern established.** Every Parrot pane that fetches a list should mirror this exact shape (LoadingSkeleton + ErrorCard + EmptyState + map-over-list). Reusable across future surfaces.
  - **useEffect fetch with cancelled-flag cleanup.** React 18 strict-mode double-fires the effect; the cleanup sets `cancelled = true` so the second response doesn't stomp the first. Migrating to React Query is a Phase 13 task once resolve/snooze actions need cross-pane invalidation.
  - **Skills-referenced "Workers AI via AI Gateway" comment block** landed in 3 places this wave: `TodoCard.tsx`, `dashboard.tsx`, ranking endpoint header in `workers/index.ts`. Greppable invariant: every file that surfaces agent output references the gateway-not-direct-REST transport rule. Audit clean (`grep -rn "gateway.ai.cloudflare.com\|/workers-ai/"` returns only `workers/lib/ai.ts`).
  - **Deviations: NONE.** Plan executed exactly as written. `files_modified` frontmatter (TodoCard.tsx + dashboard.tsx + workers/index.ts) matched `git diff --name-only HEAD~2 HEAD` output one-for-one. No additional files touched. Zero new npm packages.
  - **Phase 13 follow-ups identified:** (a) SSO-bridge `?post=` param for chat deep-link; (b) `audit_events` migration to surface Wave 2's 429-quota logs; (c) optional migration of dashboard `useEffect` fetch to `useQuery` when Phase 13 adds resolve/snooze mutations.

- 2026-05-19: **PHASE-12 Wave 2 (12-02) — Dashboard Mothership Agent ingest + extraction shipped.** Two atomic commits on main (611ba42 + b502609) in 3m 5s. Wave 2 wires email + Mattermost ingest into the Wave 1 storage scaffolding via the existing AI Gateway helper. Key locked decisions:
  - **AI Gateway is the SINGLE LLM transport for Parrot.** No direct Workers AI REST anywhere — grep audit confirms only `gateway.ai.cloudflare.com` appears in LLM-touching code. The student app keeps direct REST (single shared Maya identity, no per-user quota concept).
  - **Email storage MUST NOT block on LLM.** `extractTodosFromEmail` is called as `void this.extractTodosFromEmail(...)` inside `createEmail()` when `folderId === Folders.INBOX`. Errors are caught + console.error'd but never propagate to the inbox writer. This is the architectural invariant for v1.2 — any future change MUST preserve it.
  - **DO alarm self-reschedules in `finally`.** The 2-minute polling alarm always runs `await this.ctx.storage.setAlarm(Date.now() + 2*60*1000)` in a `finally` block — so a Mattermost 5xx, network blip, or thrown exception inside `pollMattermostNewPosts()` cannot kill the polling cycle. At-least-once delivery is guaranteed.
  - **Mattermost `since=ms-5000` overlap window.** Mattermost has a known race condition (`mattermost/mattermost#13846`) where the `since` parameter misses posts under high write load. Mitigation: every poll subtracts 5000ms from the watermark for an intentional overlap. Deduplication is handled by `INSERT OR IGNORE` on `(source_channel, source_id)` so re-fetches are idempotent.
  - **Cache TTL split: 3600 email / 1800 chat.** Email bodies are highly idempotent (same body to multiple cc/bcc recipients re-routes through same DOs) → 1 hour cache. Chat posts can be edited/forwarded → 30 minute cache. Both differentiated via `cf-aig-cache-ttl` header.
  - **`audit_events` table is best-effort.** Plan body explicitly notes this table is a future migration. Both 429-handler sites wrap the INSERT in try/catch so a missing table never crashes the extractor. When the migration lands, the existing INSERT statements start succeeding without code changes.
  - **MM_USER_ID_NONE sentinel + retry on every alarm.** First-alarm before an employee has SSO'd into Mattermost: `resolveMmUserId` returns null, we cache the `__not_found__` sentinel. On EVERY subsequent alarm we re-call `resolveMmUserId` — so the moment they log into Mattermost their alarm auto-onboards without operator intervention.
  - **First-post id as chat batch source_id.** Chat extraction batches multiple posts per LLM call to keep cost down. Using `posts[0].id` as the dedup key means re-polls of the same window land against the same source_id and `INSERT OR IGNORE` drops duplicates cleanly.
  - **`debugInsertTodo` RPC behind `PARROT_DEV_MODE` gate.** Plan 12-03's regression tests need deterministic urgency_score insertion to assert the hybrid-rank SQL. This RPC bypasses LLM extraction entirely; returns `{ inserted: false }` in production where `PARROT_DEV_MODE` is unset.
  - **Smoke endpoint `POST /api/dev/smoke/seed-email`.** `PARROT_DEV_MODE`-gated. Seeds a synthetic urgent contract email, waits 500ms for fire-and-forget extraction, asserts `getTodos('all')` returns ≥1 email-sourced todo. Returns 403 without `PARROT_DEV_MODE`. Used as the seed for Wave 3's end-to-end test harness.
  - **Deviations: NONE.** Plan executed exactly as written. The `files_modified` frontmatter listed `apps/parrot/workers/types.ts` but inspection confirmed `PARROT_AI_GATEWAY_ID` + `PARROT_DEV_MODE` were already declared by Wave 1 — Task 2 action text explicitly anticipated this ("If they are already there from Wave 1, do not duplicate them"), so leaving types.ts untouched is in-spec, not a deviation.
  - **Wave-3 readiness:** `getTodos(view)` already returns ranked rows in the hybrid formula — Wave 3 layers UI on top without DO changes. `debugInsertTodo` is the deterministic test entrypoint. `POST /api/dev/smoke/seed-email` is the e2e seed.
  - **New backlog: MM-USER-ACTION-01** — user must create Mattermost bot account (System Admin role) and `wrangler secret put MATTERMOST_BOT_TOKEN` on the `internjobs-parrot` Worker before production chat ingest starts. Code fail-soft (skip silently) when token is unset.

- 2026-05-19: **PHASE-12 Wave 1 (12-01) — Dashboard Mothership Agent foundation shipped.** Three atomic commits on main (5fe02a9 + 7ef33cc + e4e2ed4) in 6m 23s. Wave 1 establishes the storage shape, AI Gateway transport, and Phone/SMS UI seams that Waves 2 + 3 plug into. Key locked decisions:
  - **AI Gateway over direct Workers AI REST for Parrot.** Parrot LLM calls route through `gateway.ai.cloudflare.com/v1/{accountId}/{gatewayId}/workers-ai/{model}` with `Authorization: Bearer ${CLOUDFLARE_AI_API_TOKEN}` + `cf-aig-metadata: {"user_id": "<clerk_user_id>"}` + `cf-aig-cache-ttl: <seconds>` headers. Rationale: per-employee daily quota + prompt cache. Student app at `apps/app/` KEEPS direct Workers AI REST — single shared Maya agent identity has no per-user quota concept; migrating student app is a separate v1.3 task. Locked decision per `ROADMAP.md` line 209.
  - **Model: `@cf/moonshotai/kimi-k2.6`.** Pinned via `KIMI_MODEL` env var (default `@cf/moonshotai/kimi-k2.6`) so we can A/B fall back to `llama-3.3-70b-instruct-fp8-fast` without code changes. kimi-k2.6 supports `response_format: { type: "json_schema" }` natively — gives deterministic structured todo extraction with no regex parsing.
  - **Wave-1 stub-first DO RPC.** `EmployeeMailboxDO.getTodos(view): Promise<unknown[]>` returns `[]` in Wave 1 so the Hono route + React UI can ship final-state code in this wave; Wave 2 fills the implementation behind the same method signature.
  - **Fail-soft AI helper.** `callAiGateway()` + `extractTodosFromText()` both return `[]` (or `null`) on missing config, 429 quota exceeded, or any non-2xx response. The contract is: todo extraction failure NEVER blocks email storage or alarm rescheduling. Audit logging for 429s is the DO caller's responsibility (Wave 2 — only DO has `ctx.storage.sql` access).
  - **Phone + SMS = SEAM, not integration.** `/phone` and `/sms` routes render a "Coming soon — Telnyx via Cloudflare Agents SDK" card with inline source comments documenting the future `@cloudflare/voice` + `withVoice(Agent)` + `@cloudflare/voice-twilio` architecture. NO telephony npm packages installed (per `feedback-seam-not-integration` rule). The actual telephony backend lands in v1.3+.
  - **Env additions:** `CLOUDFLARE_AI_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `PARROT_AI_GATEWAY_ID`, `KIMI_MODEL` (all required for Wave 2 live LLM calls); `MATTERMOST_BOT_TOKEN?`, `PARROT_DEV_MODE?` (optional, used by later waves).
  - **Wave-2 user-action gate:** user must create CF AI Gateway (`internjobs-parrot` slug, 200 req/day/employee per-user limit) + run `wrangler secret put` for `PARROT_AI_GATEWAY_ID` + `CLOUDFLARE_AI_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` (`0fffd3dc637bdb26d4963df445a69fd3`) on the `internjobs-parrot` Worker before Wave 2 production deploy can make live LLM calls. Wave-2 dev work proceeds with envs unset (fail-soft).
  - **SEC-ROTATE-CF-AI-PARROT-01 (NEW backlog item):** rotate `CLOUDFLARE_AI_API_TOKEN` after Wave 2 production deploy lands. Same posture as SEC-ROTATE-CF-AI-01 (student app token).
  - Deviations from plan: 2 Rule-3 path corrections (routes config file is `apps/parrot/app/routes.ts` NOT `apps/parrot/app/routes/routes.ts`; `getTodos` stub added to EmployeeMailboxDO — explicitly requested in Task 2 body but missing from plan frontmatter `files_modified`). Both anticipated by plan task text, no scope creep, documented in `12-01-SUMMARY.md` Deviations section. Pre-existing `npm run build` / `wrangler deploy --dry-run` failures verified by stash+rebuild — NOT a regression from this plan. TypeScript compiles clean across all 3 task commits.

- 2026-05-17: **MEMORY-01 — self-hosted graph memory shipped (Phase B).** User picked FalkorDB + Graphiti specifically so we can SELF-HOST (no vendor lock-in, no per-month managed service); the request was literally "pick graphiti and falkordb so that we can self host". Graphiti's official lib is Python; we deliberately did NOT introduce a Python sidecar — wrote a thin Node implementation of the same temporal-fact pattern at `apps/app/src/memory/graph.mjs` against FalkorDB's Cypher API (`falkordb@6.6.2` npm client speaks Redis wire protocol + Cypher). New Fly app `internjobs-graph` in `internjobs-sios-org` region `ord`: official `falkordb/falkordb:latest` image (Redis 8 + FalkorDB module 4.18) wrapped by a custom Dockerfile + entrypoint.sh; 10GB Fly volume `falkor_data` at /data with AOF persistence; shared-cpu-1x 1GB RAM; single machine; NO public IP. Reachable ONLY at `internjobs-graph.internal:6379` on the org's private network. Password auth via Fly secret `REDIS_PASSWORD` (openssl rand -hex 32). Custom Dockerfile rationale: the upstream image's `/var/lib/falkordb/bin/run.sh` does `exec redis-server ${REDIS_ARGS} ...` and the resulting tokens are passed without a second shell expansion pass, so an embedded `$REDIS_PASSWORD` lands as a literal string at redis-server. Attempt to override the machine `cmd` collided with Fly's secret-injection prefix layer (env vars get a `<machine_id>_` prefix that only /fly/init's official-image-cmd path un-prefixes). The fix is a thin wrapper image whose `entrypoint.sh` does a fresh `${REDIS_PASSWORD}` expansion AFTER Fly init has populated the env. Diagnostic log emits the password LENGTH (not value) on every boot so a misconfigured env is caught loud. Graph schema: `:Student / :Role / :Startup` subjects, `:Fact` nodes attached via `-[:HAS_FACT]->`. Each fact stores predicate / object_value / confidence / source_message_id / valid_from / valid_to / created_at + deterministic id (sha256(subjectId|predicate|objectValue|sourceMessageId) → first 32 hex chars). Single-valued predicates (STUDIES_AT, STATUS, PREFERS) close conflicting active facts atomically (valid_to = now()) — the Graphiti close-out invariant that makes "I never said that" pushback work. Multi-valued (INTERESTED_IN, SKILLS_IN, MENTIONED, OTHER) accumulate additively. Idempotent (re-record over same logical fact is a no-op via the deterministic id). Workflow integration in `apps/app/src/workflows/student-inbound.mjs`: pre-LLM `getStudentSummary(studentId)` injection as the FIRST block of the per-turn user prompt — empty → block omitted entirely (no useless empty section on first turn). Post-reply fire-and-forget `recordTurnFacts({studentId, inboundBody, agentReply, sourceMessageId, model})` runs AFTER the workflow commits its user-visible outcome — calls Workers AI 70B with a tight JSON-only extraction prompt (temperature 0.1, max_tokens 400; up to 5 facts), parses defensively (slices first `[` to last `]`), and writes each fact via recordFact. Fail-soft end-to-end: when FALKORDB_URL is unset OR the DB is down, every exported function returns safe defaults (null/empty/"") + logs one-line warning; app boots, workflow completes turn, /healthz reports `graphReady: false`. New /healthz key `graphReady` (cached 30s — pingGraph round-trips a `RETURN 1` query). Sample SAMPLE_SUMMARY string from smoke run (the prompt-injection format): `studies at: Columbia. interested in: typescript, backend python. prefers: small startup, remote or NYC. mentioned: Acme Backend Python Intern role - intro pending operator. last active: 2026-05-17 16:01.` New script `npm run smoke:graph` validates 6 invariants: PING, schema bootstrap, 5 seed facts, conflict close-out (NYU → Columbia for STUDIES_AT), summary contains Columbia and NOT NYU, recallHistory("python") returns python-flavored top result. Skipped (exit 0) when FALKORDB_URL is unset, so build:app and smoke:ops remain hermetic in CI. Files changed: `infra/falkordb/{Dockerfile, entrypoint.sh, fly.toml}` (new); `apps/app/src/memory/graph.mjs` (new, ~440 LOC); `apps/app/src/workflows/student-inbound.mjs` (graphSummary pre-LLM block in composePrompt + recordTurnFacts post-reply hook); `apps/app/src/server.mjs` (ensureGraphSchema bootstrap + graphReady in /healthz + 30s cache); `apps/app/scripts/smoke-graph.mjs` (new); `apps/app/package.json` (new dep `falkordb@6.6.2` + `smoke:graph` script). build:app + smoke:ops + smoke:graph all green. Deployed live to `internjobs-ai-student-app`; production `/healthz` confirms `graphReady: true`. Infisical: added `FALKORDB_URL` + `FALKORDB_PASSWORD` to /internjobs-ai. Commits: `629bcc1` (FALKORDB-DEPLOY initial fly.toml + Fly app creation + volume + secrets) + `41584c4` (fix custom Dockerfile + entrypoint.sh after upstream image's REDIS_ARGS expansion bug was traced via /proc/$pid/environ inspection) + `cd4af93` (MEMORY-01 graph.mjs + workflow hooks + server bootstrap + smoke-graph script).
- 2026-05-17: **AGENT-VOICE — agent model upgrade + voice tuning.** Chat model bumped from `@cf/meta/llama-3.1-8b-instruct` → `@cf/meta/llama-3.3-70b-instruct-fp8-fast` for conversational quality. The 8B was producing generic customer-service-flavored SMS (canned-stub-feeling "Hi! Thanks for reaching out") — wrong tone for the autonomous-send posture shipped same day. fp8-fast is the cost-optimized 70B variant on Workers AI (8-bit quantized, fast-path inference). Live-probed reachable on account `0fffd3dc637bdb26d4963df445a69fd3` (returned `success:true`); `@cf/meta/llama-3.3-70b-instruct` (non-fast) returned "No route for that URI" on this account and is NOT a fallback; `@cf/meta/llama-3.1-70b-instruct` is the documented fallback if fp8-fast ever drops. System prompt rewritten with explicit voice rules + 5 few-shot exemplars lifted from a competitor's recruiting-agent SMS thread. Voice rules: all lowercase except proper nouns; short bursts; hyphen-break " - " between adjacent clauses (no em-dashes); no emojis; first-person ("I"); never refer to self as AI/bot/assistant; push back politely when candidate is wrong ("right - I never said you'd get an email from me"); name actual roles/companies/schools (no "various roles"); offer concrete next steps ("want me to pull the brief?"). New exports in `apps/app/src/workflows/student-inbound.mjs`: `AGENT_VOICE` (style rules), `AGENT_VOICE_EXEMPLARS` (5 user/agent pairs), `AGENT_SYSTEM_PROMPT` (composed: persona + voice + exemplars + safety + SMS-length cap). `AGENT_SAFETY_GUARDRAILS` retained and merged into the composed prompt. `loadStudentProfile` extended to also pull `students.name` + most recent `profile_snapshots.display_name` and derive a `firstName` for in-prompt addressing; `composeProfileBlob` emits `first_name:` + `full_name:` keys for the LLM. `composePrompt` slimmed — persona/voice/safety moved to the system message; per-turn dynamic context (profile / thread / matched role / new inbound) stays in the user message. LLM call params: `max_tokens` 512 → 800 (70B can generate slightly longer outputs when context is rich; SMS still targets 1–3 sentences so 800 is a safety ceiling not a target), `temperature` 0.7 → 0.5 (tighter style adherence — voice rules are precise). Stub-mode fallback (`LLM_PROVIDER=stub` or missing `CLOUDFLARE_AI_*` envs) preserved exactly so smoke tests still run hermetically. Embedding model UNTOUCHED — stays `@cf/baai/bge-base-en-v1.5` (768-dim, pgvector tables locked by migration 0005). No new npm deps, no new env vars, no Infisical writes (model name is a code default with `AGENT_MODEL` env override). Live sample outputs validated against the deployed system prompt with `temperature=0.5`: opener returns `"hey - been a minute. i've got a growth engineer role at valon that might be up your alley, want me to pull the brief?"`; out-of-scope ask ("can you email me the JD? what does it pay?") returns `"I can't fire off emails from this thread - I work over text - want me to lay out the growth engineer role at Valon, including comp details?"` (note the on-rule pushback + concrete next step + role/company name). Phase B (v1.3+) will add Graphiti+FalkorDB graph memory; Phase A (this session) is last-N-messages thread context via the existing slot. Files changed: `apps/app/src/workflows/student-inbound.mjs` (model default + DEFAULT_MODEL comment block + loadStudentProfile name pull + pickName/deriveFirstName helpers + composeProfileBlob first_name+full_name + AGENT_VOICE/AGENT_VOICE_EXEMPLARS/AGENT_SYSTEM_PROMPT exports + composePrompt rewrite + runLLM system message swap + max_tokens 800 + temperature 0.5). build:app + smoke:ops green. Commits: `feat(AGENT-VOICE): upgrade model to Llama 3.3 70B + style-transfer system prompt` + `docs: log model upgrade + voice tuning decision`.
- 2026-05-17: **AUTONOMY PIVOT (AGENT-AUTO + OPS-AUDIT)** — removed the v1.2 operator approval gate. Mastra workflow (apps/app/src/workflows/student-inbound.mjs) now drafts AND autonomously sends agent responses on student SMS (same `outbound.routeAndSend` call site that the future startup-email-drafting workflow will reuse). Drafts INSERT with transient `status='sending'` and UPDATE to `'sent'` (with `sent_at` + `provider_message_id`) on success or `'failed'` on send error. Send failures wrap in try/catch and write `audit_events` `event_type='auto_send_failed'`; workflow never rethrows (auto-retry is v1.3). /ops/drafts becomes a READ-ONLY audit log of every draft regardless of status, ordered newest-first; renderMessageLog replaces renderDraftQueue. Detail view at /ops/drafts/:id is read-only (no approve/edit/reject forms; only a flag-for-review form). Deprecated POST /ops/drafts/:id/{approve,edit,reject} return 410 Gone with `reason='approval_gate_removed_2026_05_17'` so external tooling gets a clear deprecation signal. POST /ops/drafts/:id/flag is the new write endpoint — writes `draft_feedback` with `feedback_type='flagged'` + free-text reason; does NOT mutate the draft. /ops/feedback now filters `feedback_type='flagged'` by default. Safety posture: system-prompt-level guardrails (AGENT_SAFETY_GUARDRAILS — no legal/financial promises, no PII about other parties, polite refusal of illegal asks). Lakera Guard pre-LLM screening planned for v1.3 (SAFETY-01 added to REQUIREMENTS.md backlog). Rationale: turn-by-turn approval latency made conversational UX impossibly slow — would feel non-conversational. Risk acknowledged (agent can say bad things) and mitigated by prompt guardrails + flag-for-review + v1.3 Lakera Guard. Files changed: apps/app/src/workflows/student-inbound.mjs (autonomy + guardrails + status='sending' transition), apps/app/src/outbound.mjs (header rewrite — structural "no auto-send" invariant dropped), apps/app/src/server.mjs (workflow call passes smsProvider+config + /ops/* route reshape + 410 Gone for deprecated routes), apps/app/src/views.mjs (renderMessageLog + read-only renderDraftDetail + status badges + renderFeedbackLog rename + startup onboarding consent copy), apps/app/src/store.mjs (listAllDrafts + listDraftFeedback feedbackType filter), apps/app/scripts/smoke-ops.mjs (30+ assertions rewritten: seed status='sent' draft, flag-for-review, 410 Gone for approve/edit/reject, failed-draft error banner). build:app smoke + smoke:ops + test:auth all green. Planning artifacts updated: PROJECT.md (Active OPS-01/02 + Constraints rewrite + Key Decisions row + Out of Scope removal), REQUIREMENTS.md (OPS-01/02 + SAFETY-01 backlog entry + Traceability table rename + Out of Scope row removal), ROADMAP.md (Phase 05 rename + success criteria rewrite + v1.2 Overview pivot note), USER-ACTIONS.md (Section E rewritten to describe autonomous flow). Commits: `d8a0bb3` (AGENT-AUTO) + `57e3320` (OPS-AUDIT). The user can now text the Spectrum number → agent replies autonomously (no operator intervention).
- 2026-05-16: **AUTH-PROD adopted `@clerk/backend.authenticateRequest()`** for prod auth. The home-rolled JWKS-only verifier (`verifyJwtWithJwks`) in `apps/app/src/auth.mjs` only checked the `__session` cookie. Clerk's production custom-domain flow (clerk.internjobs.ai + accounts.internjobs.ai → app.internjobs.ai on the first hop after sign-in) passes a `__clerk_handshake` URL parameter that needs to be exchanged for a session cookie — the old code missed it and 302'd back to sign-in, producing an auth loop in prod that bit a real user (rentalaraj@gmail.com / user_3Dpf7uzGF5ojQNsdOtCEM6SFUcr) after LinkedIn sign-in. Pinned `@clerk/backend@3.4.9` exact (no `^`). New helper `applyHandshakeOrContinue(res, auth)` is the single function that translates the SDK's `status: 'handshake'` state into a 307 with the SDK's own `Location` + `Set-Cookie` headers — called from every `getAuth()` callsite in `server.mjs` (5 of them) plus the `requireStartupAuth` + `requireOperatorAuth` middlewares. Dev paths (signed cookie + `x-clerk-user-id` header) preserved exactly and short-circuit BEFORE the SDK is constructed, so smoke tests run without CLERK_* envs. `normalizeClaims` shape unchanged so `requireAuth`, `requireOperatorAuth`, `/auth/callback`, etc. all work without callsite changes. `verifyJwtWithJwks` + `bearerToken` removed as dead code (only used on the prod path the SDK now owns). The Clerk Backend API lookup in `requireOperatorAuth` (PITFALLS #13) is unchanged — that's a separate concern from session verification. No new env vars (existing `CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `CLERK_BACKEND_API_URL` are all the SDK needs; `CLERK_JWKS_URL` is now unused but harmless to keep). Deployed live to `internjobs-ai-student-app`. test:auth (5 tests) + build:app smoke green; `/healthz` post-deploy all-green; `/auth/callback` GET → 302 to sign-in for unauthenticated curl (correct; the loop fix manifests only when a real browser with the handshake param hits it).
- `SmsProvider` interface lives at `apps/app/src/sms/provider.mjs` (JSDoc contract) + `apps/app/src/sms/spectrum.mjs` (implementation). Wire-through via `smsProvider.{verifyWebhook,parseInbound,sendSms,listen}` in `server.mjs` and `spectrum-listener.mjs`.
- Single Clerk app + `publicMetadata.userType` discriminator (`student | startup | operator`). No second Clerk app. `requireOperatorAuth` re-fetches `publicMetadata` via Clerk Backend API on every operator request (not the JWT) per PITFALLS #13.
- Mastra runs in-process inside the existing Express app. Dedicated `mastra` Postgres schema. `@mastra/core@1.35.0` exact pin. `@mastra/pg@1.11.0` adapter. `text-embedding-3-small` (1536 dims) locked in migration.
- pgvector HNSW indexes created in-transaction (CONCURRENTLY dropped — `migrate.mjs` wraps each migration in BEGIN/COMMIT). Safe at v1.2 data volume.
- Unified outbound router lives at `apps/app/src/outbound.mjs`. It is the SOLE module that calls `smsProvider.sendSms` or `sendStartupEmail` for agent drafts. Verified by grep at end of Phase 05.
- v1.1 pairing welcome SMS remains an auto-send (the one pre-existing exception; not an agent draft).
- CF Email Worker + Fly ingest use HMAC-SHA256 with Node `crypto.timingSafeEqual` for verification. Worker falls back to `message.forward(OPERATOR_FALLBACK)` on Fly failure per PITFALLS #7. Cloudflare Queues deferred (TODO note in Worker code).
- Phase 04 Flag 2/3 fixes applied: `student_threads.provider='cognee' → 'mastra'` data migration; `confirmPairingCode` parameterized on `provider` (was hardcoded `'photon'`).
- 2026-05-16: EMAIL-02 outbound provider swapped from Resend to Cloudflare Email Service (the "Agent Mail" product, public beta 2026-04-17). Rationale: internjobs.ai is already on Cloudflare DNS (the hard prereq), already uses CF Email Routing for inbound, and standardizing on one vendor for email cuts an integration. Implementation: `apps/app/src/email/outbound.mjs` rewritten as a `fetch()` call to `POST /accounts/{account_id}/email/sending/send` (no SDK, no new npm dep — `resend` removed). Config keys swapped to `CLOUDFLARE_EMAIL_ACCOUNT_ID` + `CLOUDFLARE_EMAIL_API_TOKEN`; `/healthz` now reports `cloudflareEmailReady`. Future v1.3 could move the call into a Worker binding — out of scope here.
- 2026-05-16: **STORAGE-01 scope-add** — R2 storage scaffold ported (and trimmed) from SuperIntelligence `r2-uploads-client.ts`. Private bucket `internjobs-agent-store` with per-entity folder convention (`students/{id}/`, `startups/{id}/`, `conversations/{id}/`, `startups/{id}/roles/{role_id}/`). Signed-URL-only sharing (Mala posture — every share is auditable + time-bounded). Client at `apps/app/src/storage/r2.mjs` fails soft (returns null) on missing envs; `/healthz` reports `r2Ready`. NO ingestion wired yet — STORAGE-02 (v1.3) lands attachment ingest; STORAGE-03 (v1.3) lands permanent short links via mapping bucket. Rationale: row-level partition via student_id/startup_id is sufficient for InternJobs's volume; per-user Postgres schemas (SuperIntelligence's stronger isolation) are too heavyweight here.
- 2026-05-16: **EMAIL-03 scope-add** — per-conversation Reply-To aliases (`conv-{conversation_id}@internjobs.ai`) become the PRIMARY inbound startup identification path. Catch-all CF Email Routing rule (Phase 03 B5) already routes any `*@internjobs.ai` to the Worker; we now parse the `conv-` prefix and ship the UUID in the signed JSON payload. Fly /webhooks/email validates and writes it into `inbound_messages.metadata.conversation_id`. Replaces fragile From-address lookup; legacy lookup stays as fallback. Stamped onto outbound drafts via `agent_metadata.reply_to` (read by `outbound.mjs` and passed through to CF's `reply_to` field). New audit event_type `startup_email_received_by_alias` distinguishes the deterministic path.
- 2026-05-16: **Workers AI swap** — replaced OpenAI (embeddings + chat completions) with Cloudflare Workers AI via a thin proxy Worker `internjobs-ai-proxy` (`apps/ai-worker/`). The Worker uses the native `env.AI` binding, so the Fly Node app never holds a Cloudflare API token — auth is a shared `AI_WORKER_SECRET` (constant-time compared in the Worker; stored in both Infisical and wrangler). Embedding model is `@cf/baai/bge-base-en-v1.5` (768-dim, down from OpenAI's 1536); chat model is `@cf/meta/llama-3.1-8b-instruct`. Best-effort AI Gateway routing via `{gateway:{id:'internjobs-ai'}}` — calls succeed even when the gateway doesn't exist (CF error code 2001 → silent fallback to direct Workers AI). Migration 0005 swapped both `student_embeddings.embedding` and `role_embeddings.embedding` from `vector(1536)` to `vector(768)` (tables were empty, safe atomic change) and rebuilt HNSW indexes. The `openai` npm dep was removed; `apps/app/src/embeddings.mjs` and `apps/app/src/workflows/student-inbound.mjs` now use plain `fetch()` to the Worker. `/healthz` drops `openaiKeyPresent` and adds `aiProxyReady`. Stub fallback (`LLM_PROVIDER=stub` or missing `AI_WORKER_*` envs) preserved so dev/test boots without secrets. Deployed live to prod 2026-05-16. End-to-end smoke confirmed in prod (`aiProxyReady: true`; manual /embed returns 768-dim vec; manual /chat returns Llama response).
- 2026-05-16: **EMAIL-03 subdomain isolation** — agent email aliases moved from apex `internjobs.ai` to dedicated `agent.internjobs.ai` subdomain. CF Email Routing has supported subdomains since Oct 2025; we bind the Worker to `*@agent.internjobs.ai` catch-all only. Apex (`*@internjobs.ai`) stays for human/employee email (raj@, support@) and is forwarded by a separate apex catch-all rule to `rentalaraj@gmail.com` — apex mail NEVER reaches the Worker. Hard cut-over (no apex fallback in `CONV_ALIAS_REGEX`); a startup that strips the subdomain in their reply lands in the human inbox and the operator handles it manually. Rationale: keeps the apex clean for human routing; prevents the Worker from being a silent fallback for human typos; gives operational clarity (one subdomain = agent, apex = human). Files changed: `apps/app/src/workflows/reply-to.mjs` (regex + builder return value swap to `agent.internjobs.ai`; module header rewritten); `apps/app/src/workflows/reply-to.test.mjs` (10 tests; added negative apex-rejection test); `apps/email-worker/src/index.js` (`CONV_ALIAS_REGEX` swapped; `OPERATOR_FALLBACK` changed `ops@internjobs.ai` → `rentalaraj@gmail.com` with verified-destination-address comment; new non-conv-subdomain branch forwards to operator + best-effort audit POST with `x-email-audit-only: 1`); `apps/email-worker/wrangler.toml` (comments rewritten to document subdomain catch-all + apex human-forward rules); `apps/app/scripts/smoke-ops.mjs` (test assertions + emailDraft.agent_metadata.reply_to swapped to subdomain; added apex-rejection assertion); plus comment-only updates in `apps/app/src/email/outbound.mjs`, `apps/app/src/outbound.mjs`, `apps/app/src/workflows/student-inbound.mjs`, `apps/app/src/server.mjs`. Worker redeployed to `https://internjobs-email-ingest.rentalaraj.workers.dev` (version `06755072-f7fb-4327-9b5c-878cdc78d9d9`). Fly redeployed; `/healthz` still all-green post-deploy (`mastraReady`, `pgvectorReady`, `workersAiReady` all true). Infisical: added `EMAIL_WORKER_URL=https://internjobs-email-ingest.rentalaraj.workers.dev` to `/internjobs-ai` (prod). USER-ACTIONS.md Section B revised: B4 now enables Email Routing on BOTH apex and subdomain; B5 splits into subdomain catch-all rule (→ Worker) + apex catch-all rule (→ `rentalaraj@gmail.com`); new B5a step verifies `rentalaraj@gmail.com` as a CF Email Routing Destination Address before any forwarding rule can target it.
- 2026-05-16: **Workers AI direct tear-out** — torn out the `apps/ai-worker/` proxy Worker entirely. User provided a Cloudflare API token (`CLOUDFLARE_AI_API_TOKEN`) with Workers AI scope confirmed (returned a 768-dim embedding from `POST /accounts/{id}/ai/run/@cf/baai/bge-base-en-v1.5`). Fly Node app now calls the Workers AI REST API directly from `apps/app/src/embeddings.mjs` and `apps/app/src/workflows/student-inbound.mjs` — no proxy Worker, no AI Gateway intermediary in v1.2 (AI Gateway probe returned 401, so we route direct; AI Gateway can be added later by changing the URL prefix). Rationale: one less moving part (no wrangler deploy, no shared-secret rotation, no extra hop). Config keys swap: `aiWorker.{url, secret}` → `cloudflareAi.{accountId, apiToken}`. Env keys swap: `AI_WORKER_URL` + `AI_WORKER_SECRET` → `CLOUDFLARE_AI_ACCOUNT_ID` + `CLOUDFLARE_AI_API_TOKEN`. `/healthz` drops `aiProxyReady` and adds `workersAiReady`. Infisical: deleted `AI_WORKER_URL` + `AI_WORKER_SECRET`; added `CLOUDFLARE_AI_ACCOUNT_ID` (mirroring `CLOUDFLARE_EMAIL_ACCOUNT_ID` value under a cleaner AI-namespace name); `CLOUDFLARE_AI_API_TOKEN` was already added pre-task. Fly: unstaged `AI_WORKER_URL` + `AI_WORKER_SECRET`; staged + imported `CLOUDFLARE_AI_ACCOUNT_ID` + `CLOUDFLARE_AI_API_TOKEN`. Response-shape change (vs. proxy): direct Workers AI returns `{ result: { data: [[...]] | response: "..." }, success, errors }` — embeddings unwrap `result.data[0]`, chat unwraps `result.response`. Stub fallback preserved (now keyed on `CLOUDFLARE_AI_*` envs instead of `AI_WORKER_*`). Migration 0005 untouched — vector column stays `vector(768)`, model stays `@cf/baai/bge-base-en-v1.5`. SEC-ROTATE-CF-AI-01 added to backlog. Build smoke passes (`npm run build:app`).

### Pending Todos (user-only — see USER-ACTIONS.md for ordered checklist)

- Section A: Cloudflare DNS proxy fix; rotate `CLERK_SECRET_KEY`; enable email/Google/Microsoft in Clerk; set operator `publicMetadata.userType`. (Section A5 — `OPENAI_API_KEY` — was DROPPED 2026-05-16 by the Workers AI swap. The proxy Worker / `AI_WORKER_*` user-action was also DROPPED 2026-05-16 by the Workers AI direct tear-out.)
- Section B: Generate `EMAIL_WORKER_SECRET`; store in Cloudflare Worker AND Infisical; enable CF Email Routing on internjobs.ai; add catch-all rule to Worker; confirm `ops@internjobs.ai` fallback; `wrangler deploy`.
- **Section B2 (NEW 2026-05-16):** Create R2 bucket `internjobs-agent-store` (Standard, no public access); create bucket-scoped "Object Read & Write" R2 API token; store `R2_ACCOUNT_ID=0fffd3dc637bdb26d4963df445a69fd3` + `R2_ACCESS_KEY_ID` + `R2_SECRET_ACCESS_KEY` + `R2_BUCKET=internjobs-agent-store` in Infisical; re-run Fly secrets import. Verify `r2Ready: true` in /healthz after D2.
- Section C: Cloudflare Email Service onboard `internjobs.ai` (Email Sending → Onboard Domain → adds `cf-bounce` MX + SPF + DKIM + DMARC); create Account-scoped "Email Sending" API token; store `CLOUDFLARE_EMAIL_ACCOUNT_ID` + `CLOUDFLARE_EMAIL_API_TOKEN` in Infisical.
- Section D: Apply migrations 0003 + 0003b + 0004 to prod Neon; `fly deploy`; `/healthz` green-check (now including `r2Ready`).
- Section E: Run 11-step INTEG-01 protocol; fill VERIFICATION.md.
- Section F (optional): `/rrr:audit-milestone` + `/rrr:complete-milestone`.
- Post-E: SEC-ROTATE-CF-EMAIL-01 (rotate CF Email API token after Section E passes).

### Blockers/Concerns

- `migrate.mjs` has a latent double-insert bug into `schema_migrations` when migrations self-insert. Phase 04 executor noted this but didn't fix (out of scope). Flag for a follow-up hygiene plan if it bites during D1.
- Mastra is young (`@mastra/core@1.35.0` pinned). 20-concurrent inbound load spike test deferred from week 1 of v1.2 to a post-deploy canary in Phase 06 territory.
- Tracked but not formally in v1.2: SEC-ROTATE-01 (Clerk key rotation) — backlog in REQUIREMENTS.md; instructions in USER-ACTIONS.md Section A2.
- New 2026-05-16: SEC-ROTATE-CF-EMAIL-01 (Cloudflare Email Service token rotation) — backlog in REQUIREMENTS.md; do after Section E smoke-test passes.
- New 2026-05-16 (tear-out): SEC-ROTATE-CF-AI-01 (Cloudflare Workers AI token rotation) — backlog in REQUIREMENTS.md; do after the next post-launch verification pass.
- New 2026-05-19 (Phase 12 Wave 1): SEC-ROTATE-CF-AI-PARROT-01 — rotate `CLOUDFLARE_AI_API_TOKEN` used by the Parrot Worker after Phase 12 Wave 2 production deploy lands. Same posture as SEC-ROTATE-CF-AI-01 (student app token). Store rotated value in Infisical `/internjobs-ai/CLOUDFLARE_AI_API_TOKEN` and re-run `wrangler secret put`.
- New 2026-05-19 (Phase 12 Wave 2): MM-USER-ACTION-01 — create a Mattermost bot account in System Console → Integrations → Bot Accounts; grant System Admin role; copy the bot's personal access token; run `wrangler secret put MATTERMOST_BOT_TOKEN` on the `internjobs-parrot` Worker. Without this secret, `pollMattermostNewPosts()` skips silently — alarm runs but produces zero chat-sourced todos. Store the rotated value in Infisical `/internjobs-ai/MATTERMOST_BOT_TOKEN`.

## Session Continuity

Last session: 2026-05-19 — **PHASE-12 COMPLETE** (all 3 waves landed). Wave 3 (12-03 Dashboard Mothership Agent UI + Ranking) shipped via /rrr:execute-plan in 2m 32s with 2 atomic commits (8e71b18 + 5596b9b). New `apps/parrot/app/components/TodoCard.tsx` (presentation-only card with source-icon badge + urgency dot + age/deadline chips + @mention chip + onSelect callback hoist) + rebuilt `apps/parrot/app/routes/dashboard.tsx` (four-state UI: loading skeleton + error card + view-aware empty state + ranked TodoCard list; useEffect fetch with cancelled-flag cleanup; ?view= query-param wiring; click-through routing to /inbox?message={source_id} for email and /chat for Mattermost) + new `POST /api/dev/smoke/ranking` Hono route (PARROT_DEV_MODE-gated, uses debugInsertTodo to insert two todos with explicit urgency scores 80+20, asserts hi.rank > lo.rank; 100% deterministic — no LLM required, no PARROT_AI_GATEWAY_ID required). Skills-referenced "Workers AI via AI Gateway" comment block landed in 3 files. LLM-URL audit clean. TypeScript clean. Zero new npm packages. Zero deviations. Earlier same day — PHASE-12 Wave 2 (12-02) Dashboard Mothership Agent ingest + extraction shipped (commits 611ba42 + b502609 in 3m 5s). Earlier same day — PHASE-12 Wave 1 (12-01) Dashboard Mothership Agent foundation shipped (commits 5fe02a9 + 7ef33cc + e4e2ed4 in 6m 23s).
Stopped at: Phase 12 COMPLETE (all 3 waves: 12-01, 12-02, 12-03 fully executed on main). Next: Phase 13 (Cross-pane Actions + Launch Polish) — unblocked, planner can proceed. Phase 13 builds on the Phase 12 surface: dashboard pane is now a real consumer of EmployeeMailboxDO.getTodos(), TodoCard's onSelect callback is the seam for context-menu actions (resolve/snooze/escalate), and the ranking regression endpoint is the gate for any future hybrid-rank SQL tweaks. Production deploy of Phase 12 blocked on carryover user actions: (1) CF AI Gateway provisioning + three Worker secrets (PARROT_AI_GATEWAY_ID, CLOUDFLARE_AI_API_TOKEN, CLOUDFLARE_ACCOUNT_ID); (2) Mattermost bot account + MATTERMOST_BOT_TOKEN secret. Both code paths fail-soft when secrets are unset.
Resume file: .planning/milestones/v1.2-two-sided-agent-mvp/ROADMAP.md (Phase 13 planning is next)
