# 01-01 Plan: Move Marketing Into Workspace

## Goal

Move the existing public site into `apps/marketing` without changing the rendered marketing experience.

## Must Haves

- Current Vite app source lives under `apps/marketing`.
- Marketing package keeps its own Vite, Tailwind, TypeScript, Wrangler, public assets, and verification script.
- Root scripts delegate to the marketing workspace.
- `npm run build:marketing` succeeds.
- `npm run verify:marketing:dist` succeeds.

## Verification

- Run `npm run build:marketing`.
- Run `npm run verify:marketing:dist`.
- Capture Playwright screenshots for `/` and `/startups`.

## Status

Complete on 2026-05-09.
