# 01-02 Plan: Add Fly App Shell

## Goal

Create a separate Fly.io app workspace for `app.internjobs.ai`.

## Must Haves

- `apps/app` has an independent package.
- App exposes `/healthz`.
- App has `Dockerfile` and `fly.toml`.
- Root scripts can build and verify the app.
- Shared contracts have a home in `packages/shared`.

## Verification

- Run `npm run build:app`.
- Confirm `/healthz` returns `{ "ok": true, "service": "internjobs-app" }`.

## Status

Complete on 2026-05-09.
