---
phase: 33-startups-web-app-golive
plan: 07
type: execute
wave: 2
depends_on: ["33-06"]
files_modified:
  - .planning/milestones/v1.5/phases/33-startups-web-app-golive/33-07-EVIDENCE.md
autonomous: false
skills:
  - anthropic.webapp-testing
skills_mode: normal

verification:
  surface: ui_affecting
  frontend_impact: true
  required_steps:
    - chrome_visual_check

must_haves:
  truths:
    - "nithin@growthpods.io (a real founder whose startup_members row was created with a concierge:% placeholder before this fix) refreshes employers.internjobs.ai/dashboard and sees 'Phase33 Test Co' + phase33-test-co@employers.internjobs.ai instead of 'account not linked'"
    - "The DB row proves exactly one flip happened: startup_members.clerk_user_id for this founder is now a real user_... id (not concierge:%), and updated_at matches the time of the sign-in in the checkpoint"
    - "A second dashboard refresh (or sign-out/sign-in) by the same founder still works and does not re-trigger the lazy-link path (idempotent in production, not just in the 33-06 unit tests)"
  artifacts:
    - path: .planning/milestones/v1.5/phases/33-startups-web-app-golive/33-07-EVIDENCE.md
      provides: "Pass/fail table + captured curl/psql/screenshot evidence for the 3 truths above, closing out the Phase 33 security + onboarding gap-fix"
      min_lines: 30
  key_links: []
---

<objective>
Prove, live, that Plan 33-06's fix actually resolves the exact production defect that was found by hand: the founder `nithin@growthpods.io` (startup "Phase33 Test Co", `phase33-test-co@employers.internjobs.ai`) signed up successfully but was stuck on "account not linked" because their `startup_members` row still had a `concierge:0bb7db0bf8d8ccea7555c7cd878cc2c4` placeholder `clerk_user_id`.

33-06 already proved the fix works in isolation (8 unit tests + live Fly smoke of the guarded endpoint + a live forged-JWT 401 against production). What's left is the thing no unit test or curl command can prove: does this SPECIFIC real founder, in a SPECIFIC real browser, actually reach their dashboard now? That requires a human driving a real Clerk session — the same reason 33-05 split its browser-driven checks into a `checkpoint:human-verify` task instead of automating them.

Purpose: close out the live-testing-surfaced gap that spawned 33-06/33-07, with the same standard of proof (real production, not staging, not mocks) the rest of Phase 33 held itself to.
Output: `33-07-EVIDENCE.md` recording pass/fail for all 3 must-have truths, with DB query output and the human's confirmation.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.5/phases/33-startups-web-app-golive/33-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint DB snapshot</name>
  <files></files>
  <action>
Before the human refreshes the dashboard, capture the founder row's CURRENT state so the post-checkpoint comparison is a real before/after, not just a single observation. Use the established psql-via-fly-ssh pattern from this phase (see 33-05):

```bash
fly ssh console -a internjobs-startup-api --command \
  "psql \$DATABASE_URL -c \"select id, startup_id, clerk_user_id, role, email, name, updated_at from startup_members where lower(email) = lower('nithin@growthpods.io')\""
```

Record the output. Expect `clerk_user_id` to still show the placeholder `concierge:0bb7db0bf8d8ccea7555c7cd878cc2c4` (or whatever placeholder value is currently live — confirm it still starts with `concierge:` before proceeding; if it has already been flipped by some other test run, note that in the evidence file and treat Task 2's "still shows account not linked" expectation as N/A, focusing only on confirming the row stays correctly linked).
  </action>
  <verify>A row is returned for nithin@growthpods.io with a `concierge:%`-prefixed clerk_user_id (or a documented note if already linked).</verify>
  <done>Pre-checkpoint DB state captured and recorded for the before/after comparison.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 33-06 (deployed): cryptographic JWT verification (RS256/JWKS) on every `/api/*` route in `apps/employers`, and a lazy-link fallback that flips a founder's `concierge:%` placeholder to their real Clerk id on first authenticated sign-in, guarded so an already-linked account can never be re-pointed. Already proven live: forged JWTs are rejected (401), and the guarded Fly endpoint's happy-path + takeover-guard both pass against production via `smoke.mjs`. What's NOT yet proven is the actual end-user outcome this was all for: does `nithin@growthpods.io` now reach their real dashboard.
  </what-built>
  <how-to-verify>
**1. Sign in and refresh the dashboard:**
Go to `https://employers.internjobs.ai` and sign in as `nithin@growthpods.io` (or use an already-active session — the important thing is a FRESH request, so hard-refresh `/dashboard` after signing in, or sign out and back in if already signed in from before this deploy).

Confirm the dashboard shows:
- Startup name: **"Phase33 Test Co"**
- Agent email: **phase33-test-co@employers.internjobs.ai** (in the "your agent" card)
- NOT the "account not linked" screen.

**2. Idempotency check — refresh again:**
Hard-refresh `/dashboard` a second time (or sign out and sign back in). Confirm it STILL shows the same startup data, loads without error, and doesn't flash the "account not linked" screen even momentarily.

Reply with the outcome (pass/fail for each of the two checks above), plus a screenshot if anything looks wrong.
  </how-to-verify>
  <resume-signal>Reply "approved" (or describe what you saw) once you've confirmed both the dashboard shows Phase33 Test Co's data and a second refresh is still correct.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-checkpoint DB proof + write evidence</name>
  <files>.planning/milestones/v1.5/phases/33-startups-web-app-golive/33-07-EVIDENCE.md</files>
  <action>
Re-run the same query from Task 1:

```bash
fly ssh console -a internjobs-startup-api --command \
  "psql \$DATABASE_URL -c \"select id, startup_id, clerk_user_id, role, email, name, updated_at from startup_members where lower(email) = lower('nithin@growthpods.io')\""
```

Confirm:
- `clerk_user_id` now starts with `user_` (real Clerk id), NOT `concierge:`.
- `updated_at` is recent (matches the timeframe of the human's checkpoint sign-in).

Then run it a THIRD time (simulating the "second refresh" the human just did from a server-side angle) to confirm `clerk_user_id` and `updated_at` are UNCHANGED from the second query — this is the idempotency proof at the DB layer, complementing the human's UI-level idempotency check.

Write `.planning/milestones/v1.5/phases/33-startups-web-app-golive/33-07-EVIDENCE.md` (mirror the style of `33-05`'s `EVIDENCE.md`: a status line, then a pass/fail table for the 3 `must_haves.truths` in this plan's frontmatter, each row citing the exact command output / the human's checkpoint reply). Include:

- The Task 1 (before) and Task 3 (after) psql output side by side.
- The human's exact checkpoint reply (both checks).
- A closing status line: `Phase 33 security + onboarding gap-fix (33-06/33-07): VERIFIED LIVE` (only if all 3 truths passed) or `PARTIAL — see gaps below` with a bulleted list of what's still open. If PARTIAL, note that `/rrr:plan-phase 33 --gaps` is the correct next step.
  </action>
  <verify>33-07-EVIDENCE.md exists, all 3 truths have a pass/fail entry with cited before/after DB output and the human's reply — no "TODO" placeholders.</verify>
  <done>33-07-EVIDENCE.md committed with a full pass/fail record; the founder's clerk_user_id is confirmed flipped from concierge:% to a real user_... id, stable across a second refresh.</done>
</task>

</tasks>

<verification>
- 33-07-EVIDENCE.md exists with no unresolved TODOs.
- If any truth failed, EVIDENCE.md explicitly flags it as a gap (do not silently mark the phase complete) — a follow-up gap-closure plan (`/rrr:plan-phase 33 --gaps`) is the correct next step for any failure surfaced here.
</verification>

<success_criteria>
- The real founder who originally surfaced this bug reaches their real dashboard in production.
- The DB proves the flip happened exactly once and is stable across repeat sign-ins.
- Phase 33's last blocking gap (auth bypass + broken onboarding) is closed with the same live-evidence standard as the rest of the phase.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.5/phases/33-startups-web-app-golive/33-07-SUMMARY.md`
</output>
