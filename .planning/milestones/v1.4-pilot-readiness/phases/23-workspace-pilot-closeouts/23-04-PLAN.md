---
phase: 23-workspace-pilot-closeouts
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/parrot/test/agent-uat-results.md
autonomous: false

verification:
  surface: ui_affecting
  frontend_impact: true
  required_steps:
    - playwright
    - chrome_visual_check

skills:
  - projecta.testing-vitest-playwright
  - projecta.visual-proof
  - anthropic.webapp-testing

must_haves:
  truths:
    - "All 14 steps in V1_3_1-AGENT-LIFT-REPORT.md pass with a fresh Workspace Clerk OTP session"
    - "AgentPanel quick actions (summarize, draft, translate) return live LLM results in production within 10s"
    - "MCPPanel lists all 11 internal Workspace MCP tools (list_emails, get_email, get_thread, search_emails, draft_reply, draft_email, mark_email_read, move_email, discard_draft, send_reply, send_email)"
    - "At least 3 of the 11 MCP tool calls return non-error responses"
    - "The 11 Workspace MCP tools are the INTERNAL tools from PARROT_AGENT_TOOLS — NOT the 4-tool startup-MCP at mcp.internjobs.ai"
  artifacts:
    - path: "apps/parrot/test/agent-uat-results.md"
      provides: "14-step UAT record with pass/fail per step and LLM latency measurements"
      min_lines: 60
  key_links:
    - from: "apps/parrot/app/components/EmailPanel.tsx (AgentPanel section)"
      to: "GET /api/inbox/agent/tools"
      via: "MCPPanel fetches tool catalog on mount"
      pattern: "tools"
    - from: "AgentPanel quick-action buttons"
      to: "POST /api/inbox/agent/summarize + /draft-reply + /translate"
      via: "user click → fetch → LLM via CF AI Gateway → kimi-k2.6"
      pattern: "summarize|draft-reply|translate"
---

<objective>
Run the 14-step authenticated UAT for agent-lift features and document results.

Purpose: The v1.3.1 Agent Lift shipped AgentPanel (summarize/draft/translate/chat/extract-actions) and MCPPanel (11 tools via GET /api/inbox/agent/tools). AGENT-UAT-01..03 require that ALL 14 steps pass in production with a fresh Clerk OTP session, AgentPanel quick actions return live LLM results within 10s, and MCPPanel lists all 11 internal Workspace MCP tools with >= 3 non-error tool calls.

CRITICAL DISAMBIGUATION: The 11 tools tested here are the INTERNAL Workspace MCP tools (PARROT_AGENT_TOOLS in apps/parrot/workers/lib/agent-tools.ts) — list_emails, get_email, get_thread, search_emails, draft_reply, draft_email, mark_email_read, move_email, discard_draft, send_reply, send_email. These are NOT the 4-tool Stainless-style startup-MCP at mcp.internjobs.ai (search, execute, me, discover_actions) shipped in Phase 28. Two separate servers, two separate purposes.

Output: apps/parrot/test/agent-uat-results.md documenting all 14 UAT steps with PASS/FAIL, latency measurements, and AGENT-UAT-01..03 sign-off.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/workstreams/team-workspace/STATE.md
@.planning/milestones/v1.3-pilot-hardening/phases/19-todo-auto-resolution/V1_3_1-AGENT-LIFT-REPORT.md
@apps/parrot/workers/routes/agent.ts
@apps/parrot/workers/lib/agent-tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the 14-step UAT test script</name>
  <files>apps/parrot/test/agent-uat-results.md</files>
  <action>
    Create `apps/parrot/test/agent-uat-results.md` as a structured UAT script. Leave all result fields as `[ ] PENDING` — they will be filled in during the human checkpoint (Task 2).

    The 14 steps below are derived from the V1_3_1-AGENT-LIFT-REPORT.md feature inventory and AGENT-UAT-01..03 requirements. The executor must read V1_3_1-AGENT-LIFT-REPORT.md to confirm these are the correct 14 steps; if the report specifies a different set, use the report's authoritative list.

    Standard 14 steps for v1.3.1 agent-lift UAT (adapt from report if needed):

    1. OTP auth session — sign in to workspace.internjobs.ai via phone-OTP; land on dashboard
    2. Inbox navigation — navigate to Inbox; email list renders with unread count badge
    3. Email open — click an email; EmailPanel renders body (not blank screen)
    4. EmailIframe sandbox — email body renders in sandbox iframe; no script errors in console
    5. Agent panel visible — AgentPanel appears as a side pane (or toggle button) next to the email
    6. Summarize quick action — click Summarize; LLM response appears within 10s; no 503 error
    7. Draft reply quick action — click Draft Reply; LLM draft appears within 10s; no 503 error
    8. Translate quick action — click Translate (default to English or select Spanish); translation appears within 10s
    9. Extract actions — click Extract Actions; bulleted action list appears (or "NONE" for no-action emails)
    10. Chat — type a freeform question in the agent chat box; agent replies within 10s
    11. MCPPanel visible — MCPPanel (or Tools tab) is reachable from the email view
    12. Tool catalog — MCPPanel lists exactly 11 tools: list_emails, get_email, get_thread, search_emails, draft_reply, draft_email, mark_email_read, move_email, discard_draft, send_reply, send_email
    13. Tool call (3 non-error) — invoke at least 3 tools (e.g., list_emails, get_email, search_emails); each returns a non-error JSON response
    14. Resolved view — navigate to Resolved folder; any todos resolved via 23-01's closeTodoFact appear (or confirm empty if no resolutions yet)

    File template:

    ```markdown
    # Agent-Lift UAT Results — AGENT-UAT-01..03

    **Date:** 2026-05-25
    **Session:** Fresh Clerk OTP phone sign-in (workspace.internjobs.ai)
    **Target:** Production (workspace.internjobs.ai)
    **Scope:**
    - AGENT-UAT-01: All 14 steps pass
    - AGENT-UAT-02: AgentPanel quick actions (summarize/draft/translate) return live LLM results within 10s
    - AGENT-UAT-03: MCPPanel lists all 11 internal Workspace MCP tools; >=3 tool calls return non-error

    **IMPORTANT — Server disambiguation:**
    The 11 tools tested here are the INTERNAL Workspace MCP tools (PARROT_AGENT_TOOLS in
    apps/parrot/workers/lib/agent-tools.ts). These are served by the Parrot Worker at
    workspace.internjobs.ai/api/inbox/agent/tools.
    These are NOT the 4-tool startup-MCP at mcp.internjobs.ai (Phase 28, different server).

    ---

    ## 14-Step UAT

    | Step | Description | Expected | Result | Notes |
    |------|-------------|----------|--------|-------|
    | 1 | OTP auth session | Dashboard loads after phone-OTP sign-in | [ ] PENDING | |
    | 2 | Inbox navigation | Email list renders with count badge | [ ] PENDING | |
    | 3 | Email open | EmailPanel renders email body | [ ] PENDING | |
    | 4 | EmailIframe sandbox | Iframe renders; console clean | [ ] PENDING | |
    | 5 | Agent panel visible | AgentPanel/toggle present | [ ] PENDING | |
    | 6 | Summarize (<10s) | LLM summary in <10s, no 503 | [ ] PENDING | latency: __ s |
    | 7 | Draft reply (<10s) | LLM draft in <10s, no 503 | [ ] PENDING | latency: __ s |
    | 8 | Translate (<10s) | Translation in <10s, no 503 | [ ] PENDING | latency: __ s |
    | 9 | Extract actions | Bulleted list or "NONE" | [ ] PENDING | |
    | 10 | Chat | Agent reply in <10s | [ ] PENDING | latency: __ s |
    | 11 | MCPPanel visible | Tools panel/tab reachable | [ ] PENDING | |
    | 12 | Tool catalog (11 tools) | All 11 PARROT_AGENT_TOOLS listed | [ ] PENDING | |
    | 13 | Tool calls (>=3 non-error) | 3+ tools return non-error JSON | [ ] PENDING | tools tested: |
    | 14 | Resolved view | Resolved folder accessible | [ ] PENDING | |

    ---

    ## AGENT-UAT-01 Sign-off

    All 14 steps: [ ] PASS / [ ] FAIL (N steps failed — see Notes column)

    ## AGENT-UAT-02 Sign-off

    Summarize latency: __ s (target: <10s)
    Draft reply latency: __ s (target: <10s)
    Translate latency: __ s (target: <10s)
    Status: [ ] PASS / [ ] FAIL

    ## AGENT-UAT-03 Sign-off

    Tools listed: __ / 11
    Tool call results:
    - list_emails: [ ] non-error / [ ] error
    - get_email: [ ] non-error / [ ] error
    - search_emails: [ ] non-error / [ ] error
    Additional tools called: (list any others)
    Status: [ ] PASS / [ ] FAIL (need >=3 non-error)

    ---

    ## Issues Found

    (none — to be filled in during checkpoint)

    ## Deferred / Out of Scope

    - MCPPanel tool EXECUTION via external MCP client (e.g., Claude Desktop) — this UAT tests
      the HTTP endpoint directly via the AgentPanel UI; full MCP protocol testing is v1.5.
    - Startup-MCP at mcp.internjobs.ai (Phase 28) — separate server, out of scope for this UAT.
    ```

    Create this file with the template above. The PENDING fields will be filled in by the human in Task 2.
  </action>
  <verify>
    ls -la apps/parrot/test/agent-uat-results.md
    wc -l apps/parrot/test/agent-uat-results.md
    grep -c "PENDING" apps/parrot/test/agent-uat-results.md
  </verify>
  <done>
    apps/parrot/test/agent-uat-results.md exists, >= 60 lines, contains the 14-step table with all results as PENDING, and the three AGENT-UAT-01..03 sign-off sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    UAT test script in apps/parrot/test/agent-uat-results.md with 14 steps.

    Now walk through all 14 steps in production using a fresh Workspace Clerk phone-OTP session. Record results directly in the table.
  </what-built>
  <how-to-verify>
    Pre-condition: Make sure the latest Parrot Worker is deployed (plans 23-01, 23-02, 23-03 may have added new deploy steps — run `cd apps/parrot && wrangler deploy` if any of those were executed first).

    STEP-BY-STEP:

    Step 1 — OTP auth:
    Open an incognito window → navigate to https://workspace.internjobs.ai → sign in with phone OTP → confirm dashboard loads. PASS if dashboard renders without errors.

    Step 2 — Inbox navigation:
    Click Inbox in the sidebar → email list loads. PASS if emails appear (or empty state for empty inbox) without a 500 error.

    Step 3 — Email open:
    Click any email → EmailPanel renders the email body. PASS if body text/HTML is visible.

    Step 4 — EmailIframe sandbox:
    Open browser DevTools → Console tab → reload the email view → confirm no "Refused to execute script" or CSP violation errors. PASS if console is clean.

    Step 5 — Agent panel visible:
    Confirm that an AgentPanel section (or "Agent" tab / "AI" button) appears alongside the email. PASS if visible.

    Steps 6-8 — Quick actions (time each with browser DevTools Network tab):
    - Click Summarize → record time-to-response → confirm text appears (not a spinner forever or 503)
    - Click Draft Reply → record time → confirm draft text appears
    - Click Translate → select a language → record time → confirm translation appears
    PASS if all three return results within 10 seconds. FAIL if any returns 503 or times out.

    Step 9 — Extract actions:
    Click "Extract Actions" (or equivalent) → confirm bulleted list or "NONE" message appears. PASS.

    Step 10 — Chat:
    Type "What does this email want me to do?" in the agent chat input → send → confirm agent reply within 10s. PASS.

    Step 11 — MCPPanel:
    Find the MCPPanel (it may be a "Tools" tab in the AgentPanel sidebar, or a separate panel). Click it. PASS if it loads without 404.

    Step 12 — Tool catalog (11 tools):
    In MCPPanel, count the listed tools. Confirm the list contains EXACTLY these 11:
    list_emails, get_email, get_thread, search_emails, draft_reply, draft_email,
    mark_email_read, move_email, discard_draft, send_reply, send_email

    Note: These come from GET /api/inbox/agent/tools which returns PARROT_AGENT_TOOLS.
    PASS if all 11 are listed. FAIL if count differs.

    Step 13 — Tool calls (at least 3 non-error):
    Using the MCPPanel UI (or directly via curl from a terminal), invoke at least 3 tools:

    Option A — via MCPPanel UI click-to-invoke buttons (if they exist)
    Option B — via curl (authenticated):
    First get your Clerk session token from DevTools (Application > Cookies > __session), then:

    ```bash
    TOKEN="<your __session cookie value>"
    BASE="https://workspace.internjobs.ai"

    # Tool 1: list_emails
    curl -s -X GET "$BASE/api/inbox/messages?folder=INBOX&limit=5" \
      -H "Authorization: Bearer $TOKEN" | head -c 200

    # Tool 2: search_emails
    curl -s -X GET "$BASE/api/inbox/search?q=hello&limit=5" \
      -H "Authorization: Bearer $TOKEN" | head -c 200

    # Tool 3: get_email (use a real message ID from list_emails output)
    curl -s -X GET "$BASE/api/inbox/messages/{messageId}" \
      -H "Authorization: Bearer $TOKEN" | head -c 200
    ```

    PASS if >= 3 tool calls return JSON without `{"error": ...}` at the top level.

    Step 14 — Resolved view:
    Navigate to the Resolved folder in the Workspace inbox. PASS if the view loads (even if empty — "no resolved todos yet" is acceptable for AGENT-UAT-01).

    After completing all 14 steps, fill in the UAT results table in apps/parrot/test/agent-uat-results.md, including:
    - PASS or FAIL for each step
    - Latency for steps 6-8 and 10 (in seconds)
    - Specific tools tested in step 13
    - Any issues or deviations found

  </how-to-verify>
  <resume-signal>
    Paste the completed UAT summary:
    - Steps passed: X / 14
    - Steps failed: (list which ones and why)
    - AGENT-UAT-01 (all 14 steps): PASS / FAIL
    - AGENT-UAT-02 (LLM latency < 10s): PASS / FAIL (include actual latencies)
    - AGENT-UAT-03 (11 tools listed, >= 3 non-error calls): PASS / FAIL
    Then type "UAT complete" or describe issues for auto-fix.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize UAT record and fix any blockers found in checkpoint</name>
  <files>apps/parrot/test/agent-uat-results.md</files>
  <action>
    Using the resume-signal from Task 2 (the completed UAT results), update `apps/parrot/test/agent-uat-results.md`:

    1. Replace all `[ ] PENDING` entries with `[x] PASS` or `[ ] FAIL` per step
    2. Fill in the latency measurements for steps 6-8 and 10
    3. Fill in the tool calls section (which tools were tested, which returned non-error)
    4. Update the three AGENT-UAT sign-off sections with final PASS/FAIL status
    5. Fill in the "Issues Found" section with any failures

    IF any of these blockers were found:
    - AGENT-UAT-02 FAIL: Quick actions returning 503 → diagnose: is CF AI Gateway binding configured in wrangler.jsonc? Is KIMI_MODEL set? Run `wrangler tail` during a test call and check the error. Common fix: ensure `CLOUDFLARE_AI_API_TOKEN` and `PARROT_AI_GATEWAY_ID` are set as Worker secrets.
    - AGENT-UAT-03 FAIL (tool count != 11): Check GET /api/inbox/agent/tools returns 200 with 11 entries. If it returns 404, the agentRoutes may not be mounted correctly in index.ts.
    - AGENT-UAT-03 FAIL (tool calls error): Check the specific error JSON. If 401, the session token isn't being passed correctly. If 500, check wrangler tail for the specific error.

    For any auto-fixable issue (missing env secret docs, route mount, etc.), fix the relevant file AND update the UAT record to reflect the post-fix result.

    If a blocker is NOT auto-fixable (requires dashboard config, secret rotation, etc.), document it clearly in the "Issues Found" section and mark the step as FAIL with a "deferred to human action" note.

    The final state of apps/parrot/test/agent-uat-results.md must have no `PENDING` fields — every step must be PASS or FAIL with notes.
  </action>
  <verify>
    grep -c "PENDING" apps/parrot/test/agent-uat-results.md
    grep -c "PASS\|FAIL" apps/parrot/test/agent-uat-results.md
    wc -l apps/parrot/test/agent-uat-results.md
  </verify>
  <done>
    - apps/parrot/test/agent-uat-results.md has zero PENDING entries
    - All 14 steps are marked PASS or FAIL with notes
    - AGENT-UAT-01, AGENT-UAT-02, AGENT-UAT-03 sign-off sections are complete
    - Any issues found are documented with root cause and fix status
    - File is >= 60 lines
  </done>
</task>

</tasks>

<verification>
After all tasks and checkpoint:

1. `ls -la apps/parrot/test/agent-uat-results.md` — file exists
2. `wc -l apps/parrot/test/agent-uat-results.md` — >= 60 lines
3. `grep -c "PENDING" apps/parrot/test/agent-uat-results.md` — should be 0
4. `grep -c "\[x\] PASS\|PASS" apps/parrot/test/agent-uat-results.md` — >= 11 PASSes (14 steps minus acceptable deferrals)
5. `grep "AGENT-UAT-01\|AGENT-UAT-02\|AGENT-UAT-03" apps/parrot/test/agent-uat-results.md` — all three sign-offs present

Manual confirmation:
- AgentPanel summarize/draft/translate: LLM results within 10s in production
- MCPPanel: 11 Workspace MCP tools listed (PARROT_AGENT_TOOLS — NOT startup-MCP)
- At least 3 tool calls return non-error JSON responses
</verification>

<success_criteria>
- AGENT-UAT-01: All 14 steps in V1_3_1-AGENT-LIFT-REPORT.md pass with a fresh Workspace Clerk OTP session (or documented deferrals with rationale)
- AGENT-UAT-02: AgentPanel quick actions (summarize, draft, translate) return live LLM results in production within 10s
- AGENT-UAT-03: MCPPanel lists all 11 internal Workspace MCP tools (PARROT_AGENT_TOOLS from agent-tools.ts) and >= 3 tool calls return non-error responses
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/23-workspace-pilot-closeouts/23-04-SUMMARY.md`
</output>
