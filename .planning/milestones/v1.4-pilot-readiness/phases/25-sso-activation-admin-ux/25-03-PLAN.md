---
phase: 25-sso-activation-admin-ux
plan: "25-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/parrot/package.json
  - apps/parrot/package-lock.json
autonomous: true
ui_affecting: false
coverage_diagram_not_applicable: true
coverage_diagram_skip_reason: "dependency removal only — no code paths"
verification:
  surface: backend_only
  frontend_impact: false
  required_steps:
    - unit_tests

must_haves:
  truths:
    - "@neondatabase/serverless is absent from apps/parrot/package.json dependencies"
    - "apps/parrot/package-lock.json no longer contains @neondatabase/serverless entries"
    - "grep -r @neondatabase apps/parrot/workers/ returns zero results"
    - "npm run build in apps/parrot/ passes without errors"
  artifacts:
    - path: "apps/parrot/package.json"
      provides: "Clean dependency list without @neondatabase/serverless"
    - path: "apps/parrot/package-lock.json"
      provides: "Lock file reflecting npm uninstall result"
  key_links:
    - from: "apps/parrot/package.json"
      to: "apps/parrot/workers/**"
      via: "npm run build (react-router build) — must succeed after removal"
      pattern: "neondatabase"
---

<objective>
Remove the orphan @neondatabase/serverless dependency from apps/parrot.

Purpose: The Neon-exit (v1.3, un-roadmapped 2026-05-21) migrated all student-app database access from Neon to Fly Postgres. The Workspace Worker (apps/parrot) never used @neondatabase/serverless in its worker source code — confirmed by grep finding zero import statements in apps/parrot/workers/**. The dep is a dangling leftover in package.json from an earlier integration attempt.

This plan removes it cleanly via npm uninstall, verifies the build still passes, and confirms no grep hits in worker source.

Reqs closed: NEONEX-DEP-01.

TEAM MODE NOTE: This plan runs on branch rrr/v1.4/team-workspace-25. Do NOT touch root .planning/STATE.md or .planning/team-mode.json. All state writes go to .planning/workstreams/team-workspace/STATE.md only.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@apps/parrot/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @neondatabase/serverless and verify build</name>
  <files>apps/parrot/package.json
apps/parrot/package-lock.json</files>
  <action>
Run these commands in sequence from the repo root. Each command must succeed before the next runs.

Step 1 — Confirm the dep is present (informational):
  cd apps/parrot && grep "@neondatabase" package.json

Step 2 — Confirm zero source imports (pre-condition check):
  grep -r "@neondatabase" workers/ 2>/dev/null || echo "ZERO HITS — safe to remove"

Step 3 — Uninstall:
  npm uninstall @neondatabase/serverless

This updates both package.json and package-lock.json atomically.

Step 4 — Post-removal grep to confirm clean:
  grep -r "@neondatabase" workers/ 2>/dev/null && echo "STILL PRESENT — investigate" || echo "CONFIRMED ZERO"
  grep "@neondatabase" package.json 2>/dev/null && echo "STILL IN PACKAGE.JSON — investigate" || echo "REMOVED FROM PACKAGE.JSON"

Step 5 — Build:
  npm run build

Build must exit 0. If the build fails, read the error output and determine if it is related to the removal (unexpected import of @neondatabase somewhere). If an import is found in a non-workers file (e.g. app/ route), remove that import rather than re-adding the dep.

Step 6 — Check package-lock.json (informational):
  grep -c "@neondatabase" package-lock.json && echo "ENTRIES REMAIN IN LOCK FILE" || echo "LOCK FILE CLEAN"

A small count in package-lock.json is acceptable if it appears in the nested dependency graph of a remaining package (not as a direct dep); the critical check is zero hits in workers/ source and absence from package.json dependencies.
  </action>
  <verify>
Confirm all 4 of these conditions are true:
1. `grep "@neondatabase" apps/parrot/package.json` returns no output (exit 1)
2. `grep -r "@neondatabase" apps/parrot/workers/` returns no output (exit 1)
3. `npm run build` in apps/parrot exits 0
4. `apps/parrot/package-lock.json` no longer has @neondatabase/serverless as a direct package entry (nested transitive OK; verify by checking the lock file's top-level packages section)
  </verify>
  <done>
@neondatabase/serverless is absent from package.json and has zero source imports in workers/. npm run build passes. NEONEX-DEP-01 closed.
  </done>
</task>

</tasks>

<verification>
NEONEX-DEP-01:
- `grep "@neondatabase/serverless" apps/parrot/package.json` → no match
- `grep -r "@neondatabase" apps/parrot/workers/` → no match (zero results)
- `cd apps/parrot && npm run build` → exit 0, no errors
</verification>

<success_criteria>
- @neondatabase/serverless removed from apps/parrot/package.json dependencies block
- apps/parrot/package-lock.json updated (npm uninstall ran successfully)
- npm run build in apps/parrot/ passes without error
- grep -r @neondatabase apps/parrot/workers/ returns zero results
- package.json + package-lock.json committed to branch rrr/v1.4/team-workspace-25
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/25-sso-activation-admin-ux/25-03-SUMMARY.md` using the summary template at @~/.claude/rrr/templates/summary.md.
Update `.planning/workstreams/team-workspace/STATE.md` (NOT root STATE.md) with plan 25-03 complete and Phase 25 all 3 plans closed.
</output>
