---
phase: 30-parrot-email-pane-parity
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/parrot/app/components/AgentPanel.tsx
autonomous: true
coverage_diagram_not_applicable: true
# Reason: Phase is wiring-only on existing proven infrastructure; paths covered by explicit verify sections.
skills:
  - projecta.testing-vitest-playwright
  - projecta.visual-proof
verification:
  surface: ui_affecting
  frontend_impact: true
  required_steps:
    - unit_tests
    - playwright
    - chrome_visual_check

must_haves:
  truths:
    - "AgentPanel header shows two distinct segmented tab buttons: 'Agent' (left) and 'MCP' (right)"
    - "Clicking 'Agent' tab shows the chat interface and quick-action bar"
    - "Clicking 'MCP' tab shows the MCPPanel tool catalog (same behavior as previous 'Tools' toggle)"
    - "The word 'Tools' no longer appears as a button label in the AgentPanel header"
    - "Active tab is visually distinguished (indigo background/border) from the inactive one"
  artifacts:
    - path: "apps/parrot/app/components/AgentPanel.tsx"
      provides: "Two segmented tabs replacing the single toggle button"
      contains: "Agent"
  key_links:
    - from: "AgentPanel tab state"
      to: "MCPPanel render"
      via: "tab === 'tools' conditional (~line 314)"
      pattern: "tab.*tools"
---

<objective>
Promote the existing single "Tools" toggle in AgentPanel into a proper two-tab segmented control: "Agent" (the chat + quick-actions panel) and "MCP" (the MCPPanel tool catalog).

Purpose: The current affordance is a single toggle button that switches between modes; this phase adds a proper tab bar so both modes are discoverable simultaneously, matching the agentic-inbox reference design's Agent|MCP header.
Output: A segmented tab bar in AgentPanel header with Agent and MCP tabs; no new functionality, only a layout + labeling change.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.4-pilot-readiness/phases/30-parrot-email-pane-parity/30-CONTEXT.md

@apps/parrot/app/components/AgentPanel.tsx
@apps/parrot/app/components/MCPPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the single "Tools" toggle with a segmented Agent | MCP tab control in AgentPanel header</name>
  <files>apps/parrot/app/components/AgentPanel.tsx</files>
  <action>
The current header (~line 279-311) has:
- A "Parrot Agent" title on the left
- A single toggle button labeled "Tools" (with Wrench icon) on the right that flips `tab` between "chat" and "tools"
- A close (X) button

Replace that single Tools button with a two-button segmented tab control. The tab state is already `useState<"chat" | "tools">("chat")` (~line 96) — keep it as-is; only the rendering changes.

The new header right-side section:

```tsx
<div className="flex items-center gap-1">
  {/* Segmented tab: Agent */}
  <button
    type="button"
    onClick={() => setTab("chat")}
    className={`inline-flex items-center gap-0.5 rounded-l border px-2 py-0.5 text-[10px] font-medium ${
      tab === "chat"
        ? "border-indigo-300 bg-indigo-50 text-indigo-700"
        : "border-slate-200 bg-white text-slate-600 hover:bg-slate-50"
    }`}
    title="Agent chat and quick actions"
  >
    <Sparkles size={10} />
    Agent
  </button>
  {/* Segmented tab: MCP */}
  <button
    type="button"
    onClick={() => setTab("tools")}
    className={`inline-flex items-center gap-0.5 rounded-r border-t border-r border-b px-2 py-0.5 text-[10px] font-medium ${
      tab === "tools"
        ? "border-indigo-300 bg-indigo-50 text-indigo-700"
        : "border-slate-200 bg-white text-slate-600 hover:bg-slate-50"
    }`}
    title="MCP tool catalog"
  >
    <Wrench size={10} />
    MCP
  </button>
  <button
    type="button"
    onClick={onClose}
    className="text-slate-400 hover:text-slate-700 ml-1"
    aria-label="Close agent"
  >
    <X size={14} />
  </button>
</div>
```

Note the border stitching on the two adjacent buttons — left button gets `rounded-l border` (all four sides), right button gets `rounded-r border-t border-r border-b` (three sides, sharing the left border visually). Adjust if the existing Tailwind config has a `divide` utility you prefer, but keep it simple.

The Sparkles icon is already imported. Wrench and X are already imported. No new imports needed.

The rest of the component body — the `tab === "tools" ? <MCPPanel/> : <> quick-actions + chat </>` conditional (~line 313+) — stays completely unchanged.
  </action>
  <verify>
1. `npm run typecheck` from `apps/parrot/` passes.
2. Visit the app locally (or via `wrangler dev`), open an email, open the Agent panel — confirm two tab buttons appear in the header labeled "Agent" and "MCP", the active one is highlighted, and clicking each switches content correctly.
  </verify>
  <done>
The AgentPanel header shows "Agent" and "MCP" as a segmented tab pair. Clicking "Agent" shows the chat/quick-actions body; clicking "MCP" shows the MCPPanel. The "Tools" label is gone from the header.
  </done>
</task>

</tasks>

<verification>
Visual: AgentPanel header has two tabs labeled Agent and MCP. Functional: both tabs render their correct content pane. Typecheck passes.
</verification>

<success_criteria>
AgentPanel header is a proper two-tab segmented control (Agent | MCP). No new functionality added; only the existing single-toggle is upgraded to a labeled tab bar. No regressions in the quick-actions or MCPPanel views.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/30-parrot-email-pane-parity/30-02-SUMMARY.md`
</output>
