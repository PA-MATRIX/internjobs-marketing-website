---
phase: 24-neon-exit-closeout
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/HANDOFF.md
  - .planning/ROADMAP.md
  - /Users/rajren/.claude/projects/-Users-rajren-internjobs-cms/memory/infisical-project.md
autonomous: true
verification:
  surface: backend_only
  frontend_impact: false
  required_steps:
    - unit_tests
skills:
  - projecta.nextjs-typescript
skills_mode: normal

must_haves:
  truths:
    - "HANDOFF.md §4 no longer says 'One Neon database (neondb) for everything'"
    - "HANDOFF.md §4 describes the post-exit topology: student DB on Fly, safety_events via /internal/* API"
    - "ROADMAP.md Phase 24 progress row is updated to reflect verification complete"
    - "infisical-project memory lists the post-Neon-exit secrets: DATABASE_URL, INTERNAL_API_SECRET, STUDENT_API_SECRET, STUDENT_API_URL"
  artifacts:
    - path: ".planning/HANDOFF.md"
      provides: "Session context for future Codex/Claude sessions"
      contains: "internjobs-student-db"
    - path: ".planning/ROADMAP.md"
      provides: "v1.4 milestone progress tracking"
      contains: "Phase 24"
    - path: "/Users/rajren/.claude/projects/-Users-rajren-internjobs-cms/memory/infisical-project.md"
      provides: "Infisical secrets topology reference"
      contains: "INTERNAL_API_SECRET"
  key_links:
    - from: ".planning/HANDOFF.md §4"
      to: "infra/NEON-EXIT.md"
      via: "cross-reference link"
      pattern: "NEON-EXIT"
---

<objective>
Refresh the three planning docs that still describe the old Neon topology.
All changes are prose/doc edits — no application code is modified.

Purpose: Future Claude/Codex sessions that read HANDOFF.md will currently
get false context (§4 says safety_events lives in Neon; Neon no longer exists).
The infisical-project memory omits the four Neon-exit secrets. Leaving these
stale increases the risk of a future session re-introducing a Neon dependency
or mis-diagnosing a production issue.

Output: Three updated files committed. NEONEX-DOC-01, NEONEX-DOC-02,
NEONEX-DOC-03 closed.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/HANDOFF.md
@infra/NEON-EXIT.md
@.planning/milestones/v1.4-pilot-readiness/phases/24-neon-exit-closeout/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix HANDOFF.md §4 stale Neon claim (NEONEX-DOC-01)</name>
  <files>.planning/HANDOFF.md</files>
  <action>
    Read `.planning/HANDOFF.md` in full. Locate §4 "Key context Codex needs"
    (around line 100).

    Find the bullet:
      "**One Neon database** (`neondb`) for everything; safety_events lives there."

    Replace that bullet and any immediately adjacent Neon-topology context with
    an accurate post-exit description. The replacement should:

    1. Remove ALL references to Neon being an active database (Neon is gone).
    2. Describe the actual topology:
       - Student app DB: self-hosted Fly Postgres (`internjobs-student-db`,
         Postgres 17 + pgvector), internal-only at
         `internjobs-student-db.internal:5432`.
       - `safety_events` table lives in the student DB; the student app owns it.
       - The Parrot Worker cannot reach Fly-internal Postgres — it calls the
         student app's `/internal/safety-events` Bearer-authed API instead.
       - Secrets: `INTERNAL_API_SECRET` (student app Fly secret + Infisical),
         `STUDENT_API_SECRET` (Wrangler secret, same value), `STUDENT_API_URL`
         (Wrangler secret = `https://app.internjobs.ai`).
    3. Cross-reference `infra/NEON-EXIT.md` for the full migration record.

    Suggested replacement bullet text (adjust prose as needed to fit the
    surrounding §4 style):

      "**Student app DB is self-hosted Fly Postgres** (`internjobs-student-db`,
      Postgres 17 + pgvector). `safety_events` lives there. The Parrot Worker
      cannot reach Fly-internal Postgres, so it calls the student app's
      `/internal/safety-events` Bearer-authed API (env: `STUDENT_API_URL` +
      `STUDENT_API_SECRET` on the Worker; `INTERNAL_API_SECRET` on the student
      app). The three Neon projects were deleted 2026-05-21. See
      `infra/NEON-EXIT.md` for the full migration record."

    Also scan §4 for any other Neon references (e.g. `pg_session_jwt` note if
    present, or `neondb` mentions in other bullets) and remove or correct them.
    Do NOT restructure other sections — surgical edits only.
  </action>
  <verify>
    `grep -n "Neon database\|neondb\|NEON_DATABASE_URL\|@neondatabase" .planning/HANDOFF.md`
    returns zero matches in §4. The word "Neon" may still appear in historical
    sections (§5 commit log) — only §4 must be corrected.

    `grep -n "internjobs-student-db\|INTERNAL_API_SECRET" .planning/HANDOFF.md`
    returns at least 1 match each (the replacement bullet).
  </verify>
  <done>
    HANDOFF.md §4 no longer claims Neon is the active DB. The post-exit
    topology (Fly Postgres + /internal/safety-events API + secrets) is
    accurately described. Cross-reference to infra/NEON-EXIT.md present.
    NEONEX-DOC-01: PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 24 progress entry (NEONEX-DOC-02)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Read `.planning/ROADMAP.md`. The v1.4 progress table near the bottom of
    the file contains a row for Phase 24:

      | 24. Neon-Exit Closeout | v1.4 | team-cms | 0/TBD | Not started | — |

    This task makes two edits:

    **Edit A — progress table:**
    Update Phase 24's row to:
      | 24. Neon-Exit Closeout | v1.4 | team-cms | 2/2 | ✓ Shipped | 2026-05-25 |

    **Edit B — Phase 24 plan list:**
    In the `### Phase 24: Neon-Exit Closeout` details section, find:
      **Plans**: TBD (likely 2)

    Replace with:
      **Plans**: 2 plans

    And replace the TBD plan list with the actual plan checkboxes:
      Plans:
      - [x] 24-01-PLAN.md — E2E safety_events API verification + negative tests (NEONEX-VER-01..04)
      - [x] 24-02-PLAN.md — Docs refresh — HANDOFF.md, ROADMAP.md, infisical-project memory (NEONEX-DOC-01..03)

    **Edit C — Phase 24 checkbox in the Phases list:**
    Find the line:
      - [ ] **Phase 24: Neon-Exit Closeout**

    Change to:
      - [x] **Phase 24: Neon-Exit Closeout**

    No other sections of ROADMAP.md should be modified. The historical note
    about the Neon-exit in the v1.3 collapsed section already exists and is
    accurate — leave it as-is.
  </action>
  <verify>
    `grep -n "Phase 24" .planning/ROADMAP.md` shows:
    - The Phases list entry is `[x]`
    - The progress table row has `2/2` and `✓ Shipped`
    - The Phase 24 details section has `**Plans**: 2 plans`

    `grep -n "0/TBD\|Not started" .planning/ROADMAP.md` does NOT match Phase 24.
  </verify>
  <done>
    ROADMAP.md Phase 24 entry is fully updated: checkbox checked, progress
    table shows 2/2 shipped, plan list populated.
    NEONEX-DOC-02: PASS
  </done>
</task>

<task type="auto">
  <name>Task 3: Update infisical-project memory with post-exit secrets (NEONEX-DOC-03)</name>
  <files>/Users/rajren/.claude/projects/-Users-rajren-internjobs-cms/memory/infisical-project.md</files>
  <action>
    Read the infisical-project memory file at:
      /Users/rajren/.claude/projects/-Users-rajren-internjobs-cms/memory/infisical-project.md

    The current content describes the Infisical project/org/path but lists no
    specific secrets. Add a new section at the bottom titled
    "## Post-Neon-exit secrets (added 2026-05-21)".

    The section should list the four secrets that were added as part of the
    Neon-exit and are not described elsewhere in the file:

      ## Post-Neon-exit secrets (added 2026-05-21)

      The following secrets live at Infisical `/internjobs-ai` (env=prod) and
      replaced the now-deleted `NEON_DATABASE_URL`:

      | Secret | Consumer | Purpose |
      |--------|----------|---------|
      | `DATABASE_URL` | Fly `internjobs-ai-student-app` | student app → `internjobs-student-db` Fly Postgres |
      | `INTERNAL_API_SECRET` | Fly `internjobs-ai-student-app` | Bearer auth gate on `/internal/safety-events` API |
      | `STUDENT_API_SECRET` | Wrangler secret on `internjobs-parrot` Worker | Worker → student app (same value as `INTERNAL_API_SECRET`) |
      | `STUDENT_API_URL` | Wrangler secret on `internjobs-parrot` Worker | `https://app.internjobs.ai` |
      | `STUDENT_DB_PASSWORD` / `POSTGRES_PASSWORD` | Fly `internjobs-student-db` | DB auth (ijapp role password) |

      **Note:** `NEON_DATABASE_URL` has been removed from all consumers.
      `STUDENT_API_SECRET` is a Wrangler secret (not in Infisical) — the
      Infisical value `INTERNAL_API_SECRET` is the canonical source of truth
      for the shared shared secret.

    Also correct the stale org ID in the existing file body. The current file
    says `Org: 2c12f042-e98f-4fb3-8b40-16aec29f9b91` in the description text.
    Per STATE.md and .infisical.json, the correct org ID for Infisical CLI
    purposes is this same ID — but the "stale `2c12f042`" warning in MEMORY.md
    refers to a DIFFERENT stale item (wrong repo `.infisical.json`). Do NOT
    change the org ID — it is correct. Only add the new section.
  </action>
  <verify>
    `grep -n "INTERNAL_API_SECRET\|STUDENT_API_SECRET\|DATABASE_URL\|Post-Neon" \
      /Users/rajren/.claude/projects/-Users-rajren-internjobs-cms/memory/infisical-project.md`
    returns matches for each of the four secrets and the section header.

    The file must NOT contain the word "Neon" in any claim that Neon is still
    active (the existing "Previous project ID `0484b3ce`" line may reference Neon
    implicitly — leave that factual historical line; just don't add new Neon-live
    language).
  </verify>
  <done>
    infisical-project memory lists DATABASE_URL, INTERNAL_API_SECRET,
    STUDENT_API_SECRET, STUDENT_API_URL, and STUDENT_DB_PASSWORD with
    their consumer app and purpose. NEONEX-DOC-03: PASS
  </done>
</task>

</tasks>

<verification>
All 3 NEONEX-DOC requirements:

- NEONEX-DOC-01: `grep "Neon database\|neondb" .planning/HANDOFF.md` → zero matches in §4
- NEONEX-DOC-02: `grep "Phase 24" .planning/ROADMAP.md` → shows `[x]` + `2/2` + `✓ Shipped`
- NEONEX-DOC-03: `grep "INTERNAL_API_SECRET" /Users/rajren/.claude/projects/-Users-rajren-internjobs-cms/memory/infisical-project.md` → at least 1 match

No application code was modified in this plan. Only .planning/HANDOFF.md,
.planning/ROADMAP.md, and the memory file are changed.
</verification>

<success_criteria>
1. HANDOFF.md §4 accurately describes post-Neon-exit topology (Fly Postgres + /internal API + secrets)
2. ROADMAP.md Phase 24 row shows 2/2 shipped, checkbox checked
3. infisical-project memory includes the four post-Neon-exit secrets with consumer context
4. No application code modified (docs-only plan)
5. All 7 NEONEX requirements (VER-01..04 from 24-01 + DOC-01..03 from this plan) are closed across the two plans
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/24-neon-exit-closeout/24-02-SUMMARY.md`

Include:
- Confirmation that HANDOFF.md §4 stale bullet was replaced (quote the old + new text)
- Confirmation that ROADMAP.md progress table was updated
- Confirmation that infisical-project memory section was added
- NEONEX-DOC-01, NEONEX-DOC-02, NEONEX-DOC-03 status: PASS
- Commit hash for the docs-refresh commit
</output>
