---
phase: 22-lakera-and-brand-refresh
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/src/styles.css
  - apps/marketing/tailwind.config.ts
  - apps/marketing/public/logo/lockup-gradient-ink.svg
  - apps/marketing/public/logo/lockup-gradient-lavender.svg
  - apps/marketing/public/logo/lockup-ink.svg
  - apps/marketing/public/logo/lockup-lavender.svg
  - apps/marketing/public/logo/mark-gradient.svg
  - apps/marketing/public/logo/mark-ink.svg
  - apps/marketing/public/logo/mark-lavender.svg
  - apps/marketing/public/logo/png/lockup-gradient-ink_512w.png
  - apps/marketing/public/logo/png/lockup-gradient-ink_1024w.png
  - apps/marketing/public/logo/png/lockup-gradient-ink_2048w.png
  - apps/marketing/public/logo/png/lockup-gradient-ink_4096w.png
  - apps/marketing/public/logo/png/lockup-gradient-lavender_512w.png
  - apps/marketing/public/logo/png/lockup-gradient-lavender_1024w.png
  - apps/marketing/public/logo/png/lockup-gradient-lavender_2048w.png
  - apps/marketing/public/logo/png/lockup-gradient-lavender_4096w.png
  - apps/marketing/public/logo/png/lockup-ink_512w.png
  - apps/marketing/public/logo/png/lockup-ink_1024w.png
  - apps/marketing/public/logo/png/lockup-ink_2048w.png
  - apps/marketing/public/logo/png/lockup-ink_4096w.png
  - apps/marketing/public/logo/png/lockup-lavender_512w.png
  - apps/marketing/public/logo/png/lockup-lavender_1024w.png
  - apps/marketing/public/logo/png/lockup-lavender_2048w.png
  - apps/marketing/public/logo/png/lockup-lavender_4096w.png
  - apps/marketing/public/logo/png/mark-gradient_256w.png
  - apps/marketing/public/logo/png/mark-gradient_512w.png
  - apps/marketing/public/logo/png/mark-gradient_1024w.png
  - apps/marketing/public/logo/png/mark-gradient_2048w.png
  - apps/marketing/public/logo/png/mark-ink_256w.png
  - apps/marketing/public/logo/png/mark-ink_512w.png
  - apps/marketing/public/logo/png/mark-ink_1024w.png
  - apps/marketing/public/logo/png/mark-ink_2048w.png
  - apps/marketing/public/logo/png/mark-lavender_256w.png
  - apps/marketing/public/logo/png/mark-lavender_512w.png
  - apps/marketing/public/logo/png/mark-lavender_1024w.png
  - apps/marketing/public/logo/png/mark-lavender_2048w.png
  - apps/marketing/index.html
  - apps/marketing/public/favicon.ico
autonomous: true

coverage_diagram_not_applicable: true
# Reason: This plan places static assets (SVG/PNG/CSS tokens) and has no user-facing navigation flow to diagram.

verification:
  surface: ui_affecting
  frontend_impact: true
  required_steps:
    - unit_tests
    - playwright
    - chrome_visual_check

skills:
  - projecta.testing-vitest-playwright
  - projecta.visual-proof

must_haves:
  truths:
    - "6 brand color CSS variables defined in styles.css: --lavender #E8DEF5, --ink #1A0D2E, --lime #CAFF4D, --tangerine #FF7A3A, --cobalt #3855FF, --cream #FAF6EB"
    - "3 radii tokens defined in styles.css: --radius-card 18px, --radius-pill 999px, --radius-mark 8px"
    - "Tailwind color config extended with lavender, ink, lime, tangerine, cobalt, cream keys pointing to var(--X) references"
    - "Inter font loaded with weights 400 500 600 700 800 900 (existing Google Fonts link in index.html already covers this — verify and add apple-touch-icon + favicon links)"
    - "7 SVG logo variants exist at apps/marketing/public/logo/*.svg"
    - "28 PNG logo variants exist at apps/marketing/public/logo/png/*.png"
    - "Logo asset `lockup-gradient-ink.svg` exists at `apps/marketing/public/logo/lockup-gradient-ink.svg` and is ready for App.tsx wiring in 22-04. The actual Navbar <img> swap (BRAND-LOGO-03 / BRAND-LOGO-04) is performed in 22-04 Task 1 Step D."
    - "Favicon updated to mark-gradient_256w.png (ICO with 16/32/64 sizes) or mark-gradient.svg; Apple touch icon set to 180px mark-gradient PNG; safari-pinned-tab SVG added"
  artifacts:
    - path: "apps/marketing/src/styles.css"
      provides: "6 color vars + 3 radii tokens in :root"
      contains: "--lavender"
    - path: "apps/marketing/tailwind.config.ts"
      provides: "Tailwind color extension using CSS var references"
      contains: "lavender"
    - path: "apps/marketing/public/logo/lockup-gradient-ink.svg"
      provides: "Primary logo SVG for lavender backgrounds"
    - path: "apps/marketing/public/logo/png/"
      provides: "28 PNG raster fallbacks"
    - path: "apps/marketing/index.html"
      provides: "Correct favicon + apple-touch-icon + Inter font meta"
      contains: "apple-touch-icon"
  key_links:
    - from: "apps/marketing/tailwind.config.ts"
      to: "apps/marketing/src/styles.css"
      via: "Tailwind color values reference CSS vars: 'var(--lavender)'"
      pattern: "var\\(--lavender\\)"
    - from: "apps/marketing/index.html"
      to: "apps/marketing/public/logo/png/mark-gradient_256w.png"
      via: "apple-touch-icon link rel"
      pattern: "apple-touch-icon"
---

<objective>
Lay the brand foundation for apps/marketing/: CSS custom properties for all 6 color tokens + 3 radii tokens, Tailwind config extended to reference those vars, Inter font verified with all weights loaded, all logo SVG + PNG assets copied from ~/Downloads/logo_pack/ into the correct public/ directories, favicon + apple-touch-icon updated, and the site header wired to use lockup-gradient-ink.svg.

Purpose: These are the non-negotiable brand primitives that every component in 22-04 will build on. Getting them right here means 22-04 can focus purely on layout and copy.
Output: Brand tokens in CSS, tailwind extended, all 35 logo assets in place, favicon/touch-icon updated.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/brand/BRAND-V1.md
@apps/marketing/src/styles.css
@apps/marketing/tailwind.config.ts
@apps/marketing/index.html
@apps/marketing/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand tokens to styles.css and extend Tailwind config</name>
  <files>
    apps/marketing/src/styles.css
    apps/marketing/tailwind.config.ts
  </files>
  <action>
    PART A — styles.css:
    Add the following CSS custom properties to the `:root` block at the top of apps/marketing/src/styles.css. Insert them as the FIRST declarations inside `:root { }` (before the existing color/background/font-synthesis lines).

    ```css
    :root {
      /* Brand color tokens — v1.0 (BRAND-TOKENS-01) */
      --lavender:  #E8DEF5;
      --ink:       #1A0D2E;
      --lime:      #CAFF4D;
      --tangerine: #FF7A3A;
      --cobalt:    #3855FF;
      --cream:     #FAF6EB;

      /* Radii tokens — v1.0 (BRAND-TOKENS-02) */
      --radius-card: 18px;
      --radius-pill: 999px;
      --radius-mark: 8px;

      /* Existing declarations below — DO NOT REMOVE */
      color: #111111;
      ...
    }
    ```

    Also update the hardcoded `background: #fbf7ef` on `:root`, `html`, and `body` to reference `var(--lavender)`. Do NOT change these to `background: var(--lavender)` yet — leave existing background values in place for now. ONLY add the CSS var declarations at the top of :root. The existing bg color (#fbf7ef) is close to lavender but is the old cream-ish value; it will be replaced in plan 22-04 when we do the full surface audit.

    RATIONALE: Adding the variables now makes them available to all components. The actual background swap is done in 22-04's layout tasks so it does not break the current build.

    Also add the accent-span helper classes near the bottom of styles.css (before the @media blocks):
    ```css
    /* Punctuation accent spans — inherit section accent color (BRAND-COPY-01..04) */
    .accent-dot,
    .accent-comma {
      color: var(--lime);
    }
    [data-accent="tangerine"] .accent-dot,
    [data-accent="tangerine"] .accent-comma {
      color: var(--tangerine);
    }
    [data-accent="cobalt"] .accent-dot,
    [data-accent="cobalt"] .accent-comma {
      color: var(--cobalt);
    }
    ```

    PART B — tailwind.config.ts:
    Extend the `theme.extend.colors` block to include the brand palette using CSS var references. This lets Tailwind classes like `bg-lavender`, `text-ink`, `bg-lime`, etc. reference the CSS vars (which can be overridden per-component if needed in future).

    ```ts
    colors: {
      // Brand v1.0 tokens (reference CSS vars so a single source of truth)
      lavender:  'var(--lavender)',
      ink:       'var(--ink)',
      lime:      'var(--lime)',
      tangerine: 'var(--tangerine)',
      cobalt:    'var(--cobalt)',
      cream:     'var(--cream)',
      // Keep existing colors intact:
      canvas: "#FBF7EF",
      ...
    }
    ```

    Also add radii:
    ```ts
    borderRadius: {
      card: 'var(--radius-card)',
      pill: 'var(--radius-pill)',
      mark: 'var(--radius-mark)',
    }
    ```

    IMPORTANT: Keep all existing Tailwind color and shadow entries. Only ADD to the config — do not remove canvas, ink (old), electric, etc. The brand tokens are additive at this stage.

    TYPE SCALE — add font size / line-height / letter-spacing tokens to tailwind.config.ts:
    ```ts
    fontSize: {
      'display': ['clamp(72px, 8vw, 96px)', { lineHeight: '0.95', letterSpacing: '-0.04em', fontWeight: '900' }],
      'h1':      ['clamp(36px, 4vw, 48px)', { lineHeight: '1.05', letterSpacing: '-0.025em', fontWeight: '800' }],
      'h2':      ['clamp(24px, 2.5vw, 28px)', { lineHeight: 'normal', letterSpacing: '-0.015em', fontWeight: '800' }],
      'h3':      ['clamp(18px, 2vw, 20px)', { lineHeight: 'normal', letterSpacing: 'normal', fontWeight: '700' }],
      'label':   ['clamp(10px, 1vw, 11px)', { lineHeight: 'normal', letterSpacing: '0.1em', fontWeight: '600' }],
    }
    ```

    Verify build after changes:
    cd apps/marketing && npm run build
    Expected: build succeeds (no TypeScript errors on the config file)
  </action>
  <verify>
    grep -n "\-\-lavender" apps/marketing/src/styles.css
    grep -n "\-\-radius-card" apps/marketing/src/styles.css
    grep -n "accent-dot" apps/marketing/src/styles.css
    grep -n "lavender" apps/marketing/tailwind.config.ts
    grep -n "radius-card" apps/marketing/tailwind.config.ts
    cd apps/marketing && npm run build 2>&1 | tail -5
    All greps must return results. Build must succeed.
  </verify>
  <done>
    - styles.css :root block contains all 6 color vars and 3 radii tokens
    - styles.css contains .accent-dot / .accent-comma + data-accent overrides
    - tailwind.config.ts extended with lavender/ink/lime/tangerine/cobalt/cream color refs
    - tailwind.config.ts extended with card/pill/mark border-radius tokens
    - tailwind.config.ts extended with display/h1/h2/h3/label font-size tokens
    - npm run build in apps/marketing succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy logo assets from ~/Downloads/logo_pack/ into public/logo/</name>
  <files>
    apps/marketing/public/logo/lockup-gradient-ink.svg
    apps/marketing/public/logo/lockup-gradient-lavender.svg
    apps/marketing/public/logo/lockup-ink.svg
    apps/marketing/public/logo/lockup-lavender.svg
    apps/marketing/public/logo/mark-gradient.svg
    apps/marketing/public/logo/mark-ink.svg
    apps/marketing/public/logo/mark-lavender.svg
    apps/marketing/public/logo/png/
  </files>
  <action>
    Create the target directories and copy the logo assets:

    ```bash
    mkdir -p /Users/rajren/internjobs-cms/apps/marketing/public/logo/png
    ```

    Copy the 7 SVGs:
    ```bash
    cp /Users/rajren/Downloads/logo_pack/lockup-gradient-ink.svg \
       /Users/rajren/Downloads/logo_pack/lockup-gradient-lavender.svg \
       /Users/rajren/Downloads/logo_pack/lockup-ink.svg \
       /Users/rajren/Downloads/logo_pack/lockup-lavender.svg \
       /Users/rajren/Downloads/logo_pack/mark-gradient.svg \
       /Users/rajren/Downloads/logo_pack/mark-ink.svg \
       /Users/rajren/Downloads/logo_pack/mark-lavender.svg \
       /Users/rajren/internjobs-cms/apps/marketing/public/logo/
    ```

    Copy the 28 PNGs:
    ```bash
    cp /Users/rajren/Downloads/logo_pack/png/*.png \
       /Users/rajren/internjobs-cms/apps/marketing/public/logo/png/
    ```

    Verify clearspace and min-size constraints (BRAND-LOGO-07) by reading the SVG viewBox:
    ```bash
    grep -m1 "viewBox" /Users/rajren/internjobs-cms/apps/marketing/public/logo/lockup-gradient-ink.svg
    grep -m1 "viewBox" /Users/rajren/internjobs-cms/apps/marketing/public/logo/mark-gradient.svg
    ```
    The mark SVG viewBox should have a 2:1 aspect ratio (width = 2 × height, e.g. 64×32 or 128×64).
    The lockup SVG should have approximately a 7:1 aspect ratio (width ≈ 7 × height).
    These values confirm clearspace compliance — document the actual viewBox values in SUMMARY.md.

    IMPORTANT: Do NOT modify any SVG files. Copy only, no edits.
  </action>
  <verify>
    ls /Users/rajren/internjobs-cms/apps/marketing/public/logo/*.svg | wc -l
    Expected: 7

    ls /Users/rajren/internjobs-cms/apps/marketing/public/logo/png/*.png | wc -l
    Expected: 28

    ls /Users/rajren/internjobs-cms/apps/marketing/public/logo/lockup-gradient-ink.svg
    ls /Users/rajren/internjobs-cms/apps/marketing/public/logo/lockup-lavender.svg
    ls /Users/rajren/internjobs-cms/apps/marketing/public/logo/mark-gradient.svg
    All must exist.
  </verify>
  <done>
    7 SVG files exist at apps/marketing/public/logo/
    28 PNG files exist at apps/marketing/public/logo/png/
    No SVG files were modified (bit-for-bit copy from source)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update index.html — favicon, apple-touch-icon, Inter font weights check</name>
  <files>
    apps/marketing/index.html
    apps/marketing/public/favicon.ico
  </files>
  <action>
    PART A — Verify Inter font loading (BRAND-TYPE-01):
    Check the existing Google Fonts link in index.html. The current link already loads:
    family=Inter:wght@400;500;600;700;800;900
    This satisfies BRAND-TYPE-01. No change needed to the font link itself.
    If the link is missing any weight (e.g., missing 500 or 900), update it to:
    https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap

    PART B — Favicon update (BRAND-LOGO-05):
    The current favicon uses /favicon.svg. Replace with mark-gradient:
    1. Remove: <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    2. Add multi-size favicon using the PNG (ICO approach for broad compat):
    ```html
    <link rel="icon" type="image/png" sizes="32x32" href="/logo/png/mark-gradient_256w.png" />
    <link rel="icon" type="image/png" sizes="64x64" href="/logo/png/mark-gradient_256w.png" />
    ```
    Note: mark-gradient_256w.png is used for 16/32/64 sizes since browsers scale it. If a true favicon.ico is needed for legacy support, keep /favicon.svg as a fallback OR generate a .ico in Task 3c.

    PART C — Apple Touch Icon (BRAND-LOGO-05):
    Add after the favicon links:
    ```html
    <link rel="apple-touch-icon" sizes="180x180" href="/logo/png/mark-gradient_256w.png" />
    ```
    Note: mark-gradient_256w.png at 256px is large enough for the 180px apple touch icon requirement.

    PART D — Safari Pinned Tab (BRAND-LOGO-05):
    The mark-gradient.svg uses a gradient fill. Safari pinned tab requires a monochrome SVG with a dark fill. Use mark-ink.svg:
    ```html
    <link rel="mask-icon" href="/logo/mark-ink.svg" color="#1A0D2E" />
    ```

    PART E — Title and meta description update:
    Update title to lowercase brand voice:
    ```html
    <title>internjobs.ai | internships, in your dms.</title>
    ```
    Update meta description to match brand voice:
    ```html
    <meta name="description" content="internships, in your dms. no resumes, no cover letters, just texts. internjobs.ai keeps looking while you're busy." />
    ```

    PART F — Theme color meta:
    Add:
    ```html
    <meta name="theme-color" content="#E8DEF5" />
    ```

    After all changes, run: cd apps/marketing && npm run build
    Expected: build succeeds with no errors.
  </action>
  <verify>
    grep "apple-touch-icon" apps/marketing/index.html
    grep "mark-ink.svg" apps/marketing/index.html
    grep "mark-gradient" apps/marketing/index.html
    grep "Inter:wght@400;500;600;700;800;900" apps/marketing/index.html
    grep "theme-color" apps/marketing/index.html
    cd apps/marketing && npm run build 2>&1 | tail -3
    All greps must return results. Build must succeed.
  </verify>
  <done>
    - index.html has apple-touch-icon (180px mark-gradient_256w.png)
    - index.html has mask-icon with mark-ink.svg for Safari pinned tab
    - index.html has favicon link pointing to /logo/png/mark-gradient_256w.png
    - Inter font link includes all weights 400-900
    - Title updated to brand-voice lowercase
    - Meta description updated to brand voice
    - theme-color meta set to #E8DEF5 (lavender)
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
Run from monorepo root:

1. grep -n "\-\-lavender" apps/marketing/src/styles.css — must show :root declaration
2. grep -n "\-\-radius-pill" apps/marketing/src/styles.css — must show 999px
3. grep -n "accent-dot" apps/marketing/src/styles.css — must show .accent-dot rule
4. grep -n "lavender" apps/marketing/tailwind.config.ts — must show color extension
5. ls apps/marketing/public/logo/*.svg | wc -l — must show 7
6. ls apps/marketing/public/logo/png/*.png | wc -l — must show 28
7. grep "apple-touch-icon" apps/marketing/index.html — must show link tag
8. cd apps/marketing && npm run build — must succeed
</verification>

<success_criteria>
- BRAND-TOKENS-01: 6 color CSS vars in styles.css :root block; no hex literals in components (audit deferred to 22-04)
- BRAND-TOKENS-02: 3 radii tokens in styles.css :root block
- BRAND-TYPE-01: Inter loaded with weights 400-900 in index.html Google Fonts link (existing link covers this; confirmed no missing weights)
- BRAND-TYPE-02: Type-scale tokens added to tailwind.config.ts (display/h1/h2/h3/label with correct sizes, tracking, line-height)
- BRAND-LOGO-01: 7 SVG variants at apps/marketing/public/logo/
- BRAND-LOGO-02: 28 PNG variants at apps/marketing/public/logo/png/
- BRAND-LOGO-05: Favicon = mark-gradient PNG (multi-size); Apple touch icon = 180px mark-gradient; Safari pinned tab = mark-ink.svg
- BRAND-LOGO-07: Logo clearspace and aspect ratio verified by inspecting SVG viewBox attributes (mark: 2:1 ratio; lockup: ~7:1 ratio); actual values documented in SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/22-lakera-and-brand-refresh/22-03-SUMMARY.md`
</output>
