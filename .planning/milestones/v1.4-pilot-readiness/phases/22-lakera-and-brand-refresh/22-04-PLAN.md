---
phase: 22-lakera-and-brand-refresh
plan: "04"
type: execute
wave: 2
depends_on:
  - "22-03"
files_modified:
  - apps/marketing/src/App.tsx
  - apps/marketing/src/styles.css
  - apps/marketing/index.html
autonomous: true

coverage_diagram: |
  User paths through marketing site after Phase 22-04 brand apply:

  / (apex)                        ★ hero: lavender bg, lime CTA, accent-comma/dot
    └─ /startups                  ★ hero: cobalt accent, lockup-lavender.svg, cobalt CTA
         └─ /privacy              ★ cream bg (legal exception)
         └─ /terms                ★ cream bg (legal exception)

  Navbar: lockup-gradient-ink.svg (default) / lockup-lavender.svg (/startups)

verification:
  surface: ui_affecting
  frontend_impact: true
  required_steps:
    - unit_tests
    - playwright
    - chrome_visual_check

skills:
  - projecta.testing-vitest-playwright
  - projecta.visual-proof
  - projecta.shadcn-ui

must_haves:
  truths:
    - "Apex / hero uses lavender background (var(--lavender) = #E8DEF5), ink text (var(--ink) = #1A0D2E), lime accent; data-accent='lime' on the hero section wrapper"
    - "/startups page hero uses cobalt accent (data-accent='cobalt'); header logo switches to lockup-lavender.svg on cobalt sections"
    - "Apex hero headline reads exactly: internships<span class='accent-comma'>,</span> in your dms<span class='accent-dot'>.</span>"
    - "Apex hero subhead reads exactly: no resumes · no cover letters · just texts"
    - "Apex CTA reads 'get on the list →' as a lime pill (background var(--lime), color var(--ink)), lowercase, border-radius var(--radius-pill)"
    - "/startups hero headline reads: hire interns by text<span class='accent-comma'>,</span> not by tower of resumes<span class='accent-dot'>.</span>"
    - "/startups CTA reads 'post a role →' as a cobalt pill (background var(--cobalt), color var(--lavender)), lowercase"
    - "Zero occurrences of #fff, #FFFFFF, white (as CSS value), #000, or gray-fill in apps/marketing/src/App.tsx and apps/marketing/src/styles.css"
    - "Zero occurrences of forbidden corp-speak: 'Unlock', 'Streamline', 'revolutionary', 'in today's competitive landscape', or title-case headlines in App.tsx"
    - "Every reference to the brand name is lowercase 'internjobs.ai' (including the dot)"
    - "Uppercase LABEL elements (e.g. 'JOIN EARLY ACCESS · HOUSTON, TX') use Inter 600, tracking 0.1em, uppercase CSS — verified by grep on App.tsx (BRAND-COPY-06)"
    - "Cream #FAF6EB used only on /privacy and /terms pages, never mixed with lavender on same surface"
    - "OG image meta tags present in index.html; at least one of og-1200x630.png or og.svg committed to apps/marketing/public/logo/ (path determined by tool availability pre-check in Task 3)"
    - "Logo clearspace maintained: header lockup is at least 120px wide; favicon mark is at least 28px"
    - "Navbar <img> element in App.tsx references lockup-gradient-ink.svg (default) confirming BRAND-LOGO-03 wiring from 22-03 assets"
  artifacts:
    - path: "apps/marketing/src/App.tsx"
      provides: "Rewritten hero sections, accent system, updated copy, inline span accents, pill CTAs"
      contains: "accent-comma"
    - path: "apps/marketing/src/styles.css"
      provides: "Updated background to var(--lavender), no white/black/gray hex literals"
      contains: "var(--lavender)"
    - path: "apps/marketing/index.html"
      provides: "OG image meta tags"
      contains: "og:image"
  key_links:
    - from: "apps/marketing/src/App.tsx HeroSection"
      to: "apps/marketing/src/styles.css"
      via: "data-accent='lime' attribute + CSS [data-accent='lime'] rules"
      pattern: "data-accent"
    - from: "apps/marketing/src/App.tsx StartupPage"
      to: "apps/marketing/public/logo/lockup-lavender.svg"
      via: "<img src='/logo/lockup-lavender.svg'> in cobalt hero section"
      pattern: "lockup-lavender"
---

<objective>
Apply the full brand surface to apps/marketing/: swap all backgrounds to var(--lavender), implement the data-accent system, rewrite apex and /startups hero copy with correct punctuation-accent inline spans, replace CTAs with brand pills, add cobalt exception logo on /startups, generate and wire the OG image, audit and remove all forbidden colors (white/black/gray hex literals) and corp-speak from components and styles.

Purpose: 22-03 laid the tokens and assets. This plan is the surface rewrite — all the visible brand changes users will see.
Output: apps/marketing/ fully brand-compliant, building, with correct apex + /startups heroes and a clean grep audit.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/brand/BRAND-V1.md
@apps/marketing/src/App.tsx
@apps/marketing/src/styles.css
@apps/marketing/tailwind.config.ts
@apps/marketing/index.html
.planning/milestones/v1.4-pilot-readiness/phases/22-lakera-and-brand-refresh/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite apex hero section + /startups hero section with brand copy, accent system, and logo swap</name>
  <files>
    apps/marketing/src/App.tsx
    apps/marketing/src/styles.css
  </files>
  <action>
    This is a targeted rewrite of the hero sections and accent system. Read App.tsx carefully before editing. Do NOT rewrite the entire file — only the sections listed below.

    ---
    STEP A — Add data-accent attribute to page wrappers:

    In the App() function return (the main page shell), the outermost wrapper already has `className="page-shell min-h-screen overflow-hidden bg-canvas text-ink"`. Add `data-accent="lime"` to this element:
    ```tsx
    <main className="page-shell min-h-screen overflow-hidden" style={{ background: 'var(--lavender)', color: 'var(--ink)' }} data-accent="lime">
    ```
    Remove the Tailwind `bg-canvas text-ink` classes (they used old color values) and replace with inline CSS vars. This makes the page use lavender + ink at the root level.

    In the StartupPage() function return, add `data-accent="cobalt"` to the top-level wrapper:
    ```tsx
    <main className="startup-page min-h-screen overflow-hidden" style={{ background: 'var(--lavender)', color: 'var(--ink)' }} data-accent="cobalt">
    ```

    In the PrivacyPage() and TermsPage() wrappers, add `data-accent="lime"` and use cream background:
    ```tsx
    <main className="legal-page min-h-screen overflow-hidden" style={{ background: 'var(--cream)', color: 'var(--ink)' }} data-accent="lime">
    ```
    RATIONALE: Cream is only allowed on long-form legal pages per the brand spec. Never on lavender surfaces.

    ---
    STEP B — Rewrite HeroSection headline, subhead, and CTA:

    Find the HeroSection() function. Replace the hero h1, p, and CTA block with:

    ```tsx
    <h1 className="font-display lowercase" style={{ fontSize: 'clamp(72px, 8vw, 96px)', lineHeight: '0.95', letterSpacing: '-0.04em', fontWeight: 900, color: 'var(--ink)' }}>
      internships<span className="accent-comma">,</span> in your dms<span className="accent-dot">.</span>
    </h1>

    <p className="mt-4 max-w-[28rem]" style={{ fontSize: '16px', lineHeight: '1.55', color: 'var(--ink)', opacity: 0.7 }}>
      no resumes · no cover letters · just texts
    </p>

    <div className="mt-6 flex flex-row gap-3">
      <a
        href={APP_WAITLIST_URL}
        style={{
          background: 'var(--lime)',
          color: 'var(--ink)',
          borderRadius: 'var(--radius-pill)',
          padding: '0.75rem 1.75rem',
          fontWeight: 700,
          fontSize: '1rem',
          display: 'inline-flex',
          alignItems: 'center',
          gap: '0.5rem',
          textDecoration: 'none',
        }}
      >
        get on the list <ArrowRight size={16} />
      </a>
    </div>
    ```

    Remove the old headline ("Internships over text."), old subhead, and old primary-party-button CTA from the HeroSection. Keep all other HeroSection code (phone demo, scroll effects, etc.) intact.

    Also update the label/badge above the headline. Replace "Text-first internship search" badge with the brand label pattern:
    ```tsx
    <p style={{ fontSize: '11px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: 'var(--ink)', opacity: 0.6 }}>
      JOIN EARLY ACCESS · HOUSTON, TX
    </p>
    ```

    ---
    STEP C — Rewrite /startups hero:

    Find the StartupPage() function. Find the hero headline + CTA. Replace with:

    ```tsx
    <h1 className="font-display lowercase" style={{ fontSize: 'clamp(72px, 8vw, 96px)', lineHeight: '0.95', letterSpacing: '-0.04em', fontWeight: 900, color: 'var(--ink)' }}>
      hire interns by text<span className="accent-comma">,</span> not by tower of resumes<span className="accent-dot">.</span>
    </h1>

    <p className="mt-4" style={{ fontSize: '16px', lineHeight: '1.55', color: 'var(--ink)', opacity: 0.7 }}>
      vetted students · matched in minutes · no platform fee
    </p>

    <div className="mt-6">
      <a
        href={APP_WAITLIST_URL}
        style={{
          background: 'var(--cobalt)',
          color: 'var(--lavender)',
          borderRadius: 'var(--radius-pill)',
          padding: '0.75rem 1.75rem',
          fontWeight: 700,
          fontSize: '1rem',
          display: 'inline-flex',
          alignItems: 'center',
          gap: '0.5rem',
          textDecoration: 'none',
        }}
      >
        post a role <ArrowRight size={16} />
      </a>
    </div>
    ```

    ---
    STEP D — Cobalt exception: logo swap on /startups (BRAND-LOGO-03 / BRAND-LOGO-04):

    The existing Navbar() shows the BrandMark component (the animated ∞ gradient mark). On the /startups page, the brand spec requires switching to lockup-lavender.svg on cobalt sections (cobalt exception).

    In the Navbar() function, detect whether we're on /startups:
    ```tsx
    const isStartupPage = typeof window !== 'undefined' && window.location.pathname.replace(/\/$/, '') === '/startups';
    ```
    (This is already computed in App() — pass it as a prop to Navbar, or re-read it in Navbar.)

    In the Navbar logo area, replace the BrandMark + text span with:
    ```tsx
    <a href="/" className="flex items-center" aria-label="internjobs.ai home" onClick={() => setOpen(false)}>
      {isStartupPage ? (
        <img src="/logo/lockup-lavender.svg" alt="internjobs.ai" height={28} style={{ minWidth: 120 }} />
      ) : (
        <img src="/logo/lockup-gradient-ink.svg" alt="internjobs.ai" height={28} style={{ minWidth: 120 }} />
      )}
    </a>
    ```
    This satisfies BRAND-LOGO-03 (header uses lockup-gradient-ink.svg) and BRAND-LOGO-04 (cobalt sections use lockup-lavender.svg).

    ---
    STEP E — Styles.css: replace old background values with var(--lavender):

    In styles.css, find all instances of `background: #fbf7ef` and `background: #FBF7EF` and replace with `background: var(--lavender)`.
    Also find `.page-shell` background references (the `::before` pseudo-element) and update any `#fbf7ef` in gradient stops to `var(--lavender)`.

    Find `background: #fbfbf8` in the iphone-screen / ios-statusbar (this is the iPhone UI mock — these are UI chrome colors inside the phone demo, NOT marketing surfaces — leave those intact).

    ---
    STEP F — Brand name audit (BRAND-COPY-07):

    In App.tsx, grep for "InternJobs.ai" (title-case). Replace ALL occurrences visible to users (in h1, p, span, nav copy) with lowercase "internjobs.ai". Exceptions allowed:
    - Legal text (Privacy/Terms) may use "InternJobs.ai" once in the intro formal definition sentence — leave those
    - `aria-label` can use either (prefer lowercase for screenreaders)
    - Import/variable names are code, not copy — leave PascalCase in code

    Audit: grep -n "InternJobs\.ai" apps/marketing/src/App.tsx | grep -v "aria-label\|import\|const\|type\|//\|Legal\|Privacy\|Terms"
    All remaining results should be in legal text only.

    ---
    STEP G — Corp-speak audit (BRAND-COPY-08):

    grep -ni "unlock\|streamline\|revolutionary\|competitive landscape\|Transforming\|Revolutionizing\|Empowering\|Leverage\|Synergy" apps/marketing/src/App.tsx
    If any results: remove or rewrite to brand voice. Zero matches required.

    Also grep for title-case headlines (words that shouldn't be title-cased):
    Check h1/h2/h3 content for Title Case patterns (words capitalized without being proper nouns).
    Brand rule: all headlines lowercase. Fix any violations.

    ---
    Run build: cd apps/marketing && npm run build
    Must succeed with no errors.
  </action>
  <verify>
    grep -n "accent-comma" apps/marketing/src/App.tsx
    Expected: at least 2 results (apex hero + /startups hero)

    grep -n "accent-dot" apps/marketing/src/App.tsx
    Expected: at least 2 results

    grep -n "data-accent" apps/marketing/src/App.tsx
    Expected: results for "lime", "cobalt", "lime" (on main/startup/legal pages)

    grep -n "lockup-lavender.svg" apps/marketing/src/App.tsx
    Expected: result in the Navbar logo area (cobalt exception)

    grep -n "lockup-gradient-ink.svg" apps/marketing/src/App.tsx
    Expected: result in the Navbar logo area (default — confirms BRAND-LOGO-03)

    grep -n "get on the list" apps/marketing/src/App.tsx
    Expected: 1 result (apex CTA)

    grep -n "post a role" apps/marketing/src/App.tsx
    Expected: 1 result (/startups CTA)

    grep -in "unlock\|streamline\|revolutionary\|competitive landscape" apps/marketing/src/App.tsx
    Expected: 0 results

    grep -nE "letterSpacing.*['\"]0\.1em['\"]" apps/marketing/src/App.tsx
    Expected: at least 1 hit on a label/uppercase element (BRAND-COPY-06)

    grep -nE "fontWeight.*600" apps/marketing/src/App.tsx
    Expected: at least 1 hit on a label/uppercase element (BRAND-COPY-06)

    grep -c '#fbf7ef\|#FBF7EF' apps/marketing/src/styles.css
    Expected: 0 (all old canvas bg values replaced with var(--lavender))

    cd apps/marketing && npm run build 2>&1 | tail -5
    Expected: build succeeds
  </verify>
  <done>
    - Apex hero h1: "internships, in your dms." with accent-comma and accent-dot spans
    - Apex subhead: "no resumes · no cover letters · just texts"
    - Apex CTA: "get on the list →" lime pill with ink text
    - /startups h1: "hire interns by text, not by tower of resumes." with accent spans
    - /startups CTA: "post a role →" cobalt pill with lavender text
    - data-accent attributes set on page wrappers
    - Navbar shows lockup-gradient-ink.svg (default) / lockup-lavender.svg (/startups)
    - styles.css #fbf7ef replaced with var(--lavender) on marketing surfaces (grep count = 0)
    - Label elements use fontWeight 600 + letterSpacing 0.1em + textTransform uppercase (BRAND-COPY-06)
    - Zero corp-speak grep matches
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Color/white audit — remove all forbidden hex literals from App.tsx and styles.css</name>
  <files>
    apps/marketing/src/App.tsx
    apps/marketing/src/styles.css
  </files>
  <action>
    Run the forbidden-color audit per BRAND-LAYOUT-05 and BRAND-TOKENS-01.

    PART A — App.tsx audit:
    Search for forbidden hex literals in component JSX/TSX. Marketing component surfaces must not contain:
    - #fff or #FFFFFF (white)
    - #000 or #000000 or #111 or #111111 (near-black)
    - Gray fills: any #[678][678][678] pattern, #555, #666, #888, #999, #aaa

    Run:
    grep -in "#fff\b\|#ffffff\|#000\b\|#000000\|#111\b\|#111111\|#555\b\|#666\b\|#888\b\|#999\b\|#aaa\b" apps/marketing/src/App.tsx

    For each result, determine context:
    - Marketing surface (hero, section, header, footer, CTA) → replace with brand token (var(--lavender), var(--ink), var(--lime), etc.)
    - UI chrome inside the phone demo mockup (iphone-shell, ios-bubble, whatsapp-surface, slack-surface, discord-surface, phonecall-surface, WhatsApp colors) → LEAVE INTACT. These simulate real app UIs and are not marketing surfaces.
    - Legal page body text → may use var(--ink) at reduced opacity

    Key replacements for marketing surfaces:
    - background: #ffffff → background: var(--lavender)
    - color: #111111 → color: var(--ink)
    - color: #ffffff → color: var(--lavender) (if on dark/cobalt bg)
    - background: #111111 (dark CTA button) → background: var(--ink)

    For the Navbar backdrop: the `bg-white/46` Tailwind class must be replaced. The navbar background should use lavender at opacity: `bg-lavender/80` (after Tailwind is configured with the lavender color from 22-03). Alternatively use: `style={{ background: 'rgba(232, 222, 245, 0.8)' }}`.

    PART B — styles.css audit:
    Same pattern. Forbidden:
    - `background: white;`, `background: #ffffff;`, `background: #fff;`
    - `color: black;`, `color: #000;`
    - `background: #fbf7ef;` (old canvas — replace with var(--lavender))
    - Gray fills in marketing sections

    Run:
    grep -in "#fff\b\|#ffffff\|background: white\|color: black\|#111111\|#fbf7ef" apps/marketing/src/styles.css

    EXCEPTION: UI mock components (classes like .whatsapp-surface, .slack-surface, .discord-surface, .slack-compose, .startup-slack-*, .phonecall-*) are phone/app simulations. Their internal colors simulate real app UIs (WhatsApp green, Slack purple, etc.) — leave them.

    EXCEPTION: The phone shell itself (.iphone-shell background: #1a1a1a) is a device chrome — leave it.

    After replacements, run build:
    cd apps/marketing && npm run build

    Then run the canonical audit check:
    grep -n "#fff\b\|#ffffff\b\|background: white\b\|background:#fff" apps/marketing/src/App.tsx | grep -v "whatsapp\|slack\|discord\|phone\|iphone\|//\|mock"
    Expected: 0 results on marketing surfaces

    grep -n "#fff\b\|#ffffff\b\|background: white" apps/marketing/src/styles.css | grep -v "whatsapp\|slack\|discord\|phone\|iphone"
    Expected: 0 results on marketing CSS classes
  </action>
  <verify>
    grep -n "background: white\b\|background:#fff\b\|background: #fff\b\|background:#ffffff" apps/marketing/src/styles.css | grep -v "whatsapp\|slack\|discord\|phone\|iphone\|phonecall"
    Expected: 0 results

    grep -in "\"#fff\"\|'#fff'\|\"#ffffff\"\|'#ffffff'" apps/marketing/src/App.tsx | grep -v "whatsapp\|slack\|discord\|phone\|iphone\|call\|//\|startup-slack"
    Expected: 0 results on marketing surface components

    cd apps/marketing && npm run build 2>&1 | tail -3
    Expected: build succeeds
  </verify>
  <done>
    - All white/black/gray hex literals removed from marketing surfaces in App.tsx and styles.css
    - UI mock components (phone demo internals, WhatsApp/Slack/Discord surfaces) retained as-is
    - Build succeeds after audit
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate OG image and add OG meta tags to index.html</name>
  <files>
    apps/marketing/index.html
    apps/marketing/public/logo/og-1200x630.png
  </files>
  <action>
    BRAND-LOGO-06: OG image 1200×630 with logo + "internships, in your dms." on lavender background.

    STEP 0 — Pre-check: determine which PNG tool is available (this decides the output path):
    ```bash
    which convert && echo "has-imagemagick" || \
    which rsvg-convert && echo "has-rsvg" || \
    (node -e "require('sharp')" 2>/dev/null && echo "has-sharp") || \
    (node -e "require('canvas')" 2>/dev/null && echo "has-canvas") || \
    echo "no-png-tools"
    ```
    Capture the result. Then follow the matching branch below — produce EXACTLY ONE output file and use that path in the OG meta tags. Do NOT leave an "either/or" state at verify time.

    ---
    BRANCH A — PNG tools available (has-imagemagick OR has-rsvg OR has-sharp OR has-canvas):

    Use whichever tool was detected to produce:
      apps/marketing/public/logo/og-1200x630.png

    ImageMagick example:
    ```bash
    convert -size 1200x630 xc:'#E8DEF5' \
      \( /Users/rajren/Downloads/logo_pack/png/lockup-gradient-ink_1024w.png -resize 560x \) \
      -gravity Center -composite \
      /Users/rajren/internjobs-cms/apps/marketing/public/logo/og-1200x630.png
    ```

    rsvg-convert example (write SVG first, then rasterize):
    ```bash
    cat > /tmp/og.svg << 'EOF'
    <svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
      <rect width="1200" height="630" fill="#E8DEF5"/>
      <text x="600" y="280" font-family="Inter,sans-serif" font-weight="900" font-size="72"
        fill="#1A0D2E" text-anchor="middle" letter-spacing="-2.88">internjobs.ai</text>
      <text x="600" y="370" font-family="Inter,sans-serif" font-weight="400" font-size="36"
        fill="#1A0D2E" text-anchor="middle" opacity="0.7">internships, in your dms.</text>
    </svg>
    EOF
    rsvg-convert -w 1200 -h 630 /tmp/og.svg > \
      /Users/rajren/internjobs-cms/apps/marketing/public/logo/og-1200x630.png
    ```

    OG meta tag path: `/logo/og-1200x630.png`

    ---
    BRANCH B — No PNG tools available (no-png-tools):

    Produce an SVG instead and use it as the og:image:
    ```bash
    cat > /Users/rajren/internjobs-cms/apps/marketing/public/logo/og.svg << 'EOF'
    <svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
      <rect width="1200" height="630" fill="#E8DEF5"/>
      <text x="600" y="280" font-family="Inter,sans-serif" font-weight="900" font-size="72"
        fill="#1A0D2E" text-anchor="middle" letter-spacing="-2.88">internjobs.ai</text>
      <text x="600" y="370" font-family="Inter,sans-serif" font-weight="400" font-size="36"
        fill="#1A0D2E" text-anchor="middle" opacity="0.7">internships, in your dms.</text>
    </svg>
    EOF
    ```

    OG meta tag path: `/logo/og.svg`
    Document in SUMMARY.md: "PNG tools unavailable on host — og.svg produced. Regenerate og-1200x630.png with ImageMagick for better social platform support."

    ---
    WIRING — Add OG meta tags to index.html (after the description meta).
    Use whichever path was produced above (og-1200x630.png OR og.svg):
    ```html
    <!-- Open Graph (BRAND-LOGO-06) -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://internjobs.ai" />
    <meta property="og:title" content="internjobs.ai | internships, in your dms." />
    <meta property="og:description" content="no resumes · no cover letters · just texts. internjobs.ai keeps looking while you're busy." />
    <meta property="og:image" content="https://internjobs.ai/logo/og-1200x630.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <!-- Twitter/X Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="internjobs.ai" />
    <meta name="twitter:description" content="internships, in your dms." />
    <meta name="twitter:image" content="https://internjobs.ai/logo/og-1200x630.png" />
    ```
    Replace the path in the content= attributes to match the actual file produced (og-1200x630.png or og.svg).

    Document which branch was taken in SUMMARY.md.
  </action>
  <verify>
    # Exactly one of these must exist (whichever branch was taken):
    ls -la apps/marketing/public/logo/og-1200x630.png 2>/dev/null || \
    ls -la apps/marketing/public/logo/og.svg 2>/dev/null
    Expected: exactly one file exists

    grep "og:image" apps/marketing/index.html
    Expected: meta tag content matches the file actually produced (no dangling reference to the other path)

    grep "twitter:card" apps/marketing/index.html
    Expected: summary_large_image meta tag

    cd apps/marketing && npm run build 2>&1 | tail -3
    Expected: build succeeds
  </verify>
  <done>
    - Exactly one OG image file exists (og-1200x630.png if PNG tools available, og.svg otherwise)
    - index.html og:image content attribute points to the file that actually exists
    - index.html contains og:image, og:title, og:description, og:type, twitter:card meta tags
    - Which branch (PNG or SVG) documented in SUMMARY.md
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
Run from monorepo root:

1. grep -n "accent-comma\|accent-dot" apps/marketing/src/App.tsx — must show at least 4 results (2 per hero)
2. grep -n "data-accent" apps/marketing/src/App.tsx — must show lime (main), cobalt (startup), lime (legal)
3. grep -n "lockup-lavender.svg\|lockup-gradient-ink.svg" apps/marketing/src/App.tsx — must show both
4. grep -n "get on the list" apps/marketing/src/App.tsx — must show 1 result
5. grep -n "post a role" apps/marketing/src/App.tsx — must show 1 result
6. grep -in "unlock\|streamline\|revolutionary\|competitive landscape" apps/marketing/src/App.tsx — must show 0 results
7. grep -nE "letterSpacing.*['\"]0\.1em['\"]" apps/marketing/src/App.tsx — must show >= 1 result (BRAND-COPY-06)
8. grep -nE "fontWeight.*600" apps/marketing/src/App.tsx — must show >= 1 result (BRAND-COPY-06)
9. grep -c '#fbf7ef\|#FBF7EF' apps/marketing/src/styles.css — must show 0
10. grep -n "og:image" apps/marketing/index.html — must show meta tag
11. ls apps/marketing/public/logo/og-1200x630.png 2>/dev/null || ls apps/marketing/public/logo/og.svg — must show one file
12. cd apps/marketing && npm run build — must succeed
</verification>

<success_criteria>
- BRAND-LAYOUT-01: Apex / uses lavender bg + ink text + lime accent via data-accent="lime" on main wrapper
- BRAND-LAYOUT-02: /startups uses cobalt accent via data-accent="cobalt"; logo switches to lockup-lavender.svg
- BRAND-LAYOUT-03: data-accent="lime|cobalt" attribute drives section accent; .accent-dot/.accent-comma inherit via CSS
- BRAND-LAYOUT-04: Cream (#FAF6EB) on /privacy + /terms only; never mixed with lavender
- BRAND-LAYOUT-05: Grep audit confirms zero white/pure-black/gray hex literals on marketing surfaces
- BRAND-LOGO-03: Site header uses lockup-gradient-ink.svg on lavender background
- BRAND-LOGO-04: Cobalt section (/startups header) uses lockup-lavender.svg
- BRAND-LOGO-06: OG image (PNG or documented SVG fallback) exists + og:image + twitter:card meta tags in index.html; og:image path matches actual file on disk
- BRAND-LOGO-07: Logo clearspace maintained (header lockup >= 120px wide, heights respected)
- BRAND-COPY-01: Apex hero h1 = "internships, in your dms." with accent-comma and accent-dot spans
- BRAND-COPY-02: Apex subhead = "no resumes · no cover letters · just texts"
- BRAND-COPY-03: Apex CTA = "get on the list →" lime pill, ink text, border-radius pill
- BRAND-COPY-04: /startups h1 = "hire interns by text, not by tower of resumes." with accent spans
- BRAND-COPY-05: /startups CTA = "post a role →" cobalt pill, lavender text
- BRAND-COPY-06: Uppercase labels use Inter 600 + tracking 0.1em + uppercase CSS — verified by letterSpacing/fontWeight grep on App.tsx
- BRAND-COPY-07: All user-visible brand name references are lowercase "internjobs.ai"
- BRAND-COPY-08: Zero corp-speak grep matches
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/22-lakera-and-brand-refresh/22-04-SUMMARY.md`
</output>
