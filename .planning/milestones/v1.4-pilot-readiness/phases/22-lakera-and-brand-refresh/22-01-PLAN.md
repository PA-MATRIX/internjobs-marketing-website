---
phase: 22-lakera-and-brand-refresh
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/app/src/safety/screen.mjs
  - apps/parrot/workers/lib/safety.ts
  - apps/app/src/safety/screen.test.mjs
  - infra/LAKERA-PRICING.md
autonomous: false

coverage_diagram_not_applicable: true
# Reason: Backend-only plan — verifies a third-party API schema and writes a pricing doc; no user-facing navigation paths exist.

verification:
  surface: backend_only
  frontend_impact: false
  required_steps:
    - unit_tests

must_haves:
  truths:
    - "Lakera (Cisco AI Defense) v2 endpoint URL, auth header format, and response shape are verified live at platform.lakera.ai and documented in parser-block comment header in screen.mjs"
    - "Parser blocks in screen.mjs and safety.ts match actual v2 production schema (or are updated to match if drift is found)"
    - "screen.test.mjs still passes with LAKERA_GUARD_API_KEY set (VERIFY-01/02 live tests) and without it (VERIFY-03a/b/c fail-open tests)"
    - "Pricing tier (Community vs Pro) confirmed sufficient for 30k/month pilot volume and documented in infra/LAKERA-PRICING.md"
  artifacts:
    - path: "apps/app/src/safety/screen.mjs"
      provides: "Updated parser-block comment with verified v2 schema; parser logic updated if drift found"
      contains: "LAKERA_ENDPOINT"
    - path: "apps/parrot/workers/lib/safety.ts"
      provides: "Mirror of screen.mjs parser block; updated if drift found"
      contains: "LAKERA_ENDPOINT"
    - path: "apps/app/src/safety/screen.test.mjs"
      provides: "Regression-safe unit tests passing against verified schema"
    - path: "infra/LAKERA-PRICING.md"
      provides: "Pricing tier decision doc for 30k/month pilot"
      min_lines: 20
  key_links:
    - from: "apps/app/src/safety/screen.mjs"
      to: "https://api.lakera.ai/v2/guard"
      via: "fetch POST with Bearer LAKERA_GUARD_API_KEY"
      pattern: "LAKERA_ENDPOINT"
    - from: "apps/parrot/workers/lib/safety.ts"
      to: "https://api.lakera.ai/v2/guard"
      via: "fetch POST with Bearer env.LAKERA_GUARD_API_KEY"
      pattern: "LAKERA_ENDPOINT"
---

<objective>
Verify the Lakera Guard v2 API (post-Cisco-acquisition) is working as expected against the current code. Update parser blocks if the production schema drifts from what was assumed during v1.3. Document pricing tier sufficiency. Ensure the test suite still passes.

Purpose: v1.3 Phase 20 shipped the Lakera helpers with a "verify post-Cisco-acquisition" caveat in every parser-block comment. This plan closes that caveat by actually verifying it.
Output: Updated screen.mjs + safety.ts (parser comments + logic if needed), passing test suite, and infra/LAKERA-PRICING.md.
</objective>

<execution_context>
@~/.claude/rrr/workflows/execute-plan.md
@~/.claude/rrr/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/app/src/safety/screen.mjs
@apps/parrot/workers/lib/safety.ts
@apps/app/src/safety/screen.test.mjs
@infra/NEON-EXIT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Sign in to platform.lakera.ai and retrieve live API key</name>
  <action>
    This task requires human action — Claude cannot log in to the Lakera dashboard.

    1. Go to https://platform.lakera.ai (redirects to Cisco AI Defense if rebranded)
    2. Sign in with your registered account credentials
    3. Navigate to API Keys (typically Settings > API Keys or Developers)
    4. Copy the production API key (this is LAKERA_GUARD_API_KEY)
    5. Verify the pricing tier displayed on the dashboard:
       - Note the tier name (Community, Pro, Enterprise, or Cisco AI Defense equivalent)
       - Note the monthly request limit or quota
       - Note the per-request pricing (if shown)
       - Screenshot or copy the pricing page for the LAKERA-PRICING.md doc
    6. Navigate to the API documentation link from the dashboard (look for "Docs", "API Reference")
       - Find the current endpoint URL for the guard/screen API
       - Find the current auth header format
       - Find the current request body schema
       - Find the current response schema (especially: is `flagged` top-level? what fields are in `results[]`?)
    7. Make one live test call using curl to confirm the endpoint works:
       ```
       curl -X POST https://api.lakera.ai/v2/guard \
         -H "Authorization: Bearer YOUR_KEY" \
         -H "Content-Type: application/json" \
         -d '{"messages":[{"role":"user","content":"hello"}]}'
       ```
       Paste the full JSON response here when resuming.
    8. Save LAKERA_GUARD_API_KEY to Infisical (project 26995afd, env prod, path /internjobs-ai, key name LAKERA_GUARD_API_KEY) if not already there.
  </action>
  <resume-signal>Paste: (a) the curl response JSON, (b) the current endpoint URL, (c) the tier name and monthly request quota. Then type "ready to continue".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify schema, update parser blocks if drift found, update tests</name>
  <files>
    apps/app/src/safety/screen.mjs
    apps/parrot/workers/lib/safety.ts
    apps/app/src/safety/screen.test.mjs
  </files>
  <action>
    Using the live curl response and documentation from Task 1, perform the following:

    STEP A — Schema drift analysis:
    Compare the live response JSON against the parser blocks in both files. The v1.3 assumed schema is:
      { flagged: bool, results: [{ categories: { prompt_injection: <0-1>, ... }, flagged?: bool }] }

    Drift scenarios and what to do:
    - No drift: update only the parser-block comment header to say "VERIFIED [date]" with the actual endpoint + schema
    - Endpoint moved (e.g., to api.defense.cisco.com): update LAKERA_ENDPOINT constant in BOTH files
    - Response shape changed (e.g., different top-level field, different category key names): update parser logic in BOTH files to match actual shape while preserving the ScreenResult interface contract
    - Auth header format changed: update the Authorization header in BOTH files
    - project_id now required: add it to the request body in BOTH files

    STEP B — Update screen.mjs parser-block comment header (lines 1-22):
    Replace the existing "API schema (Lakera v2 — verify post-Cisco-acquisition before runtime use)" section with:
    ```
    // API schema (Lakera v2 — VERIFIED YYYY-MM-DD against platform.lakera.ai):
    //   POST https://[ACTUAL_ENDPOINT]
    //   Auth: Authorization: Bearer <LAKERA_GUARD_API_KEY>
    //   Body: { messages: [{ role: "user", content: <text> }] }
    //   Response: [PASTE ACTUAL SCHEMA FROM LIVE RESPONSE]
    //   Tier: [TIER NAME] — [QUOTA] req/month
    ```

    STEP C — Mirror the same comment update to safety.ts (lines 1-22).

    STEP D — If parser logic changes were needed, run the test suite:
    ```
    node --test apps/app/src/safety/screen.test.mjs
    ```
    (VERIFY-03a/b/c run without a key; set LAKERA_GUARD_API_KEY env var to run VERIFY-01/02 live tests)
    ```
    LAKERA_GUARD_API_KEY=<key> node --test apps/app/src/safety/screen.test.mjs
    ```
    All tests must pass. If VERIFY-01 fails (injection not flagged), the live Lakera instance may not have prompt injection detection enabled on this key — document this in the parser comment and in LAKERA-PRICING.md under "Known issues".

    CONVENTIONS:
    - Keep the fail-open contract comment ("this function NEVER throws") intact in both files
    - Keep the TIMEOUT_MS = 1000 value unchanged (architecture decision)
    - Keep the ScreenResult interface in safety.ts unchanged (it is the contract used by callers)
    - If the endpoint moved, update ONLY the LAKERA_ENDPOINT constant — no other structural changes
    - Structured log events use snake_case: lakera_screen_non2xx, lakera_timeout, lakera_network_error — keep these unchanged
  </action>
  <verify>
    Run: node --test apps/app/src/safety/screen.test.mjs
    Expected: 3 VERIFY-03 tests pass (no key needed)
    Run: LAKERA_GUARD_API_KEY=[key] node --test apps/app/src/safety/screen.test.mjs
    Expected: All 5 tests pass including VERIFY-01 (injection flagged) and VERIFY-02 (benign passes)
    Grep: grep -n "VERIFIED" apps/app/src/safety/screen.mjs
    Expected: Line in the parser-block comment contains "VERIFIED" with today's date
  </verify>
  <done>
    - Both screen.mjs and safety.ts have updated parser-block comment headers with "VERIFIED [date]" and the actual endpoint/schema
    - If drift was found: parser logic updated in both files to match production reality
    - All 5 screen.test.mjs tests pass (3 fail-open + 2 live API)
    - No structural or interface changes beyond what was necessary to match the live schema
  </done>
</task>

<task type="auto">
  <name>Task 3: Write infra/LAKERA-PRICING.md with tier decision</name>
  <files>
    infra/LAKERA-PRICING.md
  </files>
  <action>
    Create infra/LAKERA-PRICING.md. This file sits next to infra/NEON-EXIT.md. Use the same documentation style (markdown, factual, no enterprise fluff).

    The file must contain:

    ```markdown
    # Lakera Guard Pricing — InternJobs.ai Pilot

    **Verified:** YYYY-MM-DD
    **Account:** platform.lakera.ai (Cisco AI Defense)
    **Current tier:** [TIER NAME]

    ## Pilot Volume Estimate

    | Channel | Monthly requests (est.) | Notes |
    |---------|------------------------|-------|
    | Student SMS (inbound) | ~15,000 | 500 students × 30 msg/student |
    | Employee email (inbound) | ~15,000 | 50 employees × 10 emails/day × 30 days |
    | **Total** | **~30,000** | Pilot target |

    ## Tier Assessment

    [TIER NAME] provides [QUOTA] requests/month.
    30k pilot volume fits within [quota analysis: e.g., "within the 100k/month free tier" or "at $X/1000 requests, 30k = $Y/month"].

    **Decision: [Community / Pro / Cisco AI Defense Enterprise] is sufficient / not sufficient for the 30k/month pilot.**

    [If not sufficient: recommended upgrade path and cost.]

    ## Endpoint Verified

    - URL: [ACTUAL_ENDPOINT]
    - Auth: Bearer header
    - Schema: v2 (verified YYYY-MM-DD)

    ## Known Issues

    [None / List any findings from VERIFY-01/02 test run, e.g., if prompt injection detection was not triggered]

    ## Next Review

    Review at 10k MAU or if Cisco changes the endpoint again.
    ```

    Fill in all bracketed values with actual data from Task 1. If pricing is not publicly visible from the dashboard, note "pricing on request — contact Cisco AI Defense sales".
  </action>
  <verify>
    File exists: ls -la infra/LAKERA-PRICING.md
    Contains tier decision: grep -i "sufficient" infra/LAKERA-PRICING.md
    Contains endpoint: grep "api\." infra/LAKERA-PRICING.md
    Line count >= 20: wc -l infra/LAKERA-PRICING.md
  </verify>
  <done>
    infra/LAKERA-PRICING.md exists with actual tier name, quota, 30k estimate analysis, and a clear "sufficient / not sufficient" decision. All bracketed placeholders replaced with real values from Task 1.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. node --test apps/app/src/safety/screen.test.mjs — all VERIFY-03 tests pass without a key
2. LAKERA_GUARD_API_KEY=[key] node --test apps/app/src/safety/screen.test.mjs — all 5 tests pass
3. grep "VERIFIED" apps/app/src/safety/screen.mjs — shows date stamp in parser-block comment
4. grep "VERIFIED" apps/parrot/workers/lib/safety.ts — shows date stamp in parser-block comment
5. ls infra/LAKERA-PRICING.md — file exists, wc shows >= 20 lines
6. grep -i "sufficient" infra/LAKERA-PRICING.md — shows the tier decision line
</verification>

<success_criteria>
- LAKERA-V2-01: parser-block comment in screen.mjs updated with "VERIFIED [date]", actual endpoint URL, auth format, response shape
- LAKERA-V2-02: parser blocks in both screen.mjs and safety.ts reflect production reality (updated if drift found); screen.test.mjs still passes all 5 tests
- LAKERA-V2-03: infra/LAKERA-PRICING.md exists with tier confirmation for 30k/month pilot
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-pilot-readiness/phases/22-lakera-and-brand-refresh/22-01-SUMMARY.md`
</output>
