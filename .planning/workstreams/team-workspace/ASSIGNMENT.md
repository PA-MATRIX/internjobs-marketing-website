# team-workspace Assignment

Milestone: v1.4
GitHub org: PA-MATRIX
GitHub team: @PA-MATRIX/team-workspace
Branch: rrr/v1.4/team-workspace
Draft PR: (created during dispatch)
Sprite: rrr-internjobs-marketing-website-v1-4-team-workspace

## Scope

Owns the worker-side **Workspace** app and related infrastructure (code paths use
`apps/parrot/`; verbal/written reference is "Workspace" to avoid confusion with
the unrelated deleted Neon project named "parrot"):

- `apps/parrot/` — Workspace Worker (workspace.internjobs.ai)
- `apps/agentic-inbox/`
- `apps/parrot-mattermost/`
- `apps/mattermost-proxy/`
- `infra/graph-api/`
- `infra/mattermost-db/`

## Assigned Phases

- None assigned yet.

## Operating Rules

- Work on the assigned branch/PR, not directly on `main`.
- Use RRR commands with `--team` so root `.planning/STATE.md` is not mutated.
- Submit with `$rrr-submit-phase <phase> --team team-workspace` when ready.
- GitHub reviews, required checks, and CODEOWNERS decide merge authority.
